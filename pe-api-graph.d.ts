// ProEssentialsJS -- Copyright 1994-2026 Gigasoft, Inc. All rights reserved.
// Commercial product, free for commercial use under USD 250,000 annual
// revenue. See PEJS-LICENSE.md -- https://www.gigasoft.com
//
// GENERATED BY tools\gen-web-api.py -- DO NOT EDIT.

// Declarations for the pieces that arrive by <script> tag, referenced
// here so an editor finds them without a jsconfig.json.
/// <reference path="./pe-control.d.ts" />
/// <reference path="./pe-events.d.ts" />
/// <reference path="./pe-tooltip.d.ts" />
/// <reference path="./proessentials.d.ts" />

/** Wasm memory the facade owns and the engine reads directly.
 *  Never store `array` -- it is re-derived per access because heap
 *  growth detaches typed-array views while the pointer survives.
 *  Release it from the chart (useDataAtLocation() with no argument)
 *  BEFORE calling free(). */
export interface PeBlock {
  /** Address of this block in wasm memory. */
  readonly ptr: number;
  /** Element count. */
  readonly length: number;
  /** A view over the block. Re-derived per access -- never store it. */
  readonly array: Float64Array | Float32Array | Uint32Array;
  /** Write values into the block, optionally from an offset. */
  set(values: ArrayLike<number>, at?: number): PeBlock;
  /** Release the memory. Detach it from the chart first. */
  free(): void;
}
export interface PeArray1D<T> {
  [index: number]: T;
  /** Element count. */
  readonly length: number;
  /** Copy values into the chart's data area. */
  load(values: ArrayLike<number>, count?: number): void;
  /** .NET's own spelling of load(). Same call, both names live. */
  CopyFrom(values: ArrayLike<number>, count?: number): void;
  /** .NET Clear() / Clear(newSize). No argument empties the array. */
  clear(newSize?: number): number;
  /** .NET AppendData(data, amountPerSubset) -- the strip-chart write. */
  append(values: ArrayLike<number>, count?: number): number;
  /** .NET's own spelling of append(). Same call, both names live. */
  AppendData(values: ArrayLike<number>, count?: number): number;
  /** Allocate a block sized and typed for this array. */
  allocate(n: number): PeBlock;
  /** .NET UseDataAtLocation(source, nBufferSize). No argument releases. */
  useDataAtLocation(block?: PeBlock | null, count?: number): number;
  /** .NET UseJaggedDataAtLocation(source, nSubset). Requires JaggedData. */
  useJaggedDataAtLocation(block: PeBlock | null | undefined, nSubset?: number, count?: number): number;
  /** .NET FastCopyFrom(source, nAmount) -- copy nAmount elements from a source that may be larger than the chart's data area. */
  fastCopyFromCount(values: ArrayLike<number>, nAmount?: number): number;
  /** .NET FastCopyFrom(source, nSubset, nAmount) -- partial copy into one subset. Non-jagged. */
  fastCopyFrom(values: ArrayLike<number>, nSubset: number, nAmount?: number): number;
  /** .NET FastCopyFromJagged(source, nSubset) -- copies one subset and sets its length. Requires JaggedData. */
  fastCopyFromJagged(values: ArrayLike<number>, nSubset: number): number;
  /** .NET Initialize(dataValue) -- fills every subset. Works with JaggedData true or false. */
  initialize(value: number): number;
  /** .NET InitializeJagged(nSubset, dataValue) -- fills ONE subset. Works with JaggedData true or false. */
  initializeJagged(nSubset: number, value: number): number;
  /** .NET CopyFromJagged(source, nSubset, nElements) -- sets the subset's size then copies. nElements 0 uses the whole source. */
  copyFromJagged(values: ArrayLike<number>, nSubset: number, nElements?: number): number;
  /** .NET CopyFromJaggedTo(source, nSubset, nInsertIndex) -- copies at an index, growing the subset if needed. */
  copyFromJaggedTo(values: ArrayLike<number>, nSubset: number, nInsertIndex: number): number;
  /** .NET AppendSubset(data, nAmountPerPoint) -- adds subset items per POINT, shifting top-to-bottom. */
  appendSubset(values: ArrayLike<number>, nAmountPerPoint?: number): number;
  /** .NET AppendFromJagged(source, nSubset) -- shifts within ONE subset and appends. Does not change its size. */
  appendFromJagged(values: ArrayLike<number>, nSubset: number): number;
  /** .NET GetLength(dimension): 0 = Subsets, 1 = Points. RETURNS 0 when JaggedData is true -- rows differ, so use GetJaggedPointsX/Y. */
  getLength(dimension: number): number;
  /** .NET Copy() -- reads this array out. */
  copy(): number[] | number[][];
  /** .NET CopyJagged(nSubset) -- reads ONE subset out at its own length. Requires JaggedData. */
  copyJagged(nSubset: number): number[];
}
export interface PeArray2D<T> {
  [subset: number]: { [point: number]: T };
  /** Copy values into the chart's data area, subsets by points. */
  load(values: ArrayLike<number>, subsets: number, points: number): void;
  /** .NET's own spelling of load(). Same call, both names live. */
  CopyFrom(values: ArrayLike<number>, subsets: number, points: number): void;
  /** Total element count. Under JaggedData use GetJaggedPointsX/Y -- one number cannot describe ragged rows. */
  readonly length: number;
  /** .NET Clear() / Clear(newSize). No argument empties the array. */
  clear(newSize?: number): number;
  /** .NET AppendData(data, amountPerSubset) -- the strip-chart write. */
  append(values: ArrayLike<number>, count?: number): number;
  /** .NET's own spelling of append(). Same call, both names live. */
  AppendData(values: ArrayLike<number>, count?: number): number;
  /** Allocate a block sized and typed for this array. */
  allocate(n: number): PeBlock;
  /** .NET UseDataAtLocation(source, nBufferSize). No argument releases. */
  useDataAtLocation(block?: PeBlock | null, count?: number): number;
  /** .NET UseJaggedDataAtLocation(source, nSubset). Requires JaggedData. */
  useJaggedDataAtLocation(block: PeBlock | null | undefined, nSubset?: number, count?: number): number;
  /** .NET FastCopyFrom(source, nAmount) -- copy nAmount elements from a source that may be larger than the chart's data area. */
  fastCopyFromCount(values: ArrayLike<number>, nAmount?: number): number;
  /** .NET FastCopyFrom(source, nSubset, nAmount) -- partial copy into one subset. Non-jagged. */
  fastCopyFrom(values: ArrayLike<number>, nSubset: number, nAmount?: number): number;
  /** .NET FastCopyFromJagged(source, nSubset) -- copies one subset and sets its length. Requires JaggedData. */
  fastCopyFromJagged(values: ArrayLike<number>, nSubset: number): number;
  /** .NET Initialize(dataValue) -- fills every subset. Works with JaggedData true or false. */
  initialize(value: number): number;
  /** .NET InitializeJagged(nSubset, dataValue) -- fills ONE subset. Works with JaggedData true or false. */
  initializeJagged(nSubset: number, value: number): number;
  /** .NET CopyFromJagged(source, nSubset, nElements) -- sets the subset's size then copies. nElements 0 uses the whole source. */
  copyFromJagged(values: ArrayLike<number>, nSubset: number, nElements?: number): number;
  /** .NET CopyFromJaggedTo(source, nSubset, nInsertIndex) -- copies at an index, growing the subset if needed. */
  copyFromJaggedTo(values: ArrayLike<number>, nSubset: number, nInsertIndex: number): number;
  /** .NET AppendSubset(data, nAmountPerPoint) -- adds subset items per POINT, shifting top-to-bottom. */
  appendSubset(values: ArrayLike<number>, nAmountPerPoint?: number): number;
  /** .NET AppendFromJagged(source, nSubset) -- shifts within ONE subset and appends. Does not change its size. */
  appendFromJagged(values: ArrayLike<number>, nSubset: number): number;
  /** .NET GetLength(dimension): 0 = Subsets, 1 = Points. RETURNS 0 when JaggedData is true -- rows differ, so use GetJaggedPointsX/Y. */
  getLength(dimension: number): number;
  /** .NET Copy() -- reads this array out. */
  copy(): number[] | number[][];
  /** .NET CopyJagged(nSubset) -- reads ONE subset out at its own length. Requires JaggedData. */
  copyJagged(nSubset: number): number[];
}

export declare enum AllowDataLabels {
  None = 0,
  DataValues = 1,
  PointLabels = 2,
  DataPointLabels = 3,
}
export declare enum AllowUserInterface {
  None = 0,
  All = 1,
  DisableKeyboard = 2,
  DisableMouse = 3,
}
export declare enum AllowZooming {
  None = 0,
  Horizontal = 1,
  Vertical = 2,
  HorzAndVert = 3,
  HorizontalMb = 4,
  VerticalMb = 5,
  HorzAndVertMb = 6,
}
export declare enum AnnotationHotSpot {
  NoHotSpot = 0,
  GraphOnly = 1,
  GraphAndZoomWindow = 2,
  ZoomWindowOnly = 3,
}
export declare enum AnnotationInFront {
  Default = 0,
  Behind = 1,
  InFront = 2,
  Hide = 3,
}
export declare enum AnnotationZoomWindow {
  GraphOnly = 0,
  GraphAndZoomWindow = 1,
  ZoomWindowOnly = 2,
}
export declare enum AutoExplode {
  None = 0,
  AllSubsets = 1,
  IndividualSubsets = 2,
}
export declare enum AutoStatSubsets {
  SumPerPoint = 51,
  MinAllPoints = 1,
  MinPerPoint = 52,
  MaxAllPoints = 2,
  MaxPerPoint = 53,
  AverageAllPoints = 3,
  AveragePerPoint = 54,
  Plus1StdDevAllPoints = 4,
  Plus1StdDevPerPoint = 55,
  Plus2StdDevAllPoints = 5,
  Plus2StdDevPerPoint = 56,
  Plus3StdDevAllPoints = 6,
  Plus3StdDevPerPoint = 57,
  Minus1StdDevAllPoints = 7,
  Minus1StdDevPerPoint = 58,
  Minus2StdDevAllPoints = 8,
  Minus2StdDevPerPoint = 59,
  Minus3StdDevAllPoints = 9,
  Minus3StdDevPerPoint = 60,
  ParetoAscending = 90,
  ParetoDescending = 91,
}
export declare enum AutoXData {
  IncludeSatSun = 0,
  NoWeekends = 1,
}
export declare enum AxisBorderType {
  DefaultBorder = 0,
  DropShadow = 1,
  ThinLine = 2,
  NoBorder = 3,
  Inset = 4,
  ThickLine = 5,
}
export declare enum AxisNumericFormat {
  Default = 0,
  ExpNotation = 1,
  ExpNotation3x = 2,
}
export declare enum BestFitDegree {
  Second = 0,
  Third = 1,
  Fourth = 2,
}
export declare enum BevelLighting {
  HighLowLights = 0,
  BrightLight = 1,
  MediumLowLights = 2,
}
export declare enum BevelStyle {
  None = 0,
  ThickSmooth = 1,
  MediumSmooth = 2,
  ThinSmooth = 3,
  ThickBevel = 4,
  MediumNevel = 5,
  ThinBevel = 6,
}
export declare enum BitmapStyle {
  NoBmp = 0,
  StretchBlt = 1,
  TiledBitBlt = 2,
  BitBltTopLeft = 3,
  BitBltTopCenter = 4,
  BitBltTopRight = 5,
  BitBltBottomLeft = 6,
  BitBltBottomCenter = 7,
  BitBltBottomRight = 8,
  BitBltCenter = 9,
  BitBltZooming = 10,
}
export declare enum BubbleGradientStyle {
  None = 0,
  Radial = 1,
  Beveled = 2,
}
export declare enum BubbleSize {
  Small = 0,
  Medium = 1,
  Large = 2,
}
export declare enum Composite2D3D {
  BothFrontAndBackLayers = 0,
  Background = 1,
  Foreground = 2,
}
export declare enum ContourColorSet {
  NoColors = 0,
  ContourColors = 1,
  BlueGreenGrey = 2,
  BlueGreenRed = 3,
  GreenYellowRed = 4,
  BlueGreenYellowRed = 5,
  BlueGreenYellowOrangeRed = 6,
  VioletBlueGreenYellowOrangeRed = 7,
  BlueCyanGreenLimeTanBrownDarkBrown = 8,
  BlueCyanGreenYellowBrownWhite = 9,
  DarkBlueBlueCyanMedCyanCyanGreenDarkGreenGreenLimeDarkLimeTanBrownDarkBrown = 10,
}
export declare enum ContourLegendPrecision {
  Default = 0,
  ZeroDecimals = 1,
  OneDecimal = 2,
  TwoDecimals = 3,
  ThreeDecimals = 4,
  FourDecimals = 5,
  FiveDecimals = 6,
}
export declare enum CursorMode {
  NoCursor = 0,
  Point = 1,
  DataCross = 2,
  DataSquare = 3,
  FloatingY = 4,
  FloatingXY = 5,
  FloatingXOnly = 6,
  FloatingYOnly = 7,
}
export declare enum CursorPromptLocation {
  Left = 0,
  Right = 1,
  ToolTip = 2,
  Text = 3,
}
export declare enum CursorPromptStyle {
  None = 0,
  XValue = 1,
  YValue = 2,
  XYValues = 3,
  XYZValues = 4,
  ZValue = 5,
}
export declare enum CurveGranularity {
  Coarse = 0,
  Medium = 1,
  Fine = 2,
}
export declare enum CustomMenu {
  Show = 0,
  Grayed = 1,
  Hide = 2,
}
export declare enum CustomMenuLocation {
  Top = 0,
  AboveSeparator = 1,
  BelowSeparator = 2,
  Bottom = 3,
}
export declare enum CustomMenuState {
  UnChecked = 0,
  Checked = 1,
}
export declare enum DataAndOrLabel {
  Data = 0,
  DataAndLabels = 1,
}
export declare enum DataLabelType {
  Percentage = 0,
  Value = 1,
}
export declare enum DataPrecision {
  NoDecimals = 0,
  OneDecimal = 1,
  TwoDecimals = 2,
  ThreeDecimals = 3,
  FourDecimals = 4,
  FiveDecimals = 5,
  SixDecimals = 6,
  SevenDecimals = 7,
}
export declare enum DataShadows {
  None = 0,
  Shadows = 1,
  ThreeDimensional = 2,
}
export declare enum DateTimeMode {
  None = 0,
  VbAndDelphi = 1,
}
export declare enum DayLabelType {
  ThreeCharacters = 0,
  OneCharacter = 1,
  NoDayPrompt = 2,
  NoDayNumber = 3,
}
export declare enum DefOrientation {
  DriverDefault = 0,
  LandScape = 1,
  Portrait = 2,
}
export declare enum DrawCursorAction {
  ShowCursor = 0,
  MoveCursor = 1,
  HideCursor = 2,
}
export declare enum DuplicateData {
  None = 0,
  PointIncrement = 1,
  SubsetIncrement = 2,
}
export declare enum EmfDC {
  DisplayDpiAdjusted = 0,
  Display = 1,
  Printer = 2,
}
export declare enum EmfType {
  Gdi = 0,
  GdiPlus = 1,
  PlusDual = 2,
  PlusOnly = 3,
}
export declare enum ExportDestDef {
  Clipboard = 0,
  File = 1,
  Printer = 2,
}
export declare enum ExportPrecision {
  Current = 0,
  Maximum = 1,
}
export declare enum ExportSizeDef {
  NoSizeOrPixel = 0,
  Millimeters = 1,
  Inches = 2,
  Points = 3,
}
export declare enum ExportTypeDef {
  Metafile = 0,
  Bmp = 1,
  Jpeg = 2,
  Png = 3,
  Text = 4,
  Emf = 5,
}
export declare enum Filter2D {
  Disable = 0,
  Fastest = 1,
  Fast = 2,
  Optimum = 3,
  Quality = 4,
  Slow = 5,
  Slowest = 6,
}
export declare enum FontSize {
  Large = 0,
  Medium = 1,
  Small = 2,
}
export declare enum ForceGridLines {
  Auto = 0,
  Show = 1,
  Hide = 2,
}
export declare enum ForceVerticalPoints {
  Auto = 0,
  Vertical = 1,
  Horizontal = 2,
  Slanted = 3,
}
export declare enum GradientBoundary {
  Data = 0,
  Axis = 1,
}
export declare enum GradientStyle {
  NoGradient = 0,
  Vertical = 1,
  Horizontal = 2,
  LinearBarVertical = 3,
  LinearBarHorizontal = 4,
  LinearBarDown = 5,
  LinearBarUp = 6,
  LinearDiagonalDown = 7,
  LinearDiagonalUp = 8,
  RectangleCross = 9,
  RectanglePlus = 10,
  RadialCentered = 11,
  RadialBottomRight = 12,
  RadialTopRight = 13,
  RadialBottomLeft = 14,
  RadialTopLeft = 15,
}
export declare enum GraphAnnotMoveable {
  NotMoveable = 0,
  Pointer = 1,
}
export declare enum GraphAnnotPolyDataPlotMethod {
  YHeight = 0,
  PolygonColor = 1,
}
export declare enum GraphAnnotationTextJustification {
  HorzCenteredAbove = 0,
  HorzLeftAbove = 1,
  HorzRightAbove = 2,
  HorzLeftBelow = 3,
  HorzRightBelow = 4,
  HorzCenteredBelow = 5,
  VertLeftBelow = 6,
  VertRightBelow = 7,
  VertRightAbove = 8,
  VertLeftAbove = 9,
  VertLeftCentered = 10,
  VertRightCentered = 11,
  VertCenteredAbove = 12,
  VertCenteredBelow = 13,
}
export declare enum GraphAnnotationType {
  NoSymbol = 0,
  Plus = 1,
  Cross = 2,
  Dot = 3,
  DotSolid = 4,
  Square = 5,
  SquareSolid = 6,
  Diamond = 7,
  DiamondSolid = 8,
  UpTriangle = 9,
  UpTriangleSolid = 10,
  DownTriangle = 11,
  DownTriangleSolid = 12,
  SmallPlus = 13,
  SmallCross = 14,
  SmallDot = 15,
  SmallDotSolid = 16,
  SmallSquare = 17,
  SmallSquareSolid = 18,
  SmallDiamond = 19,
  SmallDiamondSolid = 20,
  SmallUpTriangle = 21,
  SmallUpTriangleSolid = 22,
  SmallDownTriangle = 23,
  SmallDownTriangleSolid = 24,
  LargePlus = 25,
  LargeCross = 26,
  LargeDot = 27,
  LargeDotSolid = 28,
  LargeSquare = 29,
  LargeSquareSolid = 30,
  LargeDiamond = 31,
  LargeDiamondSolid = 32,
  LargeUpTriangle = 33,
  LargeUpTriangleSolid = 34,
  LargeDownTriangle = 35,
  LargeDownTriangleSolid = 36,
  Pointer = 37,
  ThinSolidLine = 38,
  DashLine = 39,
  DotLine = 40,
  DashDotLine = 41,
  DashDotDotLine = 42,
  MediumSolidLine = 43,
  ThickSolidLine = 44,
  LineContinue = 45,
  TopLeft = 46,
  BottomRight = 47,
  RectThin = 48,
  RectDash = 49,
  RectDot = 50,
  RectDashDot = 51,
  RectDashDotD = 52,
  RectMedium = 53,
  RectThick = 54,
  RectFill = 55,
  RoundRectThin = 56,
  RoundRectDash = 57,
  RoundRectDot = 58,
  RoundRectDashDot = 59,
  RoundRectDashDotDot = 60,
  RoundRectMedium = 61,
  RoundRectThick = 62,
  RoundRectFill = 63,
  EllipseThin = 64,
  EllipseDash = 65,
  EllipseDot = 66,
  EllipseDashDot = 67,
  EllipseDashDotDot = 68,
  EllipseMedium = 69,
  EllipseThick = 70,
  EllipseFill = 71,
  Dash = 72,
  Pixel = 73,
  StartPoly = 74,
  AddPolyPoint = 75,
  EndPolygon = 76,
  EndPolyLineThin = 77,
  EndPolyLineMedium = 78,
  EndPolylineThick = 79,
  EndPolylineDash = 80,
  EndPolylineDot = 81,
  EndPolylineDashDot = 82,
  EndPolylineDashDotDot = 83,
  StartText = 84,
  AddText = 85,
  Paragraph = 86,
  MediumThinSolid = 87,
  MediumThickSolid = 88,
  ExtraThickSolid = 89,
  ExtraThinSolid = 90,
  ArrowN = 92,
  ArrowNE = 93,
  ArrowE = 94,
  ArrowSE = 95,
  ArrowS = 96,
  ArrowSW = 97,
  ArrowW = 98,
  ArrowNW = 99,
  SmallObstacle = 100,
  MediumObstacle = 101,
  LargeObstacle = 102,
  RectObstacle = 103,
  NoSymbolMovable = 104,
  MediumThinDash = 105,
  MediumThinDot = 106,
  MediumThinDashDot = 107,
  MediumThinDashDotDot = 108,
  MediumDash = 109,
  MediumDot = 110,
  MediumDashDot = 111,
  MediumDashDotDot = 112,
  MediumThickDash = 113,
  MediumThickDot = 114,
  MediumThickDashDot = 115,
  MediumThickDashDotDot = 116,
  ThickDash = 117,
  ThickDot = 118,
  ThickDashDot = 119,
  ThickDashDotDot = 120,
  ExtraThickDash = 121,
  ExtraThickDot = 122,
  ExtraThickDashDot = 123,
  ExtraThickDashDotDot = 124,
  RectMediumDash = 125,
  RectMediumDot = 126,
  RectMediumDashDot = 127,
  RectMediumDashDotDot = 128,
  RectThickDash = 129,
  RectThickDot = 130,
  RectThickDashDot = 131,
  RectThickDashDotDot = 132,
  RoundRectMediumDash = 133,
  RoundRectMediumDot = 134,
  RoundRectMediumDashDot = 135,
  RoundRectMediumDashDotDot = 136,
  RoundRectThickDash = 137,
  RoundRectThickDot = 138,
  RoundRectThickDashDot = 139,
  RoundRectThickDashDotDot = 140,
  EllipseMediumDash = 141,
  EllipseMediumDot = 142,
  EllipseMediumDashDot = 143,
  EllipseMediumDashDotDot = 144,
  EllipseThickDash = 145,
  EllipseThickDot = 146,
  EllipseThickDashDot = 147,
  EllipseThickDashDotDot = 148,
  EndPolyLineMediumDash = 149,
  EndPolyLineMediumDot = 150,
  EndPolyLineMediumDashDot = 151,
  EndPolyLineMediumDashDotDot = 152,
  EndPolyLineThickDash = 153,
  EndPolyLineThickDot = 154,
  EndPolyLineThickDashDot = 155,
  EndPolyLineThickDashDotDot = 156,
  RectHatchHorizontal = 157,
  RectHatchVertical = 158,
  RectHatchFdiagonal = 159,
  RectHatchBdiagonal = 160,
  RectHatchCross = 161,
  RectHatchDiagCross = 162,
  RoundRectHatchHorizontal = 163,
  RoundRectHatchVertical = 164,
  RoundRectHatchFdiagonal = 165,
  RoundRectHatchBdiagonal = 166,
  RoundRectHatchCross = 167,
  RoundRectHatchDiagCross = 168,
  EllipseHatchHorizontal = 169,
  EllipseHatchVertical = 170,
  EllipseHatchFdiagonal = 171,
  EllipseHatchBdiagonal = 172,
  EllipseHatchCross = 173,
  EllipseHatchDiagCross = 174,
  EndPolygonHatchHorizontal = 175,
  EndPolygonHatchVertical = 176,
  EndPolygonHatchFdiagonal = 177,
  EndPolygonHatchBdiagonal = 178,
  EndPolygonHatchCross = 179,
  EndPolygonHatchDiagCross = 180,
  VectorSmall = 181,
  VectorMedium = 182,
  VectorLarge = 183,
  ArrowSmall = 184,
  ArrowMedium = 185,
  ArrowLarge = 186,
  PointerVectorSmall = 187,
  PointerVectorMedium = 188,
  PointerVectorLarge = 189,
  PointerArrowSmall = 190,
  PointerArrowMedium = 191,
  PointerArrowLarge = 192,
  NullPen = 193,
  AngledText = 194,
  AngledTextCenter = 195,
  AngledTextTop = 196,
  Top = 197,
  Bottom = 198,
  Left = 199,
  Right = 200,
  PointerArrowSolidSmall = 201,
  PointerArrowSolidMedium = 202,
  PointerArrowSolidLarge = 203,
  ArrowSolidSmall = 204,
  ArrowSolidMedium = 205,
  ArrowSolidLarge = 206,
  MajorMinorRadii = 219,
  MajorDirection = 220,
  MinorDirection = 221,
  AxisDirection = 222,
  AxisAngles = 223,
  TextAtPixel = 228,
  MajorMinorInsideRadii = 229,
  InsideRectThin = 230,
  InsideRectMedium = 231,
  InsideRectThick = 232,
  InsideRectFill = 233,
  CylinderStart = 234,
  Cylinder = 235,
  BoxStart = 236,
  Box = 237,
  Sphere = 238,
  SphereSmall = 239,
  SphereLarge = 240,
  AngledTextLeftBottom = 241,
  AngledTextLeftTop = 242,
  AngledTextRightBottom = 243,
  AngledTextRightTop = 244,
  TextBoundingBox = 245,
}
export declare enum GraphPlottingMethod {
  Line = 0,
  Bar = 1,
  Point = 2,
  Area = 3,
  AreaStacked = 4,
  AreaStackedPercent = 5,
  BarStacked = 6,
  BarStackedPercent = 7,
  PointsPlusBestFitLine = 8,
  PointsPlusBestFitLineGraphed = 9,
  PointsPlusBestFitCurve = 10,
  PointsPlusBestFitCurveGraphed = 11,
  PointsPlusSpline = 12,
  Spline = 13,
  Histogram = 14,
  SpecificPlotMode = 15,
  PointsPlusLine = 16,
  HorizontalBar = 17,
  HorizontalStackedBar = 18,
  HorizontalStackedBarPercent = 19,
  Step = 20,
  Ribbon = 21,
  LineStacked = 22,
  LineStackedPercent = 23,
  DemographicPyramid = 24,
  SplineArea = 25,
  SplineRibbon = 26,
}
export declare enum GraphPlottingMethodII {
  Line = 0,
  Point = 1,
  PointsPlusBestFitLine = 2,
  PointsPlusBestFitLineGraphed = 3,
  PointsPlusBestFitCurve = 4,
  PointsPlusBestFitCurveGraphed = 5,
  PointsPlusSpline = 6,
  Spline = 7,
  PointsPlusLine = 8,
  Step = 9,
  Ribbon = 10,
}
export declare enum GraphPlottingMethods {
  Line = 0,
  Bar = 1,
  Point = 2,
  Area = 3,
  AreaStacked = 4,
  AreaStackedPercent = 5,
  BarStacked = 6,
  BarStackedPercent = 7,
  PointsPlusBestFitLine = 8,
  PointsPlusBestFitLineGraphed = 9,
  PointsPlusBestFitCurve = 13,
  PointsPlusBestFitCurveGraphed = 14,
  PointsPlusSpline = 15,
  Spline = 16,
  PointsPlusLine = 17,
  HorizontalBar = 18,
  HorizontalStackedBar = 19,
  HorizontalStackedBarPercent = 20,
  Step = 21,
  Ribbon = 22,
  HighLowBar = 25,
  HightLowLine = 26,
  HighLowClose = 27,
  OpenHighLowClose = 28,
  BoxPlot = 29,
  LineStacked = 30,
  LineStackedPercent = 31,
  HighLowArea = 33,
  SplineArea = 35,
  SplineRibbon = 36,
  OnRightAxis = 1000,
}
export declare enum GraphPlusTable {
  Graph = 0,
  Table = 1,
  Both = 2,
}
export declare enum GraphTAAxisLocation {
  TopFullWidth = 0,
  TopLeft = 1,
  TopCenter = 2,
  TopRight = 3,
  BottomFullWidth = 4,
  BottomLeft = 5,
  BottomCenter = 6,
  BottomRight = 7,
  TopTableSpaced = 8,
  BottomTableSpaced = 9,
  NewRow = 100,
}
export declare enum GraphTALocation {
  TopCenter = 0,
  TopLeft = 1,
  LeftCenter = 2,
  BottomLeft = 3,
  BottomCenter = 4,
  BottomRight = 5,
  RightCenter = 6,
  TopRight = 7,
  InsideTopCenter = 8,
  InsideTopLeft = 9,
  InsideLeftCenter = 10,
  InsideBottomLeft = 11,
  InsideBottomCenter = 12,
  InsideBottomRight = 13,
  InsideRightCenter = 14,
  InsideTopRight = 15,
  InsidePixelUnits = 16,
  InsideAxis = 100,
  InsideAxis0 = 100,
  InsideAxis1 = 101,
  InsideAxis2 = 102,
  InsideAxis3 = 103,
  InsideAxis4 = 104,
  InsideAxis5 = 105,
  OutsideAxis = 200,
  OutsideAxis0 = 200,
  OutsideAxis1 = 201,
  OutsideAxis2 = 202,
  OutsideAxis3 = 203,
  OutsideAxis4 = 204,
  OutsideAxis5 = 205,
  InsideTable = 300,
  OverlapInsideAxis = 400,
  OverlapInsideAxis0 = 400,
  OverlapInsideAxis1 = 401,
  OverlapInsideAxis2 = 402,
  OverlapInsideAxis3 = 403,
  OverlapInsideAxis4 = 404,
  OverlapInsideAxis5 = 405,
}
export declare enum GridLineControl {
  Both = 0,
  YAxis = 1,
  XAxis = 2,
  None = 3,
}
export declare enum GridNumberType {
  X = 0,
  Y = 1,
  RightY = 2,
  TopX = 3,
  Z = 4,
  ZoomWindow = 5,
}
export declare enum GridStyle {
  Thin = 0,
  Thick = 1,
  Dot = 2,
  Dash = 3,
  OnePixel = 4,
}
export declare enum GroupingPercent {
  NoGrouping = 0,
  OnePercent = 1,
  TwoPercent = 2,
  ThreePercent = 3,
  FourPercent = 4,
  FivePercent = 5,
}
export declare enum HScrollStyle {
  ScrollingPointLabels = 0,
  StationalPointLabels = 1,
}
export declare enum HatchType {
  NoHatching = 0,
  Horizontal = 1,
  Vertical = 2,
  FDiagonal = 3,
  BDiagonal = 4,
  Cross = 5,
  DiagonalCross = 6,
}
export declare enum HideIntersectingText {
  NoHiding = 0,
  Hide = 1,
  HidePointerTopRight = 2,
  HidePointerTopLeft = 3,
  HidePointerBottomLeft = 4,
  HidePointerBottomRight = 5,
}
export declare enum HotSpotSize {
  Small = 0,
  Medium = 1,
  Large = 2,
}
export declare enum HotSpotType {
  None = 0,
  Subset = 1,
  Point = 2,
  Graph = 3,
  Table = 4,
  DataPoint = 5,
  Annotation = 6,
  XAxisAnnotation = 7,
  YAxisAnnotation = 8,
  HorzLineAnnotation = 9,
  VertLineAnnotation = 10,
  MainTitle = 11,
  SubTitle = 12,
  MultiSubTitle = 13,
  MultiBottomTitle = 14,
  YAxisLabel = 15,
  XAxisLabel = 16,
  YAxis = 17,
  XAxis = 18,
  YAxisGridNumber = 19,
  RYAxisGridNumber = 20,
  XAxisGridNumber = 21,
  TXAxisGridNumber = 22,
  TableAnnotation0 = 23,
  TableAnnotation1 = 24,
  TableAnnotation2 = 25,
  TableAnnotation3 = 26,
  TableAnnotation4 = 27,
  TableAnnotation5 = 28,
  TableAnnotation6 = 29,
  TableAnnotation7 = 30,
  TableAnnotation8 = 31,
  TableAnnotation9 = 32,
  TableAnnotation10 = 33,
  TableAnnotation11 = 34,
  TableAnnotation12 = 35,
  TableAnnotation13 = 36,
  TableAnnotation14 = 37,
  TableAnnotation15 = 38,
  TableAnnotation16 = 39,
  TableAnnotation17 = 40,
  TableAnnotation18 = 41,
  TableAnnotation19 = 42,
  TableAnnotation20 = 43,
  TableAnnotation21 = 44,
  TableAnnotation22 = 45,
  TableAnnotation23 = 46,
  TableAnnotation24 = 47,
  TableAnnotation25 = 48,
  TableAnnotation26 = 49,
  TableAnnotation27 = 50,
  TableAnnotation28 = 51,
  TableAnnotation29 = 52,
  TableAnnotation30 = 53,
  TableAnnotation31 = 54,
  TableAnnotation32 = 55,
  TableAnnotation33 = 56,
  TableAnnotation34 = 57,
  TableAnnotation35 = 58,
  TableAnnotation36 = 59,
  TableAnnotation37 = 60,
  TableAnnotation38 = 61,
  TableAnnotation39 = 62,
  TableAnnotation40 = 63,
  TableAnnotation41 = 64,
  TableAnnotation42 = 65,
  TableAnnotation43 = 66,
  TableAnnotation44 = 67,
  TableAnnotation45 = 68,
  TableAnnotation46 = 69,
  TableAnnotation47 = 70,
  TableAnnotation48 = 71,
  TableAnnotation49 = 72,
  TableAnnotation50 = 73,
  TableAnnotation51 = 74,
  TableAnnotation52 = 75,
  TableAnnotation53 = 76,
  TableAnnotation54 = 77,
  TableAnnotation55 = 78,
  TableAnnotation56 = 79,
  TableAnnotation57 = 80,
  TableAnnotation58 = 81,
  TableAnnotation59 = 82,
  ZAxisGridNumber = 83,
  ZAxisLineAnnotation = 84,
  ZoomAxisGridNumber = 100,
}
export declare enum LegendAnnotationType {
  NoSymbol = 0,
  Plus = 1,
  Cross = 2,
  Dot = 3,
  DotSolid = 4,
  Square = 5,
  SquareSolid = 6,
  Diamond = 7,
  DiamondSolid = 8,
  UpTriangle = 9,
  UpTriangleSolid = 10,
  DownTriangle = 11,
  DownTriangleSolid = 12,
  SmallPlus = 13,
  SmallCross = 14,
  SmallDot = 15,
  SmallDotSolid = 16,
  SmallSquare = 17,
  SmallSquareSolid = 18,
  SmallDiamond = 19,
  SmallDiamondSolid = 20,
  SmallUpTriangle = 21,
  SmallUpTriangleSolid = 22,
  SmallDownTriangle = 23,
  SmallDownTriangleSolid = 24,
  LargePlus = 25,
  LargeCross = 26,
  LargeDot = 27,
  LargeDotSolid = 28,
  LargeSquare = 29,
  LargeSquareSolid = 30,
  LargeDiamond = 31,
  LargeDiamondSolid = 32,
  LargeUpTriangle = 33,
  LargeUpTriangleSolid = 34,
  LargeDownTriangle = 35,
  LargeDownTriangleSolid = 36,
  ThinSolidLine = 38,
  DashLine = 39,
  DotLine = 40,
  DashDotLine = 41,
  DashDotDotLine = 42,
  MediumSolidLine = 43,
  ThickSolidLine = 44,
  Dash = 72,
  Pixel = 73,
  MediumThinSolid = 87,
  MediumThickSolid = 88,
  ExtraThickSolid = 89,
  ExtraThinSolid = 90,
  ArrowN = 92,
  ArrowNE = 93,
  ArrowE = 94,
  ArrowSE = 95,
  ArrowS = 96,
  ArrowSW = 97,
  ArrowW = 98,
  ArrowNW = 99,
  MediumThinDash = 105,
  MediumThinDot = 106,
  MediumThinDashDot = 107,
  MediumThinDashDotDot = 108,
  MediumDash = 109,
  MediumDot = 110,
  MediumDashDot = 111,
  MediumDashDotDot = 112,
  MediumThickDash = 113,
  MediumThickDot = 114,
  MediumThickDashDot = 115,
  MediumThickDashDotDot = 116,
  ThickDash = 117,
  ThickDot = 118,
  ThickDashDot = 119,
  ThickDashDotDot = 120,
  ExtraThickDash = 121,
  ExtraThickDot = 122,
  ExtraThickDashDot = 123,
  ExtraThickDashDotDot = 124,
  HatchHorizontal = 157,
  HatchVertical = 158,
  HatchFDiagonal = 159,
  HatchBDiagonal = 160,
  HatchCross = 161,
  HatchDiagCross = 162,
}
export declare enum LegendLocation {
  Top = 0,
  Bottom = 1,
  Left = 2,
  Right = 3,
}
export declare enum LegendStyle {
  TwoLine = 0,
  OneLine = 1,
  OneLineInsideAxis = 2,
  OneLineTopOfAxis = 3,
  OneLineInsideOverlap = 4,
  OneLineLeftOfAxis = 5,
}
export declare enum LineAnnotationType {
  ThinSolid = 0,
  Dash = 1,
  Dot = 2,
  DashDot = 3,
  DashDotDot = 4,
  MediumSolid = 5,
  ThickSolid = 6,
  GridTick = 7,
  GridLine = 8,
  MediumThinSolid = 9,
  MediumThickSolid = 10,
  ExtraThickSolid = 11,
  ExtraThinSolid = 12,
  GridTickColoredText = 14,
  GridLineColoredText = 15,
  MediumThinDash = 16,
  MediumThinDot = 17,
  MediumThinDashDot = 18,
  MediumThinDashDotDot = 19,
  MediumDash = 20,
  MediumDot = 21,
  MediumDashDot = 22,
  MediumDashDotDot = 23,
  MediumThickDash = 24,
  MediumThickDot = 25,
  MediumThickDashDot = 26,
  MediumThickDashDotDot = 27,
  ThickDash = 28,
  ThickDot = 29,
  ThickDashDot = 30,
  ThickDashDotDot = 31,
  ExtraThickDash = 32,
  ExtraThickDot = 33,
  ExtraThickDashDot = 34,
  ExtraThickDashDotDot = 35,
}
export declare enum LineType {
  ThinSolid = 0,
  Dash = 1,
  Dot = 2,
  DashDot = 3,
  DashDotDot = 4,
  MediumSolid = 5,
  ThickSolid = 6,
  MediumThinSolid = 9,
  MediumThickSolid = 10,
  ExtraThickSolid = 11,
  ExtraThinSolid = 12,
  MediumThinDash = 16,
  MediumThinDot = 17,
  MediumThinDashDot = 18,
  MediumThinDashDotDot = 19,
  MediumDash = 20,
  MediumDot = 21,
  MediumDashDot = 22,
  MediumDashDotDot = 23,
  MediumThickDash = 24,
  MediumThickDot = 25,
  MediumThickDashDot = 26,
  MediumThickDashDotDot = 27,
  ThickDash = 28,
  ThickDot = 29,
  ThickDashDot = 30,
  ThickDashDotDot = 31,
  ExtraThickDash = 32,
  ExtraThickDot = 33,
  ExtraThickDashDot = 34,
  ExtraThickDashDotDot = 35,
}
export declare enum LinesOrTubes {
  AllLines = 0,
  MediumThickTubes = 1,
  MediumTubes = 2,
  MediumThinTubes = 3,
  AllTubes = 4,
}
export declare enum ManualScaleControl {
  None = 0,
  Min = 1,
  Max = 2,
  MinMax = 3,
}
export declare enum MaxMarkerSize {
  Disable = 0,
  TwoPixels = 1,
  ThreePixels = 2,
  FourPixels = 3,
}
export declare enum MenuControl {
  Hide = 0,
  Show = 1,
  Grayed = 2,
}
export declare enum MinimumPointSize {
  None = 0,
  Small = 1,
  Medium = 2,
  Large = 3,
  MediumSmall = 4,
  MediumLarge = 5,
}
export declare enum MonthLabelType {
  ThreeCharacters = 0,
  OneCharacter = 1,
  NoMonthPrompt = 2,
}
export declare enum MouseCursorStyles {
  Arrow = 1,
  IBean = 2,
  Wait = 3,
  Cross = 4,
  UpArrow = 5,
  Size = 6,
  SizeNwse = 8,
  SizeNesw = 9,
  SizeWe = 10,
  SizeNw = 11,
}
export declare enum MouseWheelFunction {
  VerticalScroll = 0,
  HorizontalScroll = 1,
  Disabled = 2,
  HorizontalZoom = 3,
  HorizontalVerticalZoom = 4,
  VerticalZoom = 5,
}
export declare enum MultiAxesSeparators {
  None = 0,
  Thin = 1,
  Medium = 2,
  Thick = 3,
  ThickPlusTick = 4,
}
export declare enum MultiAxis {
  Axis0 = 0,
  Axis1 = 1,
  Axis2 = 2,
  Axis3 = 3,
  Axis4 = 4,
  Axis5 = 5,
  Axis6 = 6,
  Axis7 = 7,
  Axis8 = 8,
  Axis9 = 9,
  Axis10 = 10,
  Axis11 = 11,
  Axis12 = 12,
  Axis13 = 13,
  Axis14 = 14,
  Axis15 = 15,
}
export declare enum MultiAxisStyle {
  GroupAllAxes = 0,
  SeparateAxes = 1,
}
export declare enum MultiSampleAntiAlias {
  MSAA_4 = 0,
  MSAA_1 = 1,
  MSAA_8 = 2,
}
export declare enum ObjectType {
  Graph = 300,
  PieChart = 302,
  ScientificGraph = 304,
  PolarGraph = 308,
  ThreeDimension = 312,
}
export declare enum PEactions {
  ReinitializeResetImage = 0,
  Reinitialize = 1,
  ResetImage = 2,
  InvalidateRect = 3,
  LaunchMaximize = 4,
  LaunchCustomizeDialog = 5,
  LaunchExportDialog = 6,
  LaunchTextExportDialog = 7,
  LaunchPrintDialog = 8,
  LaunchPopupMenu = 11,
  CopyMetaToClipboard = 12,
  CopyMetaToFile = 13,
  CopyBmpToClipboard = 14,
  CopyBmpToFile = 15,
  ReinitializeQuick = 17,
  PrintGraph = 18,
  UndoZoom = 19,
  RevertToDefaults = 20,
  CopyJpegToFile = 22,
  CopyPngToClipboard = 23,
  CopyPngToFile = 24,
}
export declare enum PSAllowZooming {
  None = 0,
  HorzAndVert = 3,
  HorzAndVertMb = 6,
}
export declare enum PSCursorMode {
  NoCursor = 0,
  SquareDataCursor = 3,
}
export declare enum PSGraphPlottingMethod {
  Line = 0,
  Point = 1,
  PointsPlusLine = 2,
  Area = 3,
}
export declare enum PSGraphPlottingMethodII {
  Line = 0,
  Point = 1,
  PointsPlusLine = 2,
}
export declare enum PSGraphPlottingMethods {
  Line = 0,
  Point = 2,
  Area = 3,
  PointsPlusLine = 17,
  SecondaryAxis = 1000,
}
export declare enum PageIDs {
  FirstDefaultTab = 0,
  Page1 = 2930,
  Page2 = 2931,
  SubsetPage = 2932,
  PointsPage = 2933,
  FontPage = 2934,
  ColorPage = 2935,
  StylePage = 2936,
  AxisPage = 2937,
}
export declare enum PieGradientStyle {
  Curved = 0,
  BeveledInset = 1,
  BeveledHighlight = 2,
  NoGradient = 3,
}
export declare enum PlotGradientStyle {
  NoGradient = 0,
  Vertical = 1,
  VerticalAscent = 2,
  HorizontalRight = 3,
  HorizontalLeft = 4,
  LinearBarVertical = 5,
  LinearBarHorizontal = 6,
  LinearBarDown = 7,
  LinearBarUp = 8,
  LinearDiagonalDown = 9,
  LinearDiagonalUp = 10,
  RectangleCross = 11,
  RadialCentered = 12,
  RadialBottomRight = 13,
  RadialTopRight = 14,
  RadialBottomLeft = 15,
  RadialTopLeft = 16,
  VerticalAscentInverse = 17,
}
export declare enum PointSearchLocation {
  ChartAreaTestingVisibility = 0,
  ChartAreaNotTestingVisibility = 1,
  ZoomWindow = 2,
}
export declare enum PointSize {
  Small = 0,
  Medium = 1,
  Large = 2,
  Micro = 3,
}
export declare enum PointType {
  Plus = 0,
  Cross = 1,
  Dot = 2,
  DotSolid = 3,
  Square = 4,
  SquareSolid = 5,
  Diamond = 6,
  DiamondSolid = 7,
  UpTriangle = 8,
  UpTriangleSolid = 9,
  DownTriangle = 10,
  DownTriangleSolid = 11,
  Dash = 72,
  Pixel = 73,
  ArrowN = 92,
  ArrowNE = 93,
  ArrowE = 94,
  ArrowSE = 95,
  ArrowS = 96,
  ArrowSW = 97,
  ArrowW = 98,
  ArrowNW = 99,
}
export declare enum PointsToGraphInit {
  First = 0,
  Last = 1,
}
export declare enum PolarScaleAlternative {
  NegativeDataOffsetBy180 = 0,
  NegativeDataAtCenter = 1,
}
export declare enum PolarZoomLimits {
  None = 0,
  AxisSquare = 5,
}
export declare enum PolyMode {
  SurfacePolygons = 1,
  ThreeDBar = 2,
  PolygonData = 3,
  Scatter = 4,
}
export declare enum PrintStyleControl {
  None = 0,
  CurrentStyle = 1,
  DefaultMonochrome = 2,
}
export declare enum PrintTechnology {
  Gdi = 0,
  GdiPlus = 1,
}
export declare enum QuickStyle {
  NoStyle = 0,
  LightInset = 1,
  LightShadow = 2,
  LightLine = 3,
  LightNoBorder = 4,
  MediumInset = 5,
  MediumShadow = 6,
  MediumLine = 7,
  MediumNoBorder = 8,
  DarkInset = 9,
  DarkShadow = 10,
  DarkLine = 11,
  DarkNoBorder = 12,
}
export declare enum RenderEngine {
  Gdi = 0,
  Hybrid = 1,
  GdiPlus = 2,
  GdiTurbo = 3,
  Direct2D = 4,
  Direct3D = 5,
}
export declare enum ResourceBitmapColorizeMode {
  None = 0,
  Mask = 1,
  Tint = 2,
}
export declare enum ResourceBitmapIgnoreAlpha {
  WICBitmapUseAlpha = 0,
  WICBitmapUsePremultipliedAlpha = 1,
  WICBitmapIgnoreAlpha = 2,
}
export declare enum ResourceBitmapStyle {
  ActualSizeCentered = 0,
  ActualSizeN = 1,
  ActualSizeNW = 2,
  ActualSizeW = 3,
  ActualSizeSW = 4,
  ActualSizeS = 5,
  ActualSizeSE = 6,
  ActualSizeE = 7,
  ActualSizeNE = 8,
  MediumCentered = 9,
  MediumN = 10,
  MediumNW = 11,
  MediumW = 12,
  MediumSW = 13,
  MediumS = 14,
  MediumSE = 15,
  MediumE = 16,
  MediumNE = 17,
  SmallCentered = 19,
  SmallN = 20,
  SmallNW = 21,
  SmallW = 22,
  SmallSW = 23,
  SmallS = 24,
  SmallSE = 25,
  SmallE = 26,
  SmallNE = 27,
  LargeCentered = 29,
  LargeN = 30,
  LargeNW = 31,
  LargeW = 32,
  LargeSW = 33,
  LargeS = 34,
  LargeSE = 35,
  LargeE = 36,
  LargeNE = 37,
  DataSized = 39,
}
export declare enum RotationDetail {
  WireFrame = 0,
  PlottingMethod = 1,
  FullDetail = 2,
}
export declare enum RotationIncrement {
  IncBy15 = 0,
  IncBy10 = 1,
  IncBy5 = 2,
  IncBy2 = 3,
  IncBy1 = 4,
  DecBy1 = 5,
  DecBy2 = 6,
  DecBy5 = 7,
  DecBy10 = 8,
  DecBy15 = 9,
}
export declare enum SBCode {
  LineUp = 0,
  LineDown = 1,
  PageUp = 2,
  PageDown = 3,
  ThumbPosition = 4,
  ThumbTrack = 5,
  Top = 6,
  Bottom = 7,
}
export declare enum SGraphPlottingMethod {
  Line = 0,
  Point = 1,
  Stick = 2,
  PointsPlusBestFitLine = 3,
  PointsPlusBestFitCurve = 4,
  PointsPlusSpline = 5,
  Spline = 6,
  Bubble = 7,
  PointsPlusLine = 8,
  Area = 9,
  Bar = 10,
  SpecificPlotMode = 11,
  Step = 12,
  Ribbon = 13,
  ContourLines = 14,
  ContourColors = 15,
  AreaStacked = 16,
  AreaStackedPercent = 17,
  BarStacked = 18,
  BarStackedPercent = 19,
  LineStacked = 20,
  LineStackedPercent = 21,
  SplineArea = 22,
  SplineRibbon = 23,
  ContourColorsShadows = 24,
  ContourDelaunay = 25,
  RectHeatmap = 26,
}
export declare enum SGraphPlottingMethodII {
  Line = 0,
  Point = 1,
  PointsPlusBestFitLine = 2,
  PointsPlueBestFitCurve = 3,
  PointsPlusSpline = 4,
  Spline = 5,
  PointsPlusLine = 6,
  Step = 7,
  Ribbon = 8,
}
export declare enum SGraphPlottingMethods {
  Line = 0,
  Bar = 1,
  Point = 2,
  Area = 3,
  PointsPlusBestFitLine = 8,
  Bubble = 12,
  PointsPlusBestFitCurve = 13,
  PointsPlusSpline = 15,
  Spline = 16,
  PointsPlusLine = 17,
  Step = 21,
  Ribbon = 22,
  ContourLines = 23,
  ContourColors = 24,
  HighLowBar = 25,
  HighLowLine = 26,
  HighLowClose = 27,
  OpenHighLowClose = 28,
  BoxPlot = 29,
  HighLowArea = 33,
  Stick = 34,
  SplineArea = 35,
  SplineRibbon = 36,
  OnRightAxis = 1000,
  OnRightTopAxis = 2000,
  OnTopAxis = 3000,
}
export declare enum SGraphTAAxisLocation {
  TopFullWidth = 0,
  TopLeft = 1,
  TopCenter = 2,
  TopRight = 3,
  BottomFullWidth = 4,
  BottomLeft = 5,
  BottomCenter = 6,
  BottomRight = 7,
  NewRow = 100,
}
export declare enum SGraphTALocation {
  TopCenter = 0,
  TopLeft = 1,
  LeftCenter = 2,
  BottomLeft = 3,
  BottomCenter = 4,
  BottomRight = 5,
  RightCenter = 6,
  TopRight = 7,
  InsideTopCenter = 8,
  InsideTopLeft = 9,
  InsideLeftCenter = 10,
  InsideBottomLeft = 11,
  InsideBottomCenter = 12,
  InsideBottomRight = 13,
  InsideRightCenter = 14,
  InsideTopRight = 15,
  InsidePixelUnits = 16,
  InsideAxis = 100,
  InsideAxis0 = 100,
  InsideAxis1 = 101,
  InsideAxis2 = 102,
  InsideAxis3 = 103,
  InsideAxis4 = 104,
  InsideAxis5 = 105,
  OutsideAxis = 200,
  OutsideAxis0 = 200,
  OutsideAxis1 = 201,
  OutsideAxis2 = 202,
  OutsideAxis3 = 203,
  OutsideAxis4 = 204,
  OutsideAxis5 = 205,
  OverlapInsideAxis = 400,
  OverlapInsideAxis0 = 400,
  OverlapInsideAxis1 = 401,
  OverlapInsideAxis2 = 402,
  OverlapInsideAxis3 = 403,
  OverlapInsideAxis4 = 404,
  OverlapInsideAxis5 = 405,
}
export declare enum ScaleControl {
  Normal = 1,
  Log = 2,
}
export declare enum SequentialData {
  Auto = 0,
  Always = 1,
  Never = 2,
}
export declare enum ShadingStyle {
  White = 0,
  Color = 1,
}
export declare enum ShowAxis {
  All = 0,
  AxisLabels = 1,
  GridNumbers = 2,
  NoNumbersOrLabels = 3,
  LabelOnly = 4,
  Empty = 5,
}
export declare enum ShowBoundingBox {
  WhileRotating = 0,
  Always = 1,
  Never = 2,
}
export declare enum ShowContour {
  None = 0,
  TopLines = 1,
  BottomLines = 2,
  TopColors = 3,
  BottomColors = 4,
}
export declare enum ShowContourLegendII {
  None = 0,
  SecondPlusFirst = 1,
  SecondOnly = 2,
}
export declare enum ShowMargins {
  ShowAnnotations = 0,
  Always = 1,
  Never = 2,
}
export declare enum ShowPieLabels {
  PercentPlusLabel = 0,
  PercentOnly = 1,
  LabelOnly = 2,
  None = 3,
}
export declare enum ShowPolarGrid {
  None = 0,
  Horizontal = 1,
  HorzAndVert = 2,
}
export declare enum ShowTickMarks {
  TicksInside = 0,
  TicksOutside = 1,
  TicksHide = 2,
}
export declare enum SimpleLegendStyle {
  TwoLine = 0,
  OneLine = 1,
}
export declare enum SliceHatching {
  Monochrome = 0,
  MonochromePlusColor = 1,
}
export declare enum SmithChart {
  Polar = 0,
  Smith = 1,
  Rose = 2,
  Admittance = 3,
}
export declare enum SpecialScaling {
  None = 0,
  Financial = 1,
}
export declare enum SpecificPlotMode {
  None = 0,
  HighLowBar = 1,
  HighLowLine = 2,
  HighLowClose = 3,
  OpenHighLowClose = 4,
  BoxPlot = 5,
  HighLowArea = 6,
}
export declare enum SpecificPlotModeGradient {
  None = 0,
  Bar = 1,
  Glass = 2,
}
export declare enum SplineBevelStyle {
  None = 0,
  ThickSmooth = 1,
  MediumSmooth = 2,
  ThinSmooth = 3,
}
export declare enum SplineType {
  Cardinal = 0,
  BSpline = 1,
}
export declare enum SurfaceColors {
  WireFrame = 32000,
  SolidSurface = 32001,
}
export declare enum SymbolFrequency {
  TwentyFourPercent = 0,
  TwentyTwoPercent = 1,
  TwentyPercent = 2,
  EighteenPercent = 3,
  SixteenPercent = 4,
  FourteenPercent = 5,
  TwelvePercent = 6,
  TenPercent = 7,
  EightPercent = 8,
  SixPercent = 9,
}
export declare enum TABorder {
  DropShadow = 0,
  SingleLine = 1,
  NoBorder = 2,
  Inset = 3,
  ThickLine = 4,
}
export declare enum TAHeaderOrientation {
  Horizontal = 0,
  Ninety = 1,
  TwoHundredSeventy = 2,
}
export declare enum TAJustification {
  Left = 0,
  Center = 1,
  Right = 2,
}
export declare enum TALocation {
  TopCenter = 0,
  TopLeft = 1,
  LeftCenter = 2,
  BottomLeft = 3,
  BottomCenter = 4,
  BottomRight = 5,
  RightCenter = 6,
  TopRight = 7,
  InsideTopCenter = 8,
  InsideTopLeft = 9,
  InsideLeftCenter = 10,
  InsideBottomLeft = 11,
  InsideBottomCenter = 12,
  InsideBottomRight = 13,
  InsideRightCenter = 14,
  InsideTopRight = 15,
  InsidePixelUnits = 16,
}
export declare enum TAMoveable {
  None = 0,
  NoHotSpots = 1,
  Full = 2,
}
export declare enum TabOrComma {
  TabDelimited = 0,
  CommaDelimited = 1,
}
export declare enum TableGridLines {
  NoGridLines = 0,
  Vertical = 1,
  Horizontal = 2,
  VerticalAndHorizontal = 3,
}
export declare enum TableOrList {
  List = 0,
  Table = 1,
}
export declare enum TableStyle {
  SubsetByPoint = 0,
  PointBySubset = 1,
}
export declare enum TableWhat {
  Graphed = 0,
  AllSubsets = 1,
}
export declare enum TextRenderingHint {
  Default = 0,
  SingleBitPerPixelGridFit = 1,
  SingleBitPerPixel = 2,
  AntiAliasGridFit = 3,
  AntiAlias = 4,
  ClearTypeGridFit = 5,
}
export declare enum TextShadows {
  NoShadows = 0,
  BoldText = 1,
  AllText = 2,
}
export declare enum ThreeDGraphPlottingMethod {
  Zero = 0,
  One = 1,
  Two = 2,
  Three = 3,
  Four = 4,
}
export declare enum ThreeDGraphPlottingMethodII {
  Point = 0,
  Line = 1,
  PointPlusLine = 2,
}
export declare enum ThreeDGraphPlottingMethods {
  Point = 0,
  Line = 1,
  PointPlusLine = 2,
  Area = 3,
}
export declare enum TickStyle {
  GridStyle = 0,
  Thick = 1,
  Dot = 2,
  Dash = 3,
  OneUnit = 4,
  Thin = 5,
}
export declare enum TimeLabelType {
  TwelveHourAmPm = 0,
  TwelveHourNoAmPm = 1,
  TwentyFourHour = 2,
}
export declare enum TitleJustification {
  Left = 0,
  Middle = 1,
  Right = 2,
}
export declare enum TrackingTrigger {
  MouseMove = 0,
  CursorMove = 1,
}
export declare enum VBoundaryTypes {
  None = 0,
  Top = 1,
  Bottom = 2,
  TopAndBottom = 3,
}
export declare enum ViewingMode {
  Center = 0,
  DataLocation = 1,
}
export declare enum ViewingStyle {
  Color = 0,
  Mono = 1,
  MonoWithSymbols = 2,
}
export declare enum XLineAnnotationTextJustification {
  BottomInsideHorizontal = 0,
  BottomOutsideHorizontal = 1,
  BottomOutsideVertical = 2,
  TopInsideHorizontal = 3,
  TopOutsideHorizontal = 4,
  TopOutsideVertical = 5,
  Centered = 6,
}
export declare enum YLineAnnotationTextJustification {
  LeftInside = 0,
  LeftOutside = 1,
  RightInside = 2,
  RightOutside = 3,
  Centered = 4,
  TopInsideHorizontal = 5,
  TopOutsideHorizontal = 6,
  TopOutsideVertical = 7,
  BottomInsideHorizontal = 8,
  BottomOutsideHorizontal = 9,
  BottomOutsideVertical = 10,
}
export declare enum YearLabelType {
  FourCharacters = 0,
  TwoCharacters = 1,
  NoYearPrompt = 2,
}
export declare enum YearMonthDayPrompt {
  Enabled = 0,
  Disabled = 1,
  InsideTop = 2,
}
export declare enum YearMonthDayStyle {
  OScontrolled = 0,
  YMD = 1,
  MDY = 2,
  Legacy = 3,
}
export declare enum ZoomInterfaceOnly {
  Normal = 0,
  Rect = 1,
  Line = 2,
}
export declare enum ZoomLimits {
  None = 0,
  Axis = 1,
  AxisHorizontal = 2,
  AxisVertical = 3,
  AxisShape = 4,
  AxisSquare = 5,
}
export declare enum ZoomStyle {
  FramedRect = 0,
  Ro2Not = 1,
}
export declare enum ZoomWindowBorder {
  BorderType = 0,
  ThinLine = 1,
  NoBorder = 2,
}
export declare enum ZoomWindowPlottingMethods {
  Line = 0,
  Area = 1,
  Original = 2,
}

export declare const Enums: {
  readonly AllowDataLabels: typeof AllowDataLabels;
  readonly AllowUserInterface: typeof AllowUserInterface;
  readonly AllowZooming: typeof AllowZooming;
  readonly AnnotationHotSpot: typeof AnnotationHotSpot;
  readonly AnnotationInFront: typeof AnnotationInFront;
  readonly AnnotationZoomWindow: typeof AnnotationZoomWindow;
  readonly AutoExplode: typeof AutoExplode;
  readonly AutoStatSubsets: typeof AutoStatSubsets;
  readonly AutoXData: typeof AutoXData;
  readonly AxisBorderType: typeof AxisBorderType;
  readonly AxisNumericFormat: typeof AxisNumericFormat;
  readonly BestFitDegree: typeof BestFitDegree;
  readonly BevelLighting: typeof BevelLighting;
  readonly BevelStyle: typeof BevelStyle;
  readonly BitmapStyle: typeof BitmapStyle;
  readonly BubbleGradientStyle: typeof BubbleGradientStyle;
  readonly BubbleSize: typeof BubbleSize;
  readonly Composite2D3D: typeof Composite2D3D;
  readonly ContourColorSet: typeof ContourColorSet;
  readonly ContourLegendPrecision: typeof ContourLegendPrecision;
  readonly CursorMode: typeof CursorMode;
  readonly CursorPromptLocation: typeof CursorPromptLocation;
  readonly CursorPromptStyle: typeof CursorPromptStyle;
  readonly CurveGranularity: typeof CurveGranularity;
  readonly CustomMenu: typeof CustomMenu;
  readonly CustomMenuLocation: typeof CustomMenuLocation;
  readonly CustomMenuState: typeof CustomMenuState;
  readonly DataAndOrLabel: typeof DataAndOrLabel;
  readonly DataLabelType: typeof DataLabelType;
  readonly DataPrecision: typeof DataPrecision;
  readonly DataShadows: typeof DataShadows;
  readonly DateTimeMode: typeof DateTimeMode;
  readonly DayLabelType: typeof DayLabelType;
  readonly DefOrientation: typeof DefOrientation;
  readonly DrawCursorAction: typeof DrawCursorAction;
  readonly DuplicateData: typeof DuplicateData;
  readonly EmfDC: typeof EmfDC;
  readonly EmfType: typeof EmfType;
  readonly ExportDestDef: typeof ExportDestDef;
  readonly ExportPrecision: typeof ExportPrecision;
  readonly ExportSizeDef: typeof ExportSizeDef;
  readonly ExportTypeDef: typeof ExportTypeDef;
  readonly Filter2D: typeof Filter2D;
  readonly FontSize: typeof FontSize;
  readonly ForceGridLines: typeof ForceGridLines;
  readonly ForceVerticalPoints: typeof ForceVerticalPoints;
  readonly GradientBoundary: typeof GradientBoundary;
  readonly GradientStyle: typeof GradientStyle;
  readonly GraphAnnotMoveable: typeof GraphAnnotMoveable;
  readonly GraphAnnotPolyDataPlotMethod: typeof GraphAnnotPolyDataPlotMethod;
  readonly GraphAnnotationTextJustification: typeof GraphAnnotationTextJustification;
  readonly GraphAnnotationType: typeof GraphAnnotationType;
  readonly GraphPlottingMethod: typeof GraphPlottingMethod;
  readonly GraphPlottingMethodII: typeof GraphPlottingMethodII;
  readonly GraphPlottingMethods: typeof GraphPlottingMethods;
  readonly GraphPlusTable: typeof GraphPlusTable;
  readonly GraphTAAxisLocation: typeof GraphTAAxisLocation;
  readonly GraphTALocation: typeof GraphTALocation;
  readonly GridLineControl: typeof GridLineControl;
  readonly GridNumberType: typeof GridNumberType;
  readonly GridStyle: typeof GridStyle;
  readonly GroupingPercent: typeof GroupingPercent;
  readonly HScrollStyle: typeof HScrollStyle;
  readonly HatchType: typeof HatchType;
  readonly HideIntersectingText: typeof HideIntersectingText;
  readonly HotSpotSize: typeof HotSpotSize;
  readonly HotSpotType: typeof HotSpotType;
  readonly LegendAnnotationType: typeof LegendAnnotationType;
  readonly LegendLocation: typeof LegendLocation;
  readonly LegendStyle: typeof LegendStyle;
  readonly LineAnnotationType: typeof LineAnnotationType;
  readonly LineType: typeof LineType;
  readonly LinesOrTubes: typeof LinesOrTubes;
  readonly ManualScaleControl: typeof ManualScaleControl;
  readonly MaxMarkerSize: typeof MaxMarkerSize;
  readonly MenuControl: typeof MenuControl;
  readonly MinimumPointSize: typeof MinimumPointSize;
  readonly MonthLabelType: typeof MonthLabelType;
  readonly MouseCursorStyles: typeof MouseCursorStyles;
  readonly MouseWheelFunction: typeof MouseWheelFunction;
  readonly MultiAxesSeparators: typeof MultiAxesSeparators;
  readonly MultiAxis: typeof MultiAxis;
  readonly MultiAxisStyle: typeof MultiAxisStyle;
  readonly MultiSampleAntiAlias: typeof MultiSampleAntiAlias;
  readonly ObjectType: typeof ObjectType;
  readonly PEactions: typeof PEactions;
  readonly PSAllowZooming: typeof PSAllowZooming;
  readonly PSCursorMode: typeof PSCursorMode;
  readonly PSGraphPlottingMethod: typeof PSGraphPlottingMethod;
  readonly PSGraphPlottingMethodII: typeof PSGraphPlottingMethodII;
  readonly PSGraphPlottingMethods: typeof PSGraphPlottingMethods;
  readonly PageIDs: typeof PageIDs;
  readonly PieGradientStyle: typeof PieGradientStyle;
  readonly PlotGradientStyle: typeof PlotGradientStyle;
  readonly PointSearchLocation: typeof PointSearchLocation;
  readonly PointSize: typeof PointSize;
  readonly PointType: typeof PointType;
  readonly PointsToGraphInit: typeof PointsToGraphInit;
  readonly PolarScaleAlternative: typeof PolarScaleAlternative;
  readonly PolarZoomLimits: typeof PolarZoomLimits;
  readonly PolyMode: typeof PolyMode;
  readonly PrintStyleControl: typeof PrintStyleControl;
  readonly PrintTechnology: typeof PrintTechnology;
  readonly QuickStyle: typeof QuickStyle;
  readonly RenderEngine: typeof RenderEngine;
  readonly ResourceBitmapColorizeMode: typeof ResourceBitmapColorizeMode;
  readonly ResourceBitmapIgnoreAlpha: typeof ResourceBitmapIgnoreAlpha;
  readonly ResourceBitmapStyle: typeof ResourceBitmapStyle;
  readonly RotationDetail: typeof RotationDetail;
  readonly RotationIncrement: typeof RotationIncrement;
  readonly SBCode: typeof SBCode;
  readonly SGraphPlottingMethod: typeof SGraphPlottingMethod;
  readonly SGraphPlottingMethodII: typeof SGraphPlottingMethodII;
  readonly SGraphPlottingMethods: typeof SGraphPlottingMethods;
  readonly SGraphTAAxisLocation: typeof SGraphTAAxisLocation;
  readonly SGraphTALocation: typeof SGraphTALocation;
  readonly ScaleControl: typeof ScaleControl;
  readonly SequentialData: typeof SequentialData;
  readonly ShadingStyle: typeof ShadingStyle;
  readonly ShowAxis: typeof ShowAxis;
  readonly ShowBoundingBox: typeof ShowBoundingBox;
  readonly ShowContour: typeof ShowContour;
  readonly ShowContourLegendII: typeof ShowContourLegendII;
  readonly ShowMargins: typeof ShowMargins;
  readonly ShowPieLabels: typeof ShowPieLabels;
  readonly ShowPolarGrid: typeof ShowPolarGrid;
  readonly ShowTickMarks: typeof ShowTickMarks;
  readonly SimpleLegendStyle: typeof SimpleLegendStyle;
  readonly SliceHatching: typeof SliceHatching;
  readonly SmithChart: typeof SmithChart;
  readonly SpecialScaling: typeof SpecialScaling;
  readonly SpecificPlotMode: typeof SpecificPlotMode;
  readonly SpecificPlotModeGradient: typeof SpecificPlotModeGradient;
  readonly SplineBevelStyle: typeof SplineBevelStyle;
  readonly SplineType: typeof SplineType;
  readonly SurfaceColors: typeof SurfaceColors;
  readonly SymbolFrequency: typeof SymbolFrequency;
  readonly TABorder: typeof TABorder;
  readonly TAHeaderOrientation: typeof TAHeaderOrientation;
  readonly TAJustification: typeof TAJustification;
  readonly TALocation: typeof TALocation;
  readonly TAMoveable: typeof TAMoveable;
  readonly TabOrComma: typeof TabOrComma;
  readonly TableGridLines: typeof TableGridLines;
  readonly TableOrList: typeof TableOrList;
  readonly TableStyle: typeof TableStyle;
  readonly TableWhat: typeof TableWhat;
  readonly TextRenderingHint: typeof TextRenderingHint;
  readonly TextShadows: typeof TextShadows;
  readonly ThreeDGraphPlottingMethod: typeof ThreeDGraphPlottingMethod;
  readonly ThreeDGraphPlottingMethodII: typeof ThreeDGraphPlottingMethodII;
  readonly ThreeDGraphPlottingMethods: typeof ThreeDGraphPlottingMethods;
  readonly TickStyle: typeof TickStyle;
  readonly TimeLabelType: typeof TimeLabelType;
  readonly TitleJustification: typeof TitleJustification;
  readonly TrackingTrigger: typeof TrackingTrigger;
  readonly VBoundaryTypes: typeof VBoundaryTypes;
  readonly ViewingMode: typeof ViewingMode;
  readonly ViewingStyle: typeof ViewingStyle;
  readonly XLineAnnotationTextJustification: typeof XLineAnnotationTextJustification;
  readonly YLineAnnotationTextJustification: typeof YLineAnnotationTextJustification;
  readonly YearLabelType: typeof YearLabelType;
  readonly YearMonthDayPrompt: typeof YearMonthDayPrompt;
  readonly YearMonthDayStyle: typeof YearMonthDayStyle;
  readonly ZoomInterfaceOnly: typeof ZoomInterfaceOnly;
  readonly ZoomLimits: typeof ZoomLimits;
  readonly ZoomStyle: typeof ZoomStyle;
  readonly ZoomWindowBorder: typeof ZoomWindowBorder;
  readonly ZoomWindowPlottingMethods: typeof ZoomWindowPlottingMethods;
};

export interface PeChart {
  /** The engine handle this chart is bound to. */
  readonly handle: number;
  /** Rebuild and return the new image. */
  reinitializeResetImage(): Uint8Array;
  /** Rebuild the chart. Property writes do not take effect until
   *  this runs. Does not draw. */
  reinitialize(): void;
  /** Rebuild only if a property has been written since the last
   *  rebuild. Does nothing otherwise. */
  flush(): void;
  /** Rebuild if needed and return the image to draw. */
  render(): Uint8Array;
  /** PeFunction.Reset(). Start a new 3D initialisation with this. */
  reset(): number;
  /** PeFunction.SetViewingAt(x, y, z) -- aims the 3D camera. */
  setViewingAt(x: number, y: number, z: number): number;
  /** PeFunction.GetViewingAt() -- where the 3D camera is aimed. */
  getViewingAt(): {
    /** X in graph coordinates. */
    x: number;
    /** Y in graph coordinates. */
    y: number;
    /** Z in graph coordinates. */
    z: number;
  };
  /** PeFunction.SetLight(index, x, y, z). Only light 0 exists. */
  setLight(index: number, x: number, y: number, z: number): number;
  /** field 0/1/2 as written, 10/11/12 after the engine's /45. */
  lightState(index: number, field: number): number;
  /** PeFunction.GetHotSpot(x, y) + GetHotSpotData() in one call. */
  getHotSpot(x: number, y: number): {
    /** Which kind of hot spot was hit. 0 when the pixel hit nothing. */
    type: number;
    /** First value for this hot-spot kind. On a data hot spot it is the subset index; other kinds use it differently. */
    data1: number;
    /** Second value for this hot-spot kind. On a data hot spot it is the point index. */
    data2: number;
  };
  PeLegend: {
    /** Only used in an implementation with multiple y axes. Gets or Sets legend annotation axis (values 0 through 5 cooresponding to axis index). */
    AnnotationAxis: PeArray1D<number>;
    ContourLegendII: {
      /** Gets or Sets if the secondary contour legend is visible. */
      ShowContourLegend: ShowContourLegendII;
      /** Gets and Sets control of the range of the graph s contour legend. Use ManualContourMin and ManualContourMax to define range. For this secondary contour legend, one must use MinMax. */
      ManualContourScaleControl: ManualScaleControl;
      /** Gets and Sets the minimum contour range. Setting this value also depends on setting ManualContourScaleControl. */
      ManualContourMin: number;
      /** Gets and Sets the maximum contour range. Setting this value also depends on setting ManualContourScaleControl. */
      ManualContourMax: number;
      /** Defines the frequency of incremental contour range labels. If labels are too dense, they will be reduced by a multiple Set to some factor of 1,2,5 that will produce more divisions than colors defined. If zero (default), then contour label density is automatically determined. */
      ManualContourLine: number;
      /** Default is empty, all colors equally proportioned. This property controls the proportion of the respective color section. Given the number of contour colors, adding up all proportions defined by this property array should equate to 1. For example, 4 colors defined [ 0.25, 0.50, 0.125, 0.125 ] the... */
      ContourColorProportions: PeArray1D<number>;
      /** Sets the property array elements to redefine the 20 built-in default colors used with ContourColorSet. */
      ContourColors: PeArray1D<number>;
      /** Set ContourColors with pre defined contour colors. */
      ContourColorSet: ContourColorSet;
      /** This property creates more gradual color changes between the major colors defined by ContourColorSet or ContourColors. Setting to a value of 3 will produce a blending of 3 colors between those defined. There is max number of 100 contour colors so do not set this property to a value as to exceed 1... */
      ContourColorBlends: number;
      /** Controls an optional maximum precision (number of decimal places) used within secondary contour legend. */
      ContourLegendPrecision: ContourLegendPrecision;
      /** Optional property to assign colors to the secondary contour legend. An easier method of assigning colors is through use of ContourColorSet property. The developer may optionally also set ContourColorBlends to automatically create blends between the colors defined. */
      SubsetColors: PeArray1D<number>;
      /** Optional property to assign colors to the secondary contour legend when ViewingStyle is set to MonoChrome. */
      SubsetShades: PeArray1D<number>;
      /** This property stores the numeric labels used with secondary contour legend if ContourLabels is not defined. */
      SubsetLabels: PeArray1D<string>;
      /** This property allows for text to be associated with a color. You can assign a label per color but if using many colors, possibly define fewer labels at strategic colors to prevent overlapping. */
      ContourLabels: PeArray1D<string>;
      /** Gets or Sets text used as chart's second contour legend title. */
      ContourLegendTitle: string;
    };
    /** Gets or Sets zero based subset indices which will be included in legend. Also used to alter the order of subsets shown in the legend. Setting SubsetsToLegend(0)=-1 removes legend. Setting SubsetsToLegend(-1)=0 empties this array. */
    SubsetsToLegend: PeArray1D<number>;
    /** Gets or Sets line styles associated with zero based subset indice. */
    SubsetLineTypes: PeArray1D<number>;
    /** Gets or Sets point styles assocated with zero based subset indice. */
    SubsetPointTypes: PeArray1D<number>;
    /** Gets or Sets legend annotation type. Set to a value defined in ProEssentials.Enums.LegendAnnotationType. Annotations.Show must be true to see legend annotations. */
    AnnotationType: PeArray1D<number>;
    /** Gets or Sets legend annotation text. Annotations.Show must be true to see legend annotations. */
    AnnotationText: PeArray1D<string>;
    /** Gets or Sets legend annotation color. Annotations.Show must be true to see legend annotations. */
    AnnotationColor: PeArray1D<number>;
    /** Gets and Sets whether the legend is visible. */
    Show: boolean;
    /** Gets or Sets color associated with zero based subset indice. */
    SubsetColors: PeArray1D<number>;
    /** Gets or Sets the location of the legend. See also Legend.Style. */
    Location: LegendLocation;
    /** Gets or Sets the legend style. Controls whether legend symbol and text are on a single line, or legend symbol and text are above and below. If using the Pego or Pesgo objects, legends may additionally be placed inside the axis, or when using Grid.Option.MultiAxisStle = SeparateAxes, in between axes. */
    Style: LegendStyle;
    /** Gets and Sets point symbol style within legend. Set to false to draw point symbols with a bounding box filled with graph background color, set to true to draw symbols in actual form as plotted in chart. */
    SimplePoint: boolean;
    /** Gets and Sets line symbol style within legend. Set to false to draw line symbols in a magnified fashion and set to true to draw symbols in actual size. */
    SimpleLine: boolean;
    /** Gets and Sets whether legends force one subset legend per line when PeLegend.Style is set to OneLine. V6 now supports longer subset labels so this property visually helps when one subset has an extra long label and other subsets have shorter labels. */
    OneLinePerLegend: boolean;
    /** Default 0 and disabled, set to 200 to allow the legend to use the control width if the normal legend area is below 200. The legend is normally fit between the space above the charting area. When the control size is small, this can cramp the legend area. Setting this property to 200 or as desired,... */
    AllowLargerLegendWidth: number;
  };
  PeAnnotation: {
    Graph: {
      /** Gets or Sets whether entire rect graph annotation is hotspot (true) or just corners (false). */
      GraphAnnotRectHotSpots: boolean;
      /** Gets or Sets a potential minimum size for annotation symbols. Sizes are 2,4,6,8,10 pixels. */
      MinSymbolSize: MinimumPointSize;
      /** Gets or Sets a potential maximum size for annotation symbols. Sizes are 2,4,6,8,10 pixels. */
      MaxSymbolSize: MinimumPointSize;
      /** Gets or Sets the size of graph annotation text. Values range from 25 to 300. 100 represents same size as grid numbers, 25 is quarter size, and 300 is 3 times the size. */
      TextSize: number;
      /** Gets and Sets whether graph annotations are shown at all times or controlled via the Annotations.Show property which controls all annotations. */
      Show: boolean;
      /** Gets and Sets whether an individual graph annotation is a hot spot. See also: UI.HotSpot.GraphAnnotation. */
      HotSpot: PeArray1D<number>;
      /** Set a particular subset index to True to designate that subset as an obstacle in conjunction with graph annotation text dodging logic. For example, for a line chart, graph annotation text will dodge the line if the line s subset index is set to True. */
      SubsetObstacles: PeArray1D<boolean>;
      /** Gets and Sets graph annotation types. Set with ProEssentials.Enums.GraphAnnotationType. */
      Type: PeArray1D<number>;
      /** Gets and Sets whether graph annotation shadows are shown. */
      ShowShadows: boolean;
      /** Gets and Sets whether an individual graph annotation has a drop shadow. */
      Shadow: PeArray1D<boolean>;
      /** Gets and Sets graph annotation position. Set with ProEssentials.Enums.AnnotationInFront. */
      InFront: PeArray1D<number>;
      /** Gets and Sets graph annotation x axis value. */
      X: PeArray1D<number>;
      /** Gets and Sets graph annotation y axis value. */
      Y: PeArray1D<number>;
      /** Gets and Sets potential zoom window location. Set using ProEssentials.Enums.AnnotationZoomWindow. */
      ZoomWindow: PeArray1D<number>;
      /** Gets and Sets graph annotation text value. */
      Text: PeArray1D<string>;
      /** Gets and Sets graph annotation color. */
      Color: PeArray1D<number>;
      /** Gets and Sets graph annotation axis (0 through 16) where annotation is located. */
      Axis: PeArray1D<number>;
      /** Gets and Sets graph annotation gradient start color. Default is White and usually not needing adjusting. Useful to produce darker charts. */
      GradientColor: PeArray1D<number>;
      /** Gets and Sets graph annotation gradient styles. Set with items from Enums.PlotGradientStyle */
      GradientStyle: PeArray1D<number>;
      /** Gets and Sets graph annotation bevel styles. Set with items from Enums.BevelStyle */
      BevelStyle: PeArray1D<number>;
      /** Gets and Sets graph annotation gradient boundary. Set with items from Enums.GradientBoundary */
      GradientBoundary: PeArray1D<number>;
      /** Gets and Sets graph annotation bevel lighting. Set with items from Enums.BevelLighting */
      BevelLighting: PeArray1D<number>;
      /** This property array tests the included angles (in the respective order) while attempting to find a location void of annotation text. Logic searches all angles and then increases test radius by 15% and rechecks all angles. See help for more info. */
      TextLocation: PeArray1D<number>;
      /** This property controls how triangle annotations are located. If True, triangles are located so their vertex is at the Annotation.Graph.Y value. This allows up-triangle and down-triangle annotations to work as pointers and point to the location defined by the Annotation.Graph.Y value. If False, sy... */
      TriangleAnnotationAdj: boolean;
      /** Gets or Sets how intersecting graph annotation text is optionally handled. This property is used in conjuction with PeAnnotation.Graph.Type = Pointer(37). This features defaults to NoHiding and text will potentially move to prevent overlapping. Generally set to true when you have many annotations... */
      HideIntersectingText: HideIntersectingText;
      /** This property allows developer to tweak the size of all graph annotation symbols. For example, set to .8 to make annotations smaller, set to 1.5 to make annotations larger. */
      SizeCntl: number;
      /** Controls quantity of iterations through angles defined by Annotation.Graph.TextLocation while searching for an area void of graph annotation text. The logic searches all angles and then increases the test radius by 15% and rechecks all angles. Adjusting allows control over how long the pointer li... */
      TextDodge: number;
      /** Gets and Sets whether Pointer(37) annotation types will be moveable if they are hot spots and user clicks and drags pointer text. */
      Moveable: boolean;
      /** Gets or Sets text background color for all graph annotation text. Set to a value of Color.Empty for a transparent background. */
      BackColor: number;
      /** Gets and Sets whether an individual graph annotation's text is bold. Once any index is set for Bold, Italic, Underline, Font, or FontSize, then alternate (slightly slower) logic is used to draw graph annotations. */
      Bold: PeArray1D<boolean>;
      /** Gets and Sets whether an individual graph annotation's text is italic. Once any index is set for Bold, Italic, Underline, Font, or FontSize, then alternate (slightly slower) logic is used to draw graph annotations. */
      Italic: PeArray1D<boolean>;
      /** Gets and Sets whether an individual graph annotation's text is underline. Once any index is set for Bold, Italic, Underline, Font, or FontSize, then alternate (slightly slower) logic is used to draw graph annotations. */
      Underline: PeArray1D<boolean>;
      /** Gets and Sets graph annotation text font. Once any index is set for Font, FontSize, Bold, Italic, or Underline, then alternate (slightly slower) logic is used to draw graph annotations. */
      Font: PeArray1D<string>;
      /** Gets and Sets relative graph annotation text size for an annotation. Common ranges are from .5 to 2.0. Once any index is set for Font, FontSize, Bold, Italic, or Underline, then alternate (slightly slower) logic is used to draw graph annotations. */
      FontSize: PeArray1D<number>;
    };
    Line: {
      /** Gets or Sets the background color of line annotation text that uses 'S' (right side opaque justification code) or 'M' (left side opaque). */
      LineAnnotBackColor: number;
      /** Gets or Sets the size of line annotation text. Values range from 25 to 300. 100 represents same size as grid numbers, 25 is quarter size, and 300 is 3 times the size. */
      TextSize: number;
      /** Controls when margin properties (LeftMargin, TopMargin, RightMargin, BottomMargin) are used, only when ShowAnnotations = True, Always, or Never. */
      ShowMargins: ShowMargins;
      /** Gets or Sets a string whose length is used to allocate space left of y axis. Mostly used with horizontal line annotation text using |L justification code. Set this property to the longest annotation string placed on the outside left edge of graph. */
      LeftMargin: string;
      /** Gets or Sets a string whose length is used to allocate space right of y axis. Mostly used with horizontal line annotation text using |R justification code. Set this property to the longest annotation string placed on the outside right edge of graph. */
      RightMargin: string;
      /** Gets or Sets a string whose length is used to allocate space above chart's grid. Mostly used with vertical line annotation text using |T justification code. Set this property to the longest annotation string placed on the outside top edge of graph. */
      TopMargin: string;
      /** Gets or Sets a string whose length is used to allocate space below x axis. Mostly used with vertical line annotation text using |B justification code. Set this property to the longest annotation string placed on the outside bottom edge of graph. */
      BottomMargin: string;
      /** Gets and Sets whether y axis line annotations are shown at all times or controlled via the PeAnnotations.Show property which controls all annotations. */
      YAxisShow: boolean;
      /** Gets and Sets whether x axis line annotations are shown at all times or controlled via the PeAnnotations.Show property which controls all annotations. */
      XAxisShow: boolean;
      /** Gets and Sets y axis line annotation location. */
      YAxis: PeArray1D<number>;
      /** Gets and Sets y axis line annotation zoom window location. Set with ProEssentials.Enums.AnnotationZoomWindow. */
      YAxisZoomWindow: PeArray1D<number>;
      /** Gets and Sets y axis line annotation types. Set with ProEssentials.Enums.LineAnnotationType. */
      YAxisType: PeArray1D<number>;
      /** Gets and Sets y axis line annotation location. Set with ProEssentials.Enums.AnnotationInFront. */
      YAxisInFront: PeArray1D<number>;
      /** Gets and Sets y axis line annotation axis location (0 through 5). Only needed if implementing multiple y axes. */
      YAxisAxis: PeArray1D<number>;
      /** Gets and Sets y axis line annotation text. */
      YAxisText: PeArray1D<string>;
      /** Gets and Sets y axis line annotation color. */
      YAxisColor: PeArray1D<number>;
      /** Gets and Sets x axis line annotation location. */
      XAxis: PeArray1D<number>;
      /** Gets and Sets x axis line annotation zoom window location. Set with ProEssentials.Enums.AnnotationZoomWindow. */
      XAxisZoomWindow: PeArray1D<number>;
      /** Gets and Sets x axis line annotation types. Set with ProEssentials.Enums.LineAnnotationType. */
      XAxisType: PeArray1D<number>;
      /** Gets and Sets x axis line annotation location. Set with ProEssentials.Enums.AnnotationInFront. */
      XAxisInFront: PeArray1D<number>;
      /** Gets and Sets x axis line annotation text. */
      XAxisText: PeArray1D<string>;
      /** Gets and Sets vertical line annotation color. */
      XAxisColor: PeArray1D<number>;
    };
    Axis: {
      /** Gets or Sets the size of axis annotation text. Values range from 25 to 300. 100 represents same size as grid numbers, 25 is quarter size, and 300 is 3 times the size. */
      TextSize: number;
      /** Gets and Sets whether x axis annotations are shown at all times or controlled via the Annotations.Show property which controls all annotations. */
      XShow: boolean;
      /** Gets and Sets whether y axis annotations are shown at all times or controlled via the Annotations.Show property which controls all annotations. */
      YShow: boolean;
      /** Gets and Sets x axis annotation location. For Graph objects, a value of 1 represents first data point location. */
      X: PeArray1D<number>;
      /** Gets or Sets the x axis annotation zoom window location. Type is Enums.AnnotationZoomWindow. */
      XZoomWindow: AnnotationZoomWindow;
      /** Gets and Sets x axis annotation text. */
      XText: PeArray1D<string>;
      /** Gets and Sets x axis annotation color. */
      XColor: PeArray1D<number>;
      /** Gets and Sets y axis annotation location. */
      Y: PeArray1D<number>;
      /** Gets and Sets y axis annotation text. */
      YText: PeArray1D<string>;
      /** Gets and Sets y axis annotation color. */
      YColor: PeArray1D<number>;
    };
    Table: {
      /** Gets or Sets the table annotation location. If location set is located within an axis, further define location with Annotation.Table.AxisLocation. */
      Location: GraphTALocation;
      /** If Annotation.Table.Location is set to a location within an axis, this property controls table location within the axis. */
      AxisLocation: GraphTAAxisLocation;
      /** Gets or Sets gradient starting color if GradientStyle non zero. Default is White and usually not needing adjusting. Useful to produce darker charts. */
      GradientColor: number;
      /** Gets or sets background gradient style. Set with items from Enums.PlotGradientStyle */
      GradientStyle: PlotGradientStyle;
      /** Gets or sets background bevel style. Set with items from Enums.BevelStyle */
      BevelStyle: BevelStyle;
      /** Gets or sets background gradient boundary. Set with items from Enums.GradientBoundary */
      GradientBoundary: GradientBoundary;
      /** Gets or sets background bevel lighting. Set with items from Enums.BevelLighting */
      BevelLighting: BevelLighting;
      /** Gets and Sets the table annotation cell types. Always establish Table.Rows and Table.Columns before setting this property. */
      Type: PeArray2D<number>;
      /** Gets and Sets whether individual table annotation cells are clickable. Always establish Table.Rows and Table.Columns before setting this property. */
      HotSpot: PeArray2D<boolean>;
      /** Gets and Sets manual width of table column. By default column widths are automatically configured. If you set this property, it defines a new minimum column width. Units are in number-of-characters. Always establish Table.Columns before setting this property. */
      ColumnWidth: PeArray1D<number>;
      /** Gets and Sets the style of justification for individual cell text. Always establish Table.Rows and Table.Columns before setting this property. */
      Justification: PeArray2D<number>;
      /** Gets and Sets the text for individual cells. Always establish Table.Rows and Table.Columns before setting this property. */
      Text: PeArray2D<string>;
      /** Gets and Sets the font name for individual cells. The font for the entire table can be set with Annotation.Table.Font. Always establish Table.Rows and Table.Columns before setting this property. */
      Fonts: PeArray2D<string>;
      /** Gets and Sets the foreground color for individual cells. The foreground color for the entire table can be set with Annotation.Table.ForeColor. Always establish Table.Rows and Table.Columns before setting this property. */
      Color: PeArray2D<number>;
      /** Gets and Sets the working table index 0 through 19. Used when you need to work with multiple table annotations. Set before adjusting any additional table annotation properties. */
      Working: number;
      /** Gets and Sets the quantity of rows for the table annotation. Set this property and Annotation.Table.Columns before passing table annotation text. */
      Rows: number;
      /** Gets and Sets the quantity of columns for the table annotation. Set this property and Annotation.Table.Rows before passing table annotation text. */
      Columns: number;
      /** Gets and Sets the quantity of header rows for the table annotation. Header rows stand apart from the rest of the grid by their placement outside/above the table annotation bounding box. */
      HeaderRows: number;
      /** Gets and Sets whether the table annotation will have a header column. The header column stands apart from the rest of the grid by their placement outside/left the table annotation bounding box. */
      HeaderColumn: boolean;
      /** Gets or Gets the orientation of the header rows if Annotation.Table.HeaderRows is non-zero. */
      HeaderOrientation: TAHeaderOrientation;
      /** Gets or Sets the border style of the table annotation's bounding box. */
      Border: TABorder;
      /** Gets or Sets color used as the background color for the table annotation's bounding box. */
      BackColor: number;
      /** Gets or Sets color used as the foreground color for the table annotation's text. */
      ForeColor: number;
      /** Gets and Sets the text size for the table annotation. Default value 100 produces text that is the same size as other label text. Valid values range from 20 to 200. */
      TextSize: number;
      /** Gets and Sets whether the table annotation is visible. */
      Show: boolean;
      /** Gets or Sets font name used for the table annotation. Individual cell font control can be implemented with Annotation.Table.Fonts(). */
      Font: string;
      /** Gets and Sets whether all table annotations are visible. */
      ShowAll: boolean;
      /** Gets or Sets whether font sizes are fixed for the table annotation. Set to TRUE to disable any potential text-to-fit logic. */
      FontSizeFixed: boolean;
      /** Gets and Sets whether individual table annotation cells are bold. Always establish Table.Rows and Table.Columns before setting this property. */
      Bold: PeArray2D<boolean>;
      /** Gets and Sets whether individual table annotation cells are italic. Always establish Table.Rows and Table.Columns before setting this property. */
      Italic: PeArray2D<boolean>;
      /** Gets and Sets whether individual table annotation cells are underlined. Always establish Table.Rows and Table.Columns before setting this property. */
      Underline: PeArray2D<boolean>;
      /** Gets and Sets the pixel location of table annotation when PeAnnotation.Table.Location = InsidePixelUnits. */
      X: number;
      /** Gets and Sets the pixel location of table annotation when PeAnnotation.Table.Location = InsidePixelUnits. */
      Y: number;
      /** Gets and Sets the width in pixels of table annotation when PeAnnotation.Table.Location = InsidePixelUnits. */
      Width: number;
      /** Gets and Sets whether the table annotation will be used in TextMode to render a paragraph of text. Rows and Columns must both be set to 1 when using this feature. */
      TextMode: boolean;
      /** Gets or sets whether the table is moveable by the user when PeAnnotation.Table.Location is set to InsidePixelUnits. */
      Moveable: TAMoveable;
    };
    /** This property controls if annotations are drawn in-front (true) or behind (false) any grid lines. */
    InFrontOfGrid: boolean;
    /** Gets and Sets whether all table annotations are visible. */
    ShowAllTableAnnotations: boolean;
    /** Do not set unless working with special quick annotations inside a mouse move event. See Demo example 110 and 105. Setting this property true will call a function that prepares the chart to show temporary quick annotations (annotations with negative annotation types. Setting this property will als... */
    ShowingQuickAnnotations: boolean;
    /** Do not set unless working with special quick annotations and inside a mouse up event. See Demo example 110 and 105. Setting this property true will tell the chart to draw a partial redraw, reverting to view without quick annotations shown. Setting this property will also set PeSpecial.NeedToIniti... */
    HidingQuickAnnotations: boolean;
    /** Gets and Sets whether Graph, Line, and Axis annotations are visible. */
    Show: boolean;
    /** Gets and Sets whether annotations are plotted in front of plotting methods or behind. */
    InFront: boolean;
    /** Gets and Sets whether annotation text is shown. */
    ShowAnnotationText: boolean;
    /** Declared by the WinUI control's own interface. */
    EnableWinUIDrawTable: boolean;
  };
  PeFunction: {
    /** Used with PEsearchsubsetpointindex, this property stores latest function result. */
    ClosestSubsetIndex: number;
    /** Used with PEsearchsubsetpointindex and PEsearchpointindex, this property stores latest function result. */
    ClosestPointIndex: number;
    /** Used with PEsearchsubsetpointindex, this property stores latest function result, the axis index as relates to all potential axes as established when initializing chart. */
    ClosestAxisIndex: number;
    /** Used with PEsearchsubsetpointindex, this property stores latest function result. If Chart is zoomed, or SubsetsToGraph and or RandomSubsetsToGraph limit the quantity of visible axes, then the Visible axis index is the zero based axis index from top(0) to bottom(n). ClosestAxisIndex is generally t... */
    ClosestAxisIndexVisible: number;
    /** Force SubsetColors info to be updated when RenderEngine = Direct3D. */
    Force3dxNewColors: boolean;
    /** Forces the internal polygon data to be rebuilt to reflect changes in X, Y Data. */
    Force3dxVerticeRebuild: boolean;
    Image: {
      /** Gets or Sets the DotsPerInch info when exporting an image by calling a function or using built-in export dialog. If zero, no DPI information is embedded. */
      ExportImageDpi: number;
      /** Gets or Sets whether the ImageExportDpi setting is shown within the ProEssentials export dialog. /// */
      HideExportImageDpi: boolean;
      /** When exporting a chart as a high resolution image (larger than 2000 pixels wide or tall,) setting this property True will use alternate fixed font sizing logic producing larger fonts. /// */
      ExportImageLargeFont: boolean;
      /** Gets or Sets whether exported enhanced metafiles from the built-in export dialog will include background bitmaps and gradients. /// */
      EmfBitmapGradients: boolean;
      /** Gets or Sets the type of enhanced metafile created when exported from built-in export dialog. */
      EmfType: EmfType;
      /** Gets or Sets the target Device Context used when exporting enhanced metafiles from the built-in export dialog. DisplayDpiAdjusted uses the display DC but then manipulates the EMF header to simulate a higher DPI. */
      EmfDC: EmfDC;
      /** Gets or Sets the target Dots-Per-Inch DPI width for exported metafiles. When using the FixedFonts feature, and sending a prepared metafile directly to printer via PlayMetafile, then generally set this property to the target printer's DPI. */
      DpiX: number;
      /** Gets or Sets the target Dots-Per-Inch DPI width for exported metafiles. When using the FixedFonts feature, and sending a prepared metafile directly to printer via PlayMetafile, then generally set this property to the target printer's DPI. */
      DpiY: number;
      /** Default 6600. Hopefully does not need adjusting but made available just in case. Gets or Sets the maximum print pixel size when printing via prepared bitmap. */
      MaxPrintDimension: number;
      /** Default 5400. Hopefully does not need adjusting but made available just in case. Gets or Sets the maximum print pixel size when printing Direct3D based charts. */
      MaxPrintDimension3Dx: number;
      /** By default exports are limited in shape to a 10 to 1 ratio. We recommend a limit, but in some cases you may want to avoid this limit. Set this property to 2 will essentially remove a shape limitation. */
      ExportShapeLimit: number;
    };
    /** Resolve a pixel to graph coordinates. .NET passes five ref parameters; they cannot cross this boundary, so the inputs are arguments and the outputs are the returned object. */
    ConvPixelToGraph(pixelX: number, pixelY: number, opts?: { axisIndex?: number; graphX?: number; graphY?: number; rightAxis?: boolean; topAxis?: boolean; viceVersa?: boolean }): {
      /** False when the pixel is outside the plot -- the coordinates are then meaningless rather than clamped. */
      ok: boolean;
      /** Which axis the answer is against. */
      axisIndex: number;
      /** The pixel that was resolved. */
      pixelX: number;
      /** The pixel that was resolved. */
      pixelY: number;
      /** Graph coordinate at that pixel. */
      graphX: number;
      /** Graph coordinate at that pixel. */
      graphY: number;
    };
    /** Update one table annotation on screen without rebuilding the image. */
    DrawTable(tableAnnotationIndex: number): number;
    /** Fall daylight-saving threshold. Defaults are month 10 (October), day 5 (5th/last Sunday), hour 2. */
    GetFallDayLight(): {
      /** Month, 1-12. */
      month: number;
      /** Which weekday of the month, 1-5, where 5 means the last one. */
      day: number;
      /** Year. Zeroed by the matching Set call. */
      year: number;
      /** Hour, 0-23. */
      hour: number;
      /** Minute. Zeroed by the matching Set call. */
      minute: number;
      /** Second. Zeroed by the matching Set call. */
      second: number;
      /** Millisecond. Zeroed by the matching Set call. */
      milliSecond: number;
      /** Day of the week, 0 = Sunday. */
      weekDay: number;
      /** Day of the year. Zeroed by the matching Set call. */
      yearDay: number;
    };
    /** Graph coordinates of the most recent GRAPH hot spot. *** These describe the LAST HIT TEST and carry no freshness flag *** -- the engine fills them only inside GetHotSpotData, so call GetHotSpot(x, y) first or read this from a PeGraphHotSpot handler. Reading it cold returns the previous answer. */
    GetGraphLoc(): {
      /** Which axis these coordinates are against. */
      axis: number;
      /** Graph X of the most recent hot-spot test. */
      x: number;
      /** Graph Y of the most recent hot-spot test. */
      y: number;
    };
    /** Run a hot-spot test at a pixel and return the hot-spot type. */
    GetHotSpot(x: number, y: number): number;
    /** The no-argument overload: test at the engine's own last mouse position (PEP_ptLASTMOUSEMOVE) and return the whole struct. */
    GetHotSpot(): {
      /** Which kind of hot spot was hit. 0 when the pixel hit nothing. */
      type: number;
      /** First value for this hot-spot kind. On a data hot spot it is the subset index; other kinds use it differently. */
      data1: number;
      /** Second value for this hot-spot kind. On a data hot spot it is the point index. */
      data2: number;
      /** Pixel rectangle the hot spot occupies. */
      rect: {
        /** Left edge in canvas pixels. */
        left: number;
        /** Top edge in canvas pixels. */
        top: number;
        /** Right edge in canvas pixels. */
        right: number;
        /** Bottom edge in canvas pixels. */
        bottom: number;
      };
    };
    /** Read the struct the LAST hot-spot test filled. Does NOT run a new test, so it is safe from an event handler after the pointer moved. */
    GetHotSpotData(): {
      /** Which kind of hot spot was hit. 0 when the pixel hit nothing. */
      type: number;
      /** First value for this hot-spot kind. On a data hot spot it is the subset index; other kinds use it differently. */
      data1: number;
      /** Second value for this hot-spot kind. On a data hot spot it is the point index. */
      data2: number;
      /** Pixel rectangle the hot spot occupies. */
      rect: {
        /** Left edge in canvas pixels. */
        left: number;
        /** Top edge in canvas pixels. */
        top: number;
        /** Right edge in canvas pixels. */
        right: number;
        /** Bottom edge in canvas pixels. */
        bottom: number;
      };
    };
    /** Pixel rectangle bounding an axis. With no argument, reads whatever PeGrid.WorkingAxis is set to; with one, sets the working axis, reads, and restores it. Out of range (0-15) returns an empty rect. */
    GetRectAxis(nWorkingAxis?: number): {
      /** Left edge in canvas pixels. */
      left: number;
      /** Top edge in canvas pixels. */
      top: number;
      /** Right edge in canvas pixels. */
      right: number;
      /** Bottom edge in canvas pixels. */
      bottom: number;
      /** Width in canvas pixels. */
      width: number;
      /** Height in canvas pixels. */
      height: number;
      /** False when the engine has no rectangle to report -- the edges are then stale rather than zero. */
      ok: boolean;
      /** True when the point lies inside this rectangle. */
      contains(p: { x: number; y: number }): boolean;
    };
    /** Pixel rectangle bounding the grid / charting area. */
    GetRectGraph(): {
      /** Left edge in canvas pixels. */
      left: number;
      /** Top edge in canvas pixels. */
      top: number;
      /** Right edge in canvas pixels. */
      right: number;
      /** Bottom edge in canvas pixels. */
      bottom: number;
      /** Width in canvas pixels. */
      width: number;
      /** Height in canvas pixels. */
      height: number;
      /** False when the engine has no rectangle to report -- the edges are then stale rather than zero. */
      ok: boolean;
      /** True when the point lies inside this rectangle. */
      contains(p: { x: number; y: number }): boolean;
    };
    /** Pixel rectangle bounding the zoom window. */
    GetRectZoomWindow(): {
      /** Left edge in canvas pixels. */
      left: number;
      /** Top edge in canvas pixels. */
      top: number;
      /** Right edge in canvas pixels. */
      right: number;
      /** Bottom edge in canvas pixels. */
      bottom: number;
      /** Width in canvas pixels. */
      width: number;
      /** Height in canvas pixels. */
      height: number;
      /** False when the engine has no rectangle to report -- the edges are then stale rather than zero. */
      ok: boolean;
      /** True when the point lies inside this rectangle. */
      contains(p: { x: number; y: number }): boolean;
    };
    /** Spring daylight-saving threshold. Defaults are month 4 (April), day 1 (1st Sunday), hour 2. */
    GetSpringDayLight(): {
      /** Month, 1-12. */
      month: number;
      /** Which weekday of the month, 1-5, where 5 means the last one. */
      day: number;
      /** Year. Zeroed by the matching Set call. */
      year: number;
      /** Hour, 0-23. */
      hour: number;
      /** Minute. Zeroed by the matching Set call. */
      minute: number;
      /** Second. Zeroed by the matching Set call. */
      second: number;
      /** Millisecond. Zeroed by the matching Set call. */
      milliSecond: number;
      /** Day of the week, 0 = Sunday. */
      weekDay: number;
      /** Day of the year. Zeroed by the matching Set call. */
      yearDay: number;
    };
    /** PEreinitialize only. Use when you want to read back ManualMaxY and the other computed axis properties without producing an image. */
    Reinitialize(): number;
    /** PEreinitialize then PEresetimage. Call after a block of property writes -- this is the call every desktop transcription ends with. */
    ReinitializeResetImage(): number;
    /** Reset the object to its default state. */
    Reset(): number;
    /** Rebuild the image at the CURRENT size. .NET takes (width, height) and 0,0 means 'the control's own size'; a non-zero size is refused here because the web control owns its size -- use PeControl.setSize. */
    ResetImage(width?: number, height?: number): number;
    /** Which data point is NEAREST this pixel -- as opposed to what is UNDER it, which is GetHotSpot. `x` is the SUBSET index and `y` is the POINT index, matching .NET's Point; both are -1 when nothing was found, which is what `if (p.x >= 0)` tests. */
    SearchSubsetPointIndex(x: number, y: number): {
      /** Subset index of the nearest data point, or -1 when none was found. NOT a coordinate. */
      x: number;
      /** Point index of the nearest data point, or -1 when none was found. NOT a coordinate. */
      y: number;
    };
    /** Set the fall threshold. See SetSpringDayLight for the zero fill. */
    SetFallDayLight(month: number, day: number, hour: number, weekDay?: number): number;
    /** Set the spring threshold. `day` is 1-5 for the 1st..5th weekday of the month; `weekDay` is 0=Sun and defaults to 0. **Zeroes year, minute, second, millisecond and yearDay**, exactly as the .NET method does -- it builds a fresh struct and passes the whole thing. */
    SetSpringDayLight(month: number, day: number, hour: number, weekDay?: number): number;
    /** Undo the zoom state, exactly as the Undo Zoom menu item does. Raises the same PeZoomOut notification a user's click would. */
    UndoZoom(): number;
    /** NOT AVAILABLE in the web build -- throws. PEchangeresourcesW swaps a resource DLL; pe_load_strings takes a string blob and is NOT the same operation */
    ChangeResources(...args: unknown[]): never;
    /** NOT AVAILABLE in the web build -- throws. PEdrawcursor has no wasm export (pe_cursor is the cursor SHAPE state, a different subject) */
    DrawCursor(...args: unknown[]): never;
    /** NOT AVAILABLE in the web build -- throws. Win32 object store I/O */
    LoadFromURL(...args: unknown[]): never;
    /** NOT AVAILABLE in the web build -- throws. Win32 object store I/O */
    LoadObjectFromFile(...args: unknown[]): never;
    /** NOT AVAILABLE in the web build -- throws. Win32 object store I/O */
    LoadObjectFromStream(...args: unknown[]): never;
    /** NOT AVAILABLE in the web build -- throws. Win32 object store I/O */
    LoadObjectPartialFromStream(...args: unknown[]): never;
    /** NOT AVAILABLE in the web build -- throws. calls PEreinitializecustoms, which is dead code this project keeps on purpose -- it is deliberately not ported, and aliasing it onto Reinitialize would silently make the fast path the slow one */
    PartialReinitialize(...args: unknown[]): never;
    /** NOT AVAILABLE in the web build -- throws. same as PartialReinitialize -- PEreinitializecustoms is deliberately not ported */
    PartialReinitializeResetImage(...args: unknown[]): never;
    /** NOT AVAILABLE in the web build -- throws. PEpartialresetimage has no wasm export */
    PartialResetImage(...args: unknown[]): never;
    /** NOT AVAILABLE in the web build -- throws. prints to a Win32 device context */
    PrintGraph(...args: unknown[]): never;
    /** NOT AVAILABLE in the web build -- throws. prints to a Win32 device context */
    PrintGraphEx(...args: unknown[]): never;
    /** NOT AVAILABLE in the web build -- throws. device-context geometry; no web equivalent */
    ResetImageEx(...args: unknown[]): never;
    /** NOT AVAILABLE in the web build -- throws. Win32 object store I/O */
    SaveObjectPartialToStream(...args: unknown[]): never;
    /** NOT AVAILABLE in the web build -- throws. Win32 object store I/O */
    SaveObjectToFile(...args: unknown[]): never;
    /** NOT AVAILABLE in the web build -- throws. Win32 object store I/O */
    SaveObjectToStream(...args: unknown[]): never;
    /** NOT AVAILABLE in the web build -- throws. PEsearchpointindex has no wasm export */
    SearchPointIndex(...args: unknown[]): never;
    /** NOT AVAILABLE in the web build -- throws. Win32 object store I/O */
    SerializeActiveXToFile(...args: unknown[]): never;
    /** Names on this node that throw rather than work, so a page can feature-detect without a try/catch. */
    readonly NotAvailable: readonly string[];
  };
  PeTable: {
    /** Gets or Sets color of the table's bounding box and text within box. */
    ForeColor: number;
    /** Gets or Sets color used as the background color for the chart's table. */
    BackColor: number;
    /** Gets or Sets font for Table when GraphPlusTable is non zero. */
    Font: string;
    /** Gets or Sets whether a table is included below chart. */
    Show: GraphPlusTable;
    /** If RandomSubsetsToGraph controls which subsets are visible, this property controls if all subsets are still tabled. */
    What: TableWhat;
    /** Rarely needed, normally the Graph Object s table will not include subsets which have identical data. Setting this property to TRUE will force all subsets to be tabled. */
    ComparisonSubsets: boolean;
    /** This property controls whether tabled items will be formatted via Grid.Option.AxisFormatY and Grid.Option.AxisFormatRY. */
    Format: boolean;
    /** Gets or Sets ability of user to show the table below graph. */
    Allow: boolean;
    /** Controls order and visibility of subsets within the table. Fill this array with zero based subset indices of those subsets you wish to include in the table. */
    SubsetsToTable: PeArray1D<number>;
    /** Gets or Sets whether table includes grid lines. */
    GridLines: TableGridLines;
    /** Gets or Sets whether table font is bold. */
    FontBold: boolean;
    /** Gets or Sets whether table font is italic. */
    FontItalic: boolean;
    /** Gets or Sets whether table font is underlined. */
    FontUnderline: boolean;
  };
  PeFont: {
    /** Gets or Sets font for Table when GraphPlusTable is non zero. */
    TableFont: string;
    /** Gets and Sets adjustment to table font sizes used in image. For enlarged fonts, increase from 1.0 to any value up to 1.5. For reduced font sizes, decrease from 1.0 to 0.5. */
    SizeTableCntl: number;
    /** Gets or Sets whether table font is bold. */
    TableFontBold: boolean;
    /** Gets or Sets whether table font is italic. */
    TableFontItalic: boolean;
    /** Gets or Sets whether table font is underlined. */
    TableFontUnderline: boolean;
    /** Gets or Sets the size of graph annotation text. Values range from 25 to 300. 100 represents same size as grid numbers, 25 is quarter size, and 300 is 3 times the size. */
    GraphAnnotationTextSize: number;
    /** Gets or Sets the size of axis annotation text. Values range from 25 to 300. 100 represents same size as grid numbers, 25 is quarter size, and 300 is 3 times the size. */
    AxesAnnotationTextSize: number;
    /** Gets or Sets the size of line annotation text. Values range from 25 to 300. 100 represents same size as grid numbers, 25 is quarter size, and 300 is 3 times the size. */
    LineAnnotationTextSize: number;
    /** Gets and Sets adjustment to grid number font sizes used in image. For enlarged fonts, increase from 1.0 to any value up to 1.5. For reduced font sizes, decrease from 1.0 to 0.5. */
    SizeGridNumberCntl: number;
    /** Gets and Sets adjustment to cursor prompt font sizes used in image. For enlarged fonts, increase from 1.0 to any value up to 1.5. For reduced font sizes, decrease from 1.0 to 0.5. */
    SizeCursorPromptCntl: number;
    /** Gets and Sets adjustment to axis label font sizes used in image. For enlarged fonts, increase from 1.0 to any value up to 1.5. For reduced font sizes, decrease from 1.0 to 0.5. */
    SizeAxisLabelCntl: number;
    /** Gets or Sets the target Dots-Per-Inch DPI width for exported metafiles. When using the FixedFonts feature, it is recommended to set this property to 600 if export could possibly be printed, or 96 if the export will only be viewed on a monitor. */
    DpiX: number;
    /** Gets or Sets the target Dots-Per-Inch DPI height for exported metafiles. When using the FixedFonts feature, it is recommended to set this property to 600 if export could possibly be printed, or 96 if the export will only be viewed on a monitor. */
    DpiY: number;
    /** Gets and Sets adjustment to all font sizes used in image. For enlarged fonts, increase from 1.0 to any value up to 1.5. For reduced font sizes, decrease from 1.0 to 0.5. */
    SizeGlobalCntl: number;
    /** Gets and Sets adjustment to title font sizes used in image. For enlarged fonts, increase from 1.0 to any value up to 1.5. For reduced font sizes, decrease from 1.0 to 0.5. */
    SizeTitleCntl: number;
    /** Default = 0 where FontSizeTitleCntl controls both MainTitle and SubTitle sizes. This property allows control of both the MainTitle font size and SubTitle font size. */
    SizeSubTitleCntl: number;
    /** Gets and Sets adjustment to legend graphic and font sizes used in image. For enlarged fonts, increase from 1.0 to any value up to 1.5. For reduced font sizes, decrease from 1.0 to 0.5. */
    SizeLegendCntl: number;
    /** Gets and Sets adjustment to MultiSubTitle font sizes used in image. For enlarged fonts, increase from 1.0 to any value up to 1.5. For reduced font sizes, decrease from 1.0 to 0.5. */
    SizeMultiSubTitleCntl: number;
    /** Gets and Sets adjustment to MultiBottomTitle font sizes used in image. For enlarged fonts, increase from 1.0 to any value up to 1.5. For reduced font sizes, decrease from 1.0 to 0.5. */
    SizeMultiBottomTitleCntl: number;
    /** Gets or Sets the general font size for all text within chart. */
    FontSize: FontSize;
    MainTitle: {
      /** Gets or Sets font name used for the Main Title. */
      Font: string;
      /** Gets or Sets the bold state of Main Title font. */
      Bold: boolean;
      /** Gets or Sets the italic state of Main Title font. */
      Italic: boolean;
      /** Gets or Sets the underline state of Main Title font. */
      Underline: boolean;
    };
    SubTitle: {
      /** Gets or Sets font name used for the Sub Title. */
      Font: string;
      /** Gets or Sets the bold state of Sub Title font. */
      Bold: boolean;
      /** Gets or Sets the italic state of Sub Title font. */
      Italic: boolean;
      /** Gets or Sets the underline state of Sub Title font. */
      Underline: boolean;
    };
    Label: {
      /** Gets or Sets font name used for the Legend, Grid Line Numbers, and Axis Labels. */
      Font: string;
      /** Gets or Sets the bold state of the Legend, Grid Line Numbers, and Axis Labels. */
      Bold: boolean;
      /** Gets or Sets the italic state of the Legend, Grid Line Numbers, and Axis Labels. */
      Italic: boolean;
      /** Gets or Sets the underline state of the Legend, Grid Line Numbers, and Axis Labels. */
      Underline: boolean;
    };
    /** Gets or Sets whether fonts are fixed in size independent of control size. If False, all font sizes scale and small controls will have proportionally small fonts. */
    Fixed: boolean;
  };
  PeColor: {
    /** True to show grid with banding effect between major grid lines. See GridBandsAlpha and GridBandsColor. */
    GridBands: boolean;
    /** Gets or Sets color of grid bands. Default is Empty, meaning use a smart alpha of background for color, but if defined will show in any color. */
    GridBandsColor: number;
    /** Gets or Sets alpha value for grid bands when GridBandsColor=Empty. Default=10. Will simulate an alpha if RenderEngine not GdiPlus and BitmapGradientMode=false. Smaller number = lines less noticable. */
    GridBandsAlpha: number;
    /** Gets or Sets color of border of bar graphics. Set to -1 to disable bar border color for 2d bars. */
    BarBorderColor: number;
    Mono: {
      /** Gets or Sets monochrome color of table area's bounding box and text. */
      TableForeground: number;
      /** Gets or Sets monochrome color of table area's background. */
      TableBackground: number;
      /** If user presses 's' key to switch the ViewingStyle property, this controls if symbols are shown with monochrome charts. */
      WithSymbols: boolean;
      /** Gets or Sets monochrome color of grid area's bounding box and grid lines. */
      GraphForeground: number;
      /** Gets or Sets monochrome color of grid area's background. */
      GraphBackground: number;
      /** Gets or Sets monochrome color used as the background color behind titles and labels. */
      Desk: number;
      /** Gets or Sets monochrome color used as the foreground color for main titles and labels. */
      Text: number;
      /** If BorderTypes property is set to drop-shadow, this property controls monochrome color of drop-shadow when BitmapGradientMode is FALSE. */
      Shadow: number;
    };
    /** Gets or Sets color of right y axis grid numbers and label. */
    RYAxis: number;
    /** Gets or Sets color of minor grid tick marks. */
    TickColor: number;
    /** Gets or Sets color of the background used for all points/bars/pie slices when Plot.SubsetHatch is set. */
    HatchBackColor: number;
    /** Gets or Sets color of table area's bounding box and text. */
    TableForeground: number;
    /** Gets or Sets color of table area's background. */
    TableBackground: number;
    /** Gets or Sets starting gradient color for table area. Table gradients are enabled only if Color.BitmapGradientMode is True, Color.TableBackColor = Empty, and Color.TableGradientStyle is non-zero. */
    TableGradientStart: number;
    /** Gets or Sets ending gradient color for table area. Table gradients are enabled only if Color.BitmapGradientMode is True, Color.TableBackColor = Empty, and Color.TableGradientStyle is non-zero. */
    TableGradientEnd: number;
    /** Gets or Sets the style of table gradient. Table gradients are enabled only if Color.BitmapGradientMode is True, and Color.TableBackColor = Color.Tranparent. */
    TableGradientStyle: GradientStyle;
    /** Gets or Sets the style of table area bitmap. Table bitmaps are enabled only if Color.BitmapGradientMode is True, Color.TableBackColor = Empty, and Color.TableBmpFilename is set. */
    TableBmpStyle: BitmapStyle;
    /** Gets or Sets the opacity (0 to 100) for the TableBmpFilename bitmap. Set TableGradientStart, TableGradientEnd, and TableGradientStyle to combine a background color with background bitmap. */
    TableBmpOpacity: number;
    /** Gets or Sets filename used for the table area bitmap. Table bitmaps are enabled only if Color.BitmapGradientMode is True, Color.TableBackColor = Empty, and Color.TableBmpStyle is non-zero. */
    TableBmpFilename: string;
    /** Gets or Sets color and control of border line around solid points when PePlot.Method is set to include points. Default is argb 0 for no borderground, set to Color.Empty for border color to match brush, else set to any color as needed. */
    PointBorderColor: number;
    /** Default is empty, but can be used to define per subset gradient start colors. Only used if gradients are defined via AreaGradientStyle, BarGradientStyle, SplineGradientStyle. */
    SubsetGradientStartColors: PeArray1D<number>;
    /** Gets or Sets color on a per data point basis. To change point color per subset, use Plot.SubsetColors. If you use this property, you need to define color for all points unless Plot.SubsetForPointColors is used to designated which subsets use this property and which use the default Plot.SubsetColo... */
    PointColors: PeArray2D<number>;
    /** optionally control how PointColors property applies to Points and or Lines when PlottingMethod is PointsPlusLine 0 = Color the Points, default legacy behavior, 1 = Color the Line segments, 2 = Color the Points and the Line segments */
    PointColorPoints: number;
    /** Gets or Sets color of all grid lines when GridBold=false. Default is 1, meaning use a smart alpha of background for color, but if defined will show in any color. */
    GridLineColor: number;
    /** Gets or Sets alpha value for grid lines when GridBold=False and GridLineColor=1. Default=55. Will simulate an alpha if RenderEngine not GdiPlus and BitmapGradientMode=false. Smaller number = lines less noticable. */
    GridLineAlpha: number;
    /** Set True to show grid colors as legacy style, no alpha via GridLineAlpha. */
    GridBold: boolean;
    /** Gets or Sets color of y axis grid numbers and label. */
    YAxis: number;
    /** Gets or Sets color of x axis grid numbers / pointlabels and axis label. */
    XAxis: number;
    /** Gets or Sets color of y axis forecolor. */
    AxisForeColor: number;
    /** Gets or Sets color of y axis backcolor. */
    AxisBackColor: number;
    /** When set True, the custom file defined by GraphBmpFilename will always be the background bitmap even though the user might change the ViewingStyle setting via the customization dialog or popup menu if end user adjustments are enabled via AllowColorPage. */
    GraphBmpAlways: boolean;
    /** When set True, the custom file defined by DeskBmpFilename will always be the background bitmap even though the user might change the ViewingStyle setting via the customization dialog or popup menu if end user adjustments are enabled via AllowColorPage. */
    DeskBmpAlways: boolean;
    /** Gets or Sets colors used to identify subsets. Only used if Color.ViewingStyle = Color. */
    SubsetColors: PeArray1D<number>;
    /** Gets or Sets shaded colors used to identify subsets when Color.ViewingStyle is set to monochrome. Only bars, areas, pie slices, and other large filled areas are shaded. Points and Lines are always black in a monochrome viewing style. */
    SubsetShades: PeArray1D<number>;
    /** Gets or Sets whether the image is color or monochrome. */
    ViewingStyle: ViewingStyle;
    /** Gets or Sets the current predefined color configuation. There are two sets of configurations controlled via BitmapGradientMode. */
    QuickStyle: QuickStyle;
    /** Gets or Sets bitmap mode. TRUE to use Bitmap/Gradients quick style, FALSE to use color quick styles. */
    BitmapGradientMode: boolean;
    /** Gets or Sets color of grid area's bounding box and grid lines. */
    GraphForeground: number;
    /** Gets or Sets color of grid area's background. */
    GraphBackground: number;
    /** Gets or Sets color used as the background color behind titles and labels. */
    Desk: number;
    /** Gets or Sets color used as the foreground color for main titles and labels. */
    Text: number;
    /** If BorderTypes property is set to drop-shadow, this property controls color of drop-shadow when BitmapGradientMode is FALSE. */
    Shadow: number;
    /** Gets or Sets starting gradient color for desk area. Desk gradients are enabled only if Color.BitmapGradientMode is True, Color.Desk = Empty, and Color.DeskGradientStyle is non-zero. */
    DeskGradientStart: number;
    /** Gets or Sets ending gradient color for desk area. Desk gradients are enabled only if Color.BitmapGradientMode is True, Color.Desk = Empty, and Color.DeskGradientStyle is non-zero. */
    DeskGradientEnd: number;
    /** Gets or Sets the style of desk gradient. Desk gradients are enabled only if Color.BitmapGradientMode is True, and Color.Desk = Empty. */
    DeskGradientStyle: GradientStyle;
    /** Gets or Sets the style of desk bitmap. Desk bitmaps are enabled only if Color.BitmapGradientMode is True, Color.Desk = Empty, and Color.DeskBmpFilename is set. */
    DeskBmpStyle: BitmapStyle;
    /** Gets or Sets the opacity (0 to 100) for the DeskBmpFilename bitmap. Set DeskGradientStart, DeskGradientEnd, and DeskGradientStyle to combine a background color with background bitmap. */
    DeskBmpOpacity: number;
    /** Gets or Sets filename used for the desk bitmap. Desk bitmaps are enabled only if Color.BitmapGradientMode is True, Color.DeskBackColor = Empty, and Color.DeskBmpStyle is non-zero. See also DeskBmpAlways. */
    DeskBmpFilename: string;
    /** Gets or Sets starting gradient color for charting area. Graph gradients are enabled only if Color.BitmapGradientMode is True, Color.GraphBackColor = Empty, and Color.GraphGradientStyle is non-zero. */
    GraphGradientStart: number;
    /** Gets or Sets ending gradient color for charting area. Graph gradients are enabled only if Color.BitmapGradientMode is True, Color.GraphBackColor = Empty, and Color.GraphGradientStyle is non-zero. */
    GraphGradientEnd: number;
    /** Gets or Sets the style of graph gradient. Graph gradients are enabled only if Color.BitmapGradientMode is True, and Color.GraphBackColor = Empty. */
    GraphGradientStyle: GradientStyle;
    /** Gets or Sets the style of charting area bitmap. Graph bitmaps are enabled only if Color.BitmapGradientMode is True, Color.GraphBackColor = Empty, and Color.GraphBmpFilename is set. */
    GraphBmpStyle: BitmapStyle;
    /** Gets or Sets the opacity (0 to 100) for the GraphBmpFilename bitmap. Set GraphGradientStart, GraphGradientEnd, and GraphGradientStyle to combine a background color with background bitmap. */
    GraphBmpOpacity: number;
    /** Gets or Sets filename used for the charting area bitmap. Graph bitmaps are enabled only if Color.BitmapGradientMode is True, Color.GraphBackColor = Empty, and Color.GraphBmpStyle is non-zero. See also GraphBmpAlways. */
    GraphBmpFilename: string;
    /** When any ProEssentials PNG export feature is utilized, this property controls whether the output includes the transparent color defined by Color.PngTransparentColor. */
    PngIsTransparent: boolean;
    /** When any ProEssentials PNG export feature is utilized, this property controls the RGB color considered transparent if Color.PngIsTransparent is True. */
    PngTransparentColor: number;
    /** When any ProEssentials PNG export feature is utilized, this property controls whether the output is interlaced. */
    PngIsInterlaced: boolean;
    /** Gets and Sets the quality of JPEG export. Values range from 1 to 100 where 100 represents maximum quality. This quality setting effects any JPEG export features within the ProEssentials product. */
    JpgQuality: number;
  };
  PeString: {
    /** This property is a 2D version of PointLabels, adding multiple rows of PointLabels. Identical cells will be consolidated into one label. See also, PeGrid.Option.GridLineSeparators, PointLabelsIISeparators, PointLabelsIIBoxed */
    PointLabelsII: PeArray2D<string>;
    /** Related to PointLabelsII, set True to change grid line locations to border/separate points instead of grid lines centered on points. */
    GridLineSeparators: boolean;
    /** Related to PointLabelsII, set True to extend grid line ticks below chart to match depth of PointLabelsII. Also set PointLabelsIIBoxed True to fully enclose PointLabelsII. */
    PointLabelsIISeparators: boolean;
    /** Related to PointLabelsII and PointLabelsIISeparators, set this property True to fully enclose PointLabelsII within a thin line box. */
    PointLabelsIIBoxed: boolean;
    /** Gets or Sets text used as chart's right y axis label. */
    RYAxisLabel: string;
    /** Set to 2 or larger to form multi-line point labels. PointLabels will need to include pipe symbol '|' delimiters to signify where line-feeds exist within the point label. */
    PointLabelRows: number;
    /** Gets or Sets text used as chart's x axis label. */
    XAxisLabel: string;
    /** Gets or Sets text used as chart's y axis label. */
    YAxisLabel: string;
    /** Gets or Sets if YAxisLabels can be truncated to fit. */
    TruncateYAxisLabels: boolean;
    /** Gets or Sets if XAxisLabels can be truncated to fit. */
    TruncateXAxisLabels: boolean;
    /** Gets or Sets strings used to identify subsets. */
    SubsetLabels: PeArray1D<string>;
    /** Gets or Sets strings used to identify points. */
    PointLabels: PeArray1D<string>;
    /** Gets or Sets strings used to identify multiple sub titles. Indices 0 through 9 allow for 10 extra sub titles. Each subtitle has a potential left, centered, and right justified component delimited with the pipe character | . */
    MultiSubTitles: PeArray1D<string>;
    /** Gets or Sets strings used to identify multiple bottom sub titles. Indices 0 through 9 allow for 10 extra sub titles. Each subtitle has a potential left, centered, and right justified component delimited with the pipe character | . */
    MultiBottomTitles: PeArray1D<string>;
    /** To support multi-byte strings when RenderEngine is set to GDI(0) and within exported metafiles, WideCharToMultiByte must be set True and CodePage and CharSet set appropriately. */
    WideCharToMultiByte: boolean;
    /** To support multi-byte strings when RenderEngine is set to GDI(0) and within exported metafiles, WideCharToMultiByte must be set True and CodePage and CharSet set appropriately. */
    CodePage: number;
    /** To support multi-byte strings when RenderEngine is set to GDI(0) and within exported metafiles, WideCharToMultiByte must be set True and CodePage and CharSet set appropriately. */
    CharSet: number;
    /** Gets or Sets text used as chart's main title. Pipe symbols can be used to separate into 3 regions, left, middle, and right. */
    MainTitle: string;
    /** Gets or Sets text used as chart's sub title. Pipe symbols can be used to separate into 3 regions, left, middle, and right. */
    SubTitle: string;
    /** Gets or Sets if MainTitle and SubTitle text can be truncated to fit. */
    TruncateTitles: boolean;
    /** Gets and Sets whether text will include a drop-shadow effect. */
    TextShadows: TextShadows;
    /** If false, dark text is never shadowed. If true, dark text is drawn with a 3D inset effect rather than shadowed. */
    DarkTextInset: boolean;
  };
  PeUserInterface: {
    Scrollbar: {
      /** Set to True to enable the ZoomIn events to be generated upon zooming inward and outward via MouseWheel as enabled by MouseWheelFunction. */
      MouseWheelZoomEvents: boolean;
      /** Gets and Sets adjustment to zoom increment per mouse wheel message. Default 1.25. Set larger ie 1.5 to zoom faster, or 1.01 to zoom slower. */
      MouseWheelZoomFactor: number;
      /** Gets and Sets adjustment to zoom increment per gesture zoom message. Default 1.1. Set larger ie 1.5 to zoom faster, or 1.01 to zoom slower. */
      PinchZoomFactor: number;
      /** Gets or Sets the quantity of extra image updates used to animate the zoom via mouse wheel. Default 0. */
      MouseWheelZoomSmoothness: number;
      /** Gets or Sets the quantity of extra image updates used to animate the zoom via pinch. Default 0. */
      PinchZoomSmoothness: number;
      /** Gets or Sets the quantity of points that get viewed simultaneously, enabling a horizontal scrollbar to access other points. */
      PointsToGraph: number;
      /** If PointsToGraph is non-zero, Gets or Sets whether the first or last points are initially shown. */
      PointsToGraphInit: PointsToGraphInit;
      /** Read Only after calling Function.ReinitializeResetImage. Use this property to get the extents of the current horizontal scrolling range. */
      ScrollingRange: number;
      /** Read Only after calling Function.ReinitializeResetImage. When Data.Points is more than 32000 then scrolling 1 position on the horizontal scrollbar actually moves the chart by more than one point index. The quantity of points scrolled in one scrollbar position is read from this property. */
      ScrollingFactor: number;
      /** The property defaults to ScrollingPointLabels which enables a new scrolling style for Graph objects. In this new style, if there is no table shown, the PointLabels scroll/move with the data. */
      HScrollStyle: HScrollStyle;
      /** Set true to disable new logic that performs immediate chart updates while dragging scrollbars. */
      DisableThumbTracking: boolean;
      /** Gets or Sets whether user can click, grab and drag the chart via the mouse in horizontal direction. The horizontal scrollbar must be visible for this feature to function. */
      MouseDraggingX: boolean;
      /** Gets or Sets whether user can click, grab and drag the chart via the mouse in vertical direction. The vertical scrollbar must be visible for this feature to function. */
      MouseDraggingY: boolean;
      /** Gets or Sets the quantity of subsets that get viewed simultaneously, enabling a vertical scrollbar to access other subsets. */
      ScrollingSubsets: number;
      /** Gets or Sets the increment/decrement of the cursor position when the user presses the Page-Up/Page-Down keys. */
      CursorPageAmount: number;
      /** Gets or Sets the current horizontal scrollbar position. NOTE: only set this property after the control is initialized via one of the Functions.Reinitialize.. functions. */
      HorzScrollPos: number;
      /** Gets or Sets the current vertical scrollbar position. NOTE: only set this property after the control is initialized via one of the Functions.Reinitialize.. functions. */
      VertScrollPos: number;
      /** Gets or Sets whether a vertical scrollbar will be made available upon zooming. The scrollbar will allow the user to pan up and down to view all of the graph s data. This feature is NOT compatible with the use of Grid.Configure.MultiAxesSubsets. */
      ScrollingVertZoom: boolean;
      /** Gets or Sets whether y axes change their min and max values to match the currently viewed subsets and or points, or as user zooms and pans the chart. */
      ScrollingScaleControl: boolean;
      /** Read Only, gets the most recent scrollbar code cooresponding to latest scroll event. Used within PreHScroll and PreVScroll events. If the developer wants to prevent the future scroll event, set this property to -1. If the developer wants to alter the future scrollbar position, set this property t... */
      SBCode: number;
      /** Read Only, gets the most recent scrollbar position cooresponding to latest scroll event. Used with PreHScroll and PreVScroll events. */
      SBPos: number;
      /** Gets or Sets the action of the MouseWheel. */
      MouseWheelFunction: MouseWheelFunction;
    };
    Dialog: {
      /** Gets or Sets whether user has ability of adjusting PointsToGraph item within the "Points" tab of the customization dialog. */
      PointsToGraph: boolean;
      /** Gets and Sets the maximum quantity of data points selectable within the PointsToGraph item of the Points tab of the customization dialog. */
      MaxPointsToGraph: number;
      /** Gets or Sets whether user has access to the 'Points' property page within customization dialog. */
      Points: boolean;
      /** Gets or Sets whether user has ability of adjusting ScrollingSubsets item within the "Subsets" tab of the customization dialog. */
      ScrollingSubsets: boolean;
      /** Gets or Sets whether user has ability of adjusting Plot Style item within the "Plot" tab of the customization dialog. */
      PlotCustomization: boolean;
      /** Gets or Sets whether user has access to the 'Subsets' property page within customization dialog. */
      Subsets: boolean;
      /** Gets or Sets whether user has access to the 'Axis' property page within customization dialog. */
      Axis: boolean;
      /** Controls if the customization dialog sends PeCustomizeDlg event when the dialog closes. Older versions only sent this event when the OK button was pressed. Now if the users closes the dialog or the dialog closes because of ModelessAutoClose, the developer can test for this case by reading the pro... */
      CustomizeDlgCancelMsg: boolean;
      /** Read only, gets the last dialog result code when the customization dialog closes. IDOK=1 IDCANCEL=2 IDEXPORT=1015 IDMAXIMIZE=1016 IDORIGINAL=1109 IDAPPLYNOW=12321 */
      DialogResult: number;
      /** When exporting PNG to clipboard, if True, export is sent as a DIB rather than PNG. The PNG clipboard format is not commonly supported by containers and a DIB format often makes more sense. /// */
      ExportPngAsDibToClip: boolean;
      /** Gets or Sets whether the Jpeg image format is shown within the ProEssentials Export Dialog. /// */
      AllowJpegExport: boolean;
      /** Gets or Sets whether the SVG image format is shown within the ProEssentials Export Dialog. /// */
      AllowSvgExport: boolean;
      /** Gets or Sets whether the EMF image format is shown within the ProEssentials Export Dialog. /// */
      AllowEmfExport: boolean;
      /** Gets or Sets whether the WMF image format is shown within the ProEssentials Export Dialog. /// */
      AllowWmfExport: boolean;
      /** Gets or Sets the DotsPerInch info when exporting an image by calling a function or using built-in export dialog. If zero, no DPI information is embedded. */
      ExportImageDpi: number;
      /** Gets or Sets whether the ExportImageDpi setting is shown within the ProEssentials export dialog. /// */
      HideExportImageDpi: boolean;
      /** Gets or Sets whether exported enhanced metafiles from the built-in export dialog will include background bitmaps and gradients. /// */
      EmfBitmapGradients: boolean;
      /** Gets or Sets the type of enhanced metafile created when exported from built-in export dialog. */
      EmfType: EmfType;
      /** Gets or Sets the target Device Context used when exporting enhanced metafiles from the built-in export dialog. DisplayDpiAdjusted uses the display DC but then manipulates the EMF header to simulate a higher DPI. */
      EmfDC: EmfDC;
      /** When exporting a chart as a high resolution image (larger than 2000 pixels wide or tall,) setting this property True will use alternate fixed font sizing logic producing larger fonts. /// */
      ExportImageLargeFont: boolean;
      /** Gets or Sets the target DotsPerInch when printing. If zero, the chart image is sent via the actual graphic primitives. If non zero, the chart image is prepared as a bitmap and this bitmap is sent to the printer. */
      PrintDpi: number;
      /** Gets or Sets whether the PrintDpi setting is shown within the ProEssentials print dialog. /// */
      HidePrintDpi: boolean;
      /** Gets or Sets a property that controls how printing is sent to the printer when PrintDpi is non-zero, as GDI (prepares as BMP and StretchBlts to printer) or GDIPLUS (prepares as PNG.) */
      PrintTechnology: PrintTechnology;
      /** Gets or Sets default orientation of the printer paper when printing. */
      DefOrientation: DefOrientation;
      /** Gets or Sets whether the user has access to selecting monochrome or color printing from the built-in print dialog. Also controls whether dialog defaults to monochrome output. */
      PrintStyleControl: PrintStyleControl;
      /** Gets or Sets whether user has access to the first General property page within customization dialog. */
      Page1: boolean;
      /** Gets or Sets whether user has access to the second property page within customization dialog. Pego, Pesgo, and Pepso objects name the second property page 'Plot'. Pepco objects name the second property page "Slices". Pe3do objects name the second property page "More". */
      Page2: boolean;
      /** Gets or Sets whether user has access to the 'Color' property page within customization dialog. */
      Color: boolean;
      /** Gets or Sets whether user has access to the 'Font' property page within customization dialog. */
      Font: boolean;
      /** Gets or Sets whether user has access to the 'Style' property page within customization dialog. */
      Style: boolean;
      /** This property controls the maximum number of decimal positions that the user can select from either the customization dialog or popup menu. Possible values are: 3 through 7. */
      MaxDataPrecision: number;
      /** This property controls how all built-in dialogs are launched, modal, or modeless. Dialogs are modeless by default unless Dialog.Modal is set to TRUE. A modal dialog must be closed before accessing the rest of the application. If using a modeless dialog, this lets the user shift the focus from the... */
      Modal: boolean;
      /** Set this property to TRUE for modeless dialogs to always be on top of all other open windows. */
      ModelessOnTop: boolean;
      /** This property controls how the modeless dialogs are closed. TRUE = Modeless dialogs will close if focus is lost. FALSE = Modeless dialogs will close only if Ok or Cancel is selected. */
      ModelessAutoClose: boolean;
      /** Gets or Sets the default export type button selected in the export dialog. */
      ExportTypeDef: ExportTypeDef;
      /** Gets or Sets the default destination button selected in the export dialog. */
      ExportDestDef: ExportDestDef;
      /** Gets or Sets the default size button selected in the export dialog. */
      ExportSizeDef: ExportSizeDef;
      /** Gets and Sets the default file name for graphic and text export within Export Dialog. */
      ExportFileDef: string;
      /** Gets and Sets the textual default unit width within the Export Dialog. */
      ExportUnitXDef: string;
      /** Gets and Sets the textual default unit height within the Export Dialog. */
      ExportUnitYDef: string;
    };
    HotSpot: {
      /** Gets or Sets whether user has ability of clicking PointLabels. */
      Point: boolean;
      /** Gets or Sets whether user has ability of clicking table cells. */
      Table: boolean;
      /** Empty by default, if you define elements of this property array, only those subset indices defined will generate hot spot locations and events. To later enable all subsets as hotspots, empty like HotSpot.SubsetForHotSpots.Clear() When the chart is initialized with this setting, the search for hot... */
      SubsetForHotSpots: PeArray1D<number>;
      /** Empty by default, if you define elements of this property array, then only those subset indices defined will be enabled to work with the PEgethotspot hot spot search mechanism. This enables a quick way to enable/disable potential hot spot events without rebuilding the image and resulting hot spot... */
      SubsetForGetHotSpot: PeArray1D<number>;
      /** Gets or Sets whether individual y axis line annotation indices are clickable by user. UI.HotSpot.YAxisLineAnnotation must be True to use this property. */
      YAxisLineAnnotations: PeArray1D<number>;
      /** Gets or Sets whether individual x axis line annotation indices are clickable by user. UI.HotSpot.XAxisLineAnnotation must be True to use this property. */
      XAxisLineAnnotations: PeArray1D<number>;
      /** Read Only, Normally the GridNumber event retrieves grid number values as part of the event handler. The developer can use this property along with Function.GetHotSpot and Function.GetHotSpotData when the hot spot type identifies a grid number. */
      GridNumber: PeArray1D<number>;
      /** Gets or Sets whether user has ability of clicking anywhere within grid area. */
      Graph: boolean;
      /** Gets or Sets whether user has ability of clicking graph annotations. */
      GraphAnnotation: AnnotationHotSpot;
      /** Gets or Sets whether user has ability of clicking y axis line annotations. */
      YAxisLineAnnotation: AnnotationHotSpot;
      /** Gets or Sets whether user has ability of clicking x axis line annotations. */
      XAxisLineAnnotation: AnnotationHotSpot;
      /** Gets or Sets whether user has ability of clicking x axis annotations. */
      XAxisAnnotation: AnnotationHotSpot;
      /** Gets or Sets whether user has ability of clicking y axis annotations. */
      YAxisAnnotation: boolean;
      /** Gets and Sets the size of data hot spots. */
      Size: HotSpotSize;
      /** Gets or Sets whether user has ability of clicking axis label text. */
      AxisLabel: boolean;
      /** Gets or Sets whether user has ability of clicking axes. */
      Axis: boolean;
      /** Gets or Sets whether user has ability of clicking y axis grid numbers. */
      GridNumberY: boolean;
      /** Gets or Sets whether user has ability of clicking SubsetLabels. */
      Subset: boolean;
      /** Gets or Sets whether user has ability of clicking Data Points. */
      Data: boolean;
      /** Gets or Sets whether user has ability of clicking MainTitle and SubTitle text. */
      Title: boolean;
      /** Gets or Sets whether user has ability of clicking MultiSubTitle text. */
      MultiSubTitle: boolean;
      /** Gets or Sets whether user has ability of clicking MultiBottomTitle text. */
      MultiBottomTitle: boolean;
    };
    Allow: {
      /** This property will enable individual axes to be sized by using the mouse and clicking and dragging the lower axis of a Multi-Axes section. The mouse displays a cursor defined by Cursor.SizeNS while dragging. */
      MultiAxesSizing: boolean;
      /** Gets or Sets zooming restraints. */
      ZoomLimits: ZoomLimits;
      /** Gets or Sets the zoom-box dragging size threshold to enable zoom (1 - 100 pixels) Default is the user must drag 20 pixels to enable zoom. */
      ZoomThreshold: number;
      /** Gets or Sets user's access to built-in zooming functionality. Right clicking control or pressing 'z' un-zooms. */
      Zooming: AllowZooming;
      /** Gets or Sets whether the built-in zooming either zooms as normal, or works as a user interface mechanism allowing user to draw/select rectanges or lines. */
      ZoomInterfaceOnly: ZoomInterfaceOnly;
      /** Gets or Sets the framing style of the zoom box rectangle. */
      ZoomStyle: ZoomStyle;
      /** Gets or Sets whether grid coordinates are shown if the grid is double clicked. */
      CoordinatePrompting: boolean;
      /** Gets or Sets ability to access text export features of export dialog. */
      TextExport: boolean;
      /** Gets or Sets ability to access customization dialog. Double click or press 'spacebar' to invoke dialog. */
      Customization: boolean;
      /** Gets or Sets ability to access export dialog. Pressing 'x' invokes dialog. */
      Exporting: boolean;
      /** Gets or Sets ability to access maximization dialog. Pressing 'm' invokes dialog. */
      Maximization: boolean;
      /** Gets or Sets ability to access popup menu. Right clicking control or pressing 'q' invokes menu. */
      Popup: boolean;
      /** Gets or Sets the enabling of mouse or keyboard based built in features. */
      UserInterface: AllowUserInterface;
      /** Gets or Sets availability of focal rect. True shows focal rect, False hides focal rect. */
      FocalRect: boolean;
    };
    Cursor: {
      /** When set to True the end-user does not need to click on a data hotspot to move the cursor (when CursorMode is set to a mode giving a subset and point index focus.) The control will search for the closest data point to the mouse. For Pesgo controls, the XData must be sequentially increasing. This ... */
      MouseCursorControlClosestPoint: boolean;
      /** Gets or Sets the mouse's cursor style while changing axis size via UI.Allow.MultiAxesSizing property. Setting to 0 uses default North South sizing style. Set to alternate values defined in ProEssentials.Enums.MouseCursorStyles. Also set to any operating system based cursor handle like Cursor.Hand... */
      SizeNS: number;
      /** Read only, after calling GetHotSpotData and or CursorPromptTracking and or HighlightColor is non zero. Gets Y value under mouse position. */
      CursorValueY: number;
      /** Set to .5 to 2.0 to decrease or increase the size of the text used for the ToolTip or Text CursorPromptLocation styles. Default = 1.0. */
      FontSizeTrackingCntl: number;
      /** This property is used to set a maximum tool tip width. If string content will not fit, the text will be forced to line-feed. Set to a value from 50 to 2000 pixels. This features is ideal if one wants to force a tool tip with multiple items, like x, y and z data prompting (CursorPromptStyle), to p... */
      TrackingTooltipMaxWidth: number;
      /** This property is read within the CustomTrackingDataText event handler to learn how the event was triggered. */
      TrackingPromptTrigger: TrackingTrigger;
      /** Set to True and also implement the CustomTrackingDataText event handler to supply event based data prompt text. Within the CustomTrackingDataText event handler, one will set the property TrackingText which provides the requested text. The tracking events for this feature are limited to within the... */
      TrackingCustomDataText: boolean;
      /** Set to True and also implement the CustomTrackingOtherText event handler to supply event based data prompt text. Within the CustomTrackingOtherText event handler, one will set the property TrackingText which provides the requested text. The tracking events for this feature are limited to outside ... */
      TrackingCustomOtherText: boolean;
      /** Sets a bold title string as the first line of the ToolTip cursor prompt when CursorPromptTracking is True and CursorPromptLocation is set to ToolTip or Text. The title string can be static, or set dynamically if TrackingCustomDataText is True and handling the CustomTrackingDataText message/event. */
      TrackingTooltipTitle: string;
      /** Sets the tool tip background color when CursorPromptTracking is True and CursorPromptLocation is set to ToolTip. The color can be static, or set dynamically if TrackingCustomDataText is True and handling the CustomTrackingDataText message/event. */
      TrackingTooltipBkColor: number;
      /** Sets the tool tip text color when CursorPromptTracking is True and CursorPromptLocation is set to ToolTip or Text. The color can be static, or set dynamically if TrackingCustomDataText is True and handling the CustomTrackingDataText message/event. */
      TrackingTooltipTextColor: number;
      /** Gets or Sets the data point quantity threshold when the mouse cursor changes to an hour-glass while building image like Cursor.Handle.ToInt32(). */
      HourGlassThreshold: number;
      /** Gets or Sets the mouse's cursor style while zooming. Setting to 0 uses default magnifying glass style. Set to alternate values defined in ProEssentials.Enums.MouseCursorStyles. Also set to any operating system based cursor handle like Cursor.Handle.ToInt32(). */
      Zoom: number;
      /** Gets or Sets the mouse's cursor style while zooming and zoom not possible. Setting to 0 uses default no-drop style. Set to alternate values defined in ProEssentials.Enums.MouseCursorStyles. Also set to any operating system based cursor handle like Cursor.Handle.ToInt32(). */
      NoDrop: number;
      /** Gets or Sets cursor style. Cursor styles POINT, DATACROSS, DATASQUARE, and FLOATINGY are all designed to give either a point index the focus or an individual data point the focus. */
      Mode: CursorMode;
      /** Gets or Sets the zero-based index of the subset that has the cursor s focus. If setting this property, set Cursor.Point after this property. */
      Subset: number;
      /** Gets or Sets the zero-based index of the point that has the cursor s focus. If setting this property, set Cursor.Subset before this property. */
      Point: number;
      /** If Cursor.PromptTracking is true, Gets or Sets the style of coordinate prompting when moving the mouse or cursor. */
      PromptStyle: CursorPromptStyle;
      /** Setting this property to true causes the cursor to be drawn to a cached screen, enables CursorColor, and enables anti-aliasing of the cursor. If CursorColor is set to 1, we will auto set the color, though it may be best to set your own CursorColor. Setting CursorHorzLineType to thick dash (or oth... */
      ImprovedCursor: boolean;
      /** Setting this property to true causes the cursor to be drawn to a cached screen. This will enable the CursorColor property and you will not be forced into an ROP2 inverted color cursor. The cursor will be drawn with GDI without anti-aliasing. If you enable ImprovedCursor, the cursor will be drawn ... */
      DrawCursorToCache: boolean;
      /** Controls the color of the cursor when using CursorMode. Default value or 1, where solid lines will use an ROP2 color (inverted color) and it Cursor line type is dashed, then a color will be automatically chosen. base on background color. Else define any 32 bit color. Where the alpha channel value... */
      CursorColor: number;
      /** Allow control of style of horiztonal cross hair cursor. Set to a value of Enums.LineAnnotationType */
      HorzLineType: LineAnnotationType;
      /** Allow control of style of vertical cross hair cursor. Set to a value of Enums.LineAnnotationType */
      VertLineType: LineAnnotationType;
      /** When set to True, if the cursor prompt string contains a numeric value over 9 digits, then the format is switched to exponential form so the string never exceeds 9 digits. */
      PromptShorten: boolean;
      /** Gets or Sets whether the graph continuously prompts the user of the data point value directly under the mouse cursor. See also: Cursor.PromptStyle */
      PromptTracking: boolean;
      /** If Cursor.PromptTracking is true, Gets or Sets the location of coordinate prompting when moving the mouse or cursor. */
      PromptLocation: CursorPromptLocation;
      /** Gets or Sets whether the user can use the mouse to move the cursor's focus to a data point clicked. See also: Cursor.Mode */
      MouseCursorControl: boolean;
      /** Gets or Sets the mouse's cursor style over hot spots. Setting to 0 uses default hand style. Set to alternate values defined in ProEssentials.Enums.MouseCursorStyles. Also set to any operating system based cursor handle like Cursor.Handle.ToInt32(). */
      Hand: number;
      /** Gets the mouse's most recent mouse move coordinates. */
      LastMouseMove: {
        /** Pixel X the engine last saw the pointer at. */
        readonly x: number;
        /** Pixel Y the engine last saw the pointer at. */
        readonly y: number;
        /** False when the engine has no position to report yet. */
        readonly ok: boolean;
      };
      /** Read only. Read within MouseMove event to see if any built-in ProEssentials based logic is currently also processing the mousemove event. This prevents conflict in custom versus built-in user interaction type logic. */
      ProcessingMouseMove: boolean;
      /** A Windows cursor handle on the desktop controls. A browser has no cursor handles -- set the CSS cursor on the host element instead. */
      Arrow: number;
      /** Gets the mouse's most recent mouse move coordinates. */
      LastMouseMovePoint: {
        /** Pixel X the engine last saw the pointer at. */
        readonly x: number;
        /** Pixel Y the engine last saw the pointer at. */
        readonly y: number;
        /** False when the engine has no position to report yet. */
        readonly ok: boolean;
      };
    };
    Menu: {
      /** Gets and Sets whether the Graph Plus Table menu is visible. */
      GraphPlusTable: MenuControl;
      /** Gets and Sets whether the Force Vertical Points menu is visible. */
      ForceVerticalPoints: MenuControl;
      /** Gets and Sets whether the Table What menu is visible. */
      TableWhat: MenuControl;
      /** Gets and Sets whether the GridBold menu is visible. */
      GridBold: MenuControl;
      /** Gets and Sets whether the GridBands menu is visible. */
      GridBands: MenuControl;
      /** Gets and Sets whether the Data Shadow menu is visible. */
      DataShadow: MenuControl;
      /** Gets or Sets whether the Show Annotations menu item is included in built-in popup menu. */
      AnnotationControl: boolean;
      /** Gets and Sets whether the Multi Axis Style menu is visible. */
      MultiAxisStyle: MenuControl;
      /** Gets and Sets whether the Grid Line Style menu is visible. */
      GridLine: MenuControl;
      /** Gets and Sets whether the Plotting Method menu is visible. */
      PlotMethod: MenuControl;
      /** Gets and Sets whether the Grid In Front menu is visible. */
      GridInFront: MenuControl;
      /** Gets and Sets whether the Treat Comparisons As Normal menu is visible. */
      ComparisonsAsNormal: MenuControl;
      /** Gets and Sets whether the Mark Data Points menu is visible. */
      MarkDataPoints: MenuControl;
      /** Gets and Sets whether the Show Annotations menu is visible. */
      ShowAnnotations: MenuControl;
      /** Gets and Sets whether the Undo Zoom menu is visible. */
      UndoZoom: MenuControl;
      /** Gets and Sets whether the Y Axis Long Tick menu is visible. */
      YAxisLongTick: MenuControl;
      /** Gets and Sets whether the Show Annotation Labels menu is visible. */
      ShowAnnotationText: MenuControl;
      /** This property controls the visibility of custom menu items. Custom menus are created with the UI.Menu.CustomMenuText property. */
      CustomMenu: PeArray2D<number>;
      /** This property controls the checked state of custom menu items. For single string menu items, submenu index is set to zero. For popup menus, submenu index is set to 1 through nth item in sub menu. Custom menus are created with the UI.Menu.CustomMenuText property. */
      CustomMenuState: PeArray2D<number>;
      /** This property controls the location of custom menu items. Custom menus are created with the UI.Menu.CustomMenuText property. */
      CustomMenuLocation: PeArray1D<number>;
      /** This property creates custom menu items by providing their text. For simple string menu items, provide one string with no delimiters (no pipe symbols '|'). To produce a popup menu, provide a string with 2 or more delimiters ('|') which divide text into multiple menu text items. The first delimite... */
      CustomMenuText: PeArray1D<string>;
      /** Gets or Sets ability to access popup menu. Right clicking control or pressing 'q' invokes menu. */
      Show: boolean;
      /** This property controls the maximum number of decimal positions that the user can select from either the customization dialog or popup menu. Possible values are: 3 through 7. */
      MaxDataPrecision: number;
      /** Gets and Sets whether the Viewing Style menu is visible. */
      ViewingStyle: MenuControl;
      /** Gets and Sets whether the Font Size menu is visible. */
      FontSize: MenuControl;
      /** Gets and Sets whether the Data Precision menu is visible. */
      DataPrecision: MenuControl;
      /** Gets and Sets whether the separators are visible is popup menu. */
      Separator: boolean;
      /** Gets and Sets whether the Maximize menu is visible. */
      Maximize: MenuControl;
      /** Gets and Sets whether the Customization Dialog menu is visible. */
      CustomizeDialog: MenuControl;
      /** Gets and Sets whether the Export Dialog menu is visible. */
      ExportDialog: MenuControl;
      /** Gets and Sets whether the Border Type menu is visible. */
      BorderType: MenuControl;
      /** Gets and Sets whether the Show Legend menu is visible. */
      ShowLegend: MenuControl;
      /** Gets and Sets whether the Legend Location menu is visible. */
      LegendLocation: MenuControl;
      /** Gets and Sets whether the Show Table Annotations menu is visible. */
      ShowTableAnnotations: MenuControl;
      /** Gets and Sets whether the Fixed Font menu is visible. */
      FixedFont: MenuControl;
      /** Gets and Sets whether the Bitmap Gradient menu is visible. */
      BitmapGradient: MenuControl;
      /** Gets and Sets whether the Quick Style menu is visible. */
      QuickStyle: MenuControl;
    };
  };
  PeGrid: {
    /** When PeConfigure.BorderTypes is set to NoBorders, this property controls if axes still include a border line. */
    AxisBorder: boolean;
    /** True to show grid with banding effect between major grid lines. See PeColor.GridBandsAlpha and PeColor.GridBandsColor. */
    GridBands: boolean;
    /** Gets and Sets the working multi-axis index (0-5) used when accessing multi-axis related properties. */
    WorkingAxis: number;
    /** Allows per subset control of axis index. The subset order is not related to setting MultiAxesSubsets. Gets and Sets the axis index. SubsetAxes[nSubset] = 0-15 When not using this feature, be sure it is emptied with .Clear() */
    SubsetAxes: PeArray1D<number>;
    /** Subdivides subsets among multiple axes. Gets and Sets the quantity of subset per axis index. See help for more info. */
    MultiAxesSubsets: PeArray1D<number>;
    /** Used in combination with MultiAxesSubsets, Gets and Sets the proportional size (.10 through .90) for each axis index. All indices should add up to 1.0 */
    MultiAxesProportions: PeArray1D<number>;
    /** Subdivides multiple axes into grouped overlapping axes. Gets and Sets the quantity of axes per overlapped region index. See help for more info. */
    OverlapMultiAxes: PeArray1D<number>;
    Option: {
      /** Gets and Sets whether x axis grid lines are forced visible or hidden. Mostly used to improve overlapped axis configurations. */
      ForceGridLinesX: ForceGridLines;
      /** Gets and Sets whether y axis grid lines are forced visible or hidden. Mostly used to improve overlapped axis configurations. */
      ForceGridLinesY: ForceGridLines;
      /** Gets and Sets whether right y axis grid lines are forced visible or hidden. Mostly used to improve overlapped axis configurations. */
      ForceGridLinesRY: ForceGridLines;
      /** Gets or Sets whether log scales show grid numbers in scientific notation or normal numeric. */
      LogScaleExpLabels: boolean;
      /** Gets or Sets the orientation of vertical point labels and vertical x axis grid numbers. True = 90 degrees, False = 270 degrees. */
      VertOrient90Degrees: boolean;
      /** Gets or Sets whether y axis is duplicated/mirrored on the opposite side of grid. */
      ForceRightYAxis: boolean;
      /** Gets or Sets whether y axis is on left or right side of chart. True=right, False=left. If both left and right axes are present, this property reverses their location. This property applies to all y axes in a multi y axes implementation. */
      YAxisOnRight: boolean;
      /** Gets and Sets whether right y axis grid lines, numbers, and labels are visible. */
      ShowRYAxis: ShowAxis;
      /** Gets and Sets whether x axis grid lines, numbers, and labels are visible. */
      ShowXAxis: ShowAxis;
      /** Gets or Sets whether y axis is vertically reversed in polarity. Subset data must be multiplied by (-1) when passed into the chart to achieve the desired effect. */
      InvertedYAxis: boolean;
      /** Gets or Sets whether right y axis is vertically reversed in polarity. Subset data must be multiplied by (-1) when passed into the chart to achieve the desired effect. */
      InvertedRYAxis: boolean;
      /** Gets or Sets whether right y axis minor tick marks extend across chart as major grid lines. */
      RYAxisLongTicks: boolean;
      /** Gets and Sets whether grouped multiple y axes are separated in varying fashions. Only use this feature when Grid.Option.MultiAxisStyle is Grouped. */
      MultiAxesSeparators: MultiAxesSeparators;
      /** Gets or Sets a format string used to format y axis grid numbers. {PRE-TEXT}|{.}{,}{0000}|{POST-TEXT} Where all items are optional except pipe symbols, and one or more zeros can be provided in middle section. Comma and period order not important. Comma means add thousand separator. */
      AxisFormatY: string;
      /** Gets or Sets a format string used to format right y axis grid numbers. {PRE-TEXT}|{.}{,}{0000}|{POST-TEXT} Where all items are optional except pipe symbols, and one or more zeros can be provided in middle section. Comma and period order not important. Comma means add thousand separator. */
      AxisFormatRY: string;
      /** Controls numerical format of Y Axis. For Pego and Pesgo controls, set WorkingAxis prior to getting/setting this property. */
      AxisNumericFormatY: AxisNumericFormat;
      /** Controls numerical format of right Y Axis. For Pego and Pesgo controls, set WorkingAxis prior to getting/setting this property. */
      AxisNumericFormatRY: AxisNumericFormat;
      /** Gets and Sets whether y axis grid numbers are formatted as fractional numbers or decimals. */
      SpecialScalingY: SpecialScaling;
      /** Gets and Sets whether right y axis grid numbers are formatted as fractional numbers or decimals. */
      SpecialScalingRY: SpecialScaling;
      /** If a log scale s range goes beyond the number of cycles defined by this property, log scale tick marks are shortened rather than extended across the graph. Default is 7 cycles. */
      LogTickThreshold: number;
      /** Set this property to True to enable event driven right y axis grid number formatting via CustomGridNumber event. Within the CustomGridNumber event, the developer can set the e.NumberString argument which controls the text for the grid line. */
      CustomGridNumbersRY: boolean;
      /** This property controls the height of the section/gap produced by Grid.Option.MultiAxisStyle = SeparateAxes. Units are logical and are 1/100th of the size of the x axis label height. For example, a value of 100 produces an axis gap equal to the x axis label height, 200 produces an axis gap twice t... */
      MultiAxisSeparatorSize: number;
      /** This property determines if multiple axes will be stacked or separated. The size of the separation may be adjusted with Grid.Option.MultiAxisSeparatorSize. Legends may also be inserted between axes using Legend.Style. */
      MultiAxisStyle: MultiAxisStyle;
      /** This property can be used to reduce the chartable height of the left y axis. Because the axis no longer consumes the entire possible axis height, Grid.Option.AxisLocationY is used to control where the resulting axis is located. Values range from 20 to 100 representing 20% to 100%. */
      AxisSizeY: number;
      /** This property controls axis location when Grid.Option.AxisSizeY is used to force a smaller charting left y axis height. Units are 0 to (100 - AxisSizeY). */
      AxisLocationY: number;
      /** This property can be used to reduce the chartable height of the right y axis. Because the axis no longer consumes the entire possible axis height, Grid.Option.AxisLocationRY is used to control where the resulting axis is located. Values range from 20 to 100 representing 20% to 100%. */
      AxisSizeRY: number;
      /** This property controls axis location when Grid.Option.AxisSizeRY is used to force a smaller charting right y axis height. Units are 0 to (100 - AxisSizeRY). */
      AxisLocationRY: number;
      /** Gets and Sets the year-month-day prompt format when Data.DateTimeMode = True. This prompt generally communicates the time span of the x axis. */
      YearMonthDayPrompt: YearMonthDayPrompt;
      /** Gets and Sets the order of year, month, day witin bottom-left and top-axis date prompts when DateTimeMode = True. */
      YearMonthDayStyle: YearMonthDayStyle;
      /** Gets and Sets the time label grid line format when Data.DateTimeMode = True. */
      TimeLabelType: TimeLabelType;
      /** Gets and Sets the day label grid line format when Data.DateTimeMode = True. */
      DayLabelType: DayLabelType;
      /** Gets and Sets the month label grid line format when Data.DateTimeMode = True. */
      MonthLabelType: MonthLabelType;
      /** Gets and Sets the year label grid line format when Data.DateTimeMode = True. */
      YearLabelType: YearLabelType;
      /** This property is only used when Grid.Option.YAxisVertGridNumbers is True. Set to True to place axis labels at top of axis in replacement of grid numbers near top of axis. */
      VgnAxisLabelLocation: boolean;
      /** Set to True to draw Y axis grid numbers vertical rather than horizontal. See also, Grid.Option.VgnAxisLabelLocation. */
      YAxisVertGridNumbers: boolean;
      /** This property controls whether date/time x axis scales account for day light savings. Set to True for x axis to spring forward an hour in spring and fall backward an hour in fall. See also: Function.SetSpringDayLight, Function.SetFallDayLight */
      DayLightSavings: boolean;
      /** Gets and Sets the style of tick marks for y axis. Establish Grid.WorkingAxis before setting this property if working with multiple y axes. */
      ShowTickMarkY: ShowTickMarks;
      /** Gets and Sets the style of tick marks for right y axis. Establish Grid.WorkingAxis before setting this property if working with multiple y axes. */
      ShowTickMarkRY: ShowTickMarks;
      /** Gets and Sets the style of tick marks for x axis. */
      ShowTickMarkX: ShowTickMarks;
      /** Gets and Sets the fixed grid aspect ratio (width over height). Set to 1.0 to produce a square grid. Set to zero to create a grid at maximum size. */
      GridAspect: number;
      /** This property allows developer to tweak the amount of space between y axis grid numbers and corresponding y axis label. For example, setting to .25 will shrink the gap whereas 1.25 will increase the gap. Default is .75. Units are pecentage of the axis label width. */
      AxisNumberSpacing: number;
      /** This property allows developer to tweak the amount of space between y axis grid numbers and corresponding y axis tick marks. Default is .25, for example, increasing to .5 will widen the gap. Units are pecentage of the axis label width. */
      AxisTickSpacing: number;
      /** This property allows developer to tweak the amount of space between x axis grid numbers/pointlablels and corresponding x axis labels. Default is 0, for example, setting to 0.5 will widen this gap, setting to -0.5 will shrink the gap. */
      AxisNumberSpacingX: number;
      /** This property allows developer to tweak the amount of space between x axis grid numbers/pointlablels and corresponding x axis tick marks. Default is 0.25, for example, setting to 0.5 will widen this gap. */
      AxisTickSpacingX: number;
      /** Gets or Sets whether gdi commands are clipped to an axis by truncating gdi coordinates to axis border. It hides the graphics behind the section between axes so this works best with MultiAxisStyle set to 1. Metafiles do not directly support clipping so this feature attempts to work around this lim... */
      ClipAxesInMetafiles: boolean;
      /** Gets and Sets whether y axis grid lines, numbers, and labels are visible. */
      ShowYAxis: ShowAxis;
      /** Gets or Sets whether y axis minor tick marks extend across chart as major grid lines. */
      YAxisLongTicks: boolean;
      /** Set this property to True to enable event driven left y axis grid number formatting via CustomGridNumber event. Within the CustomGridNumber event, the developer can set the e.NumberString argument which controls the text for the grid line. */
      CustomGridNumbersY: boolean;
      /** Gets and Sets the grid minor tick mark style when Grid.Option.YAxisLongTicks is True. */
      TickStyle: TickStyle;
      /** ProEssentials v4 has new scaling logic that places symbols within the Grid Numbers to help preserve real-estate when displaying large or small numbers. These symbols are defined with this property and default to: "p n u m K M B T ". These symbols are only used if Data.AutoScaleData = True. Note s... */
      ScaleSymbols: string;
    };
    Zoom: {
      /** Gets whether the chart is currently zoomed. */
      Mode: boolean;
      /** Generally read in response to a ZoomIn event. Gets and Sets zoomed min Y value. */
      MinY: number;
      /** Generally read in response to a ZoomIn event. Gets and Sets zoomed max Y value. */
      MaxY: number;
      /** Generally read in response to a ZoomIn event. Gets and Sets zoomed min right Y axis value. */
      MinRY: number;
      /** Generally read in response to a ZoomIn event. Gets and Sets zoomed max right Y axis value. */
      MaxRY: number;
      /** Generally read in response to a ZoomIn event. Gets and Sets zoomed min X value. */
      MinX: number;
      /** Generally read in response to a ZoomIn event. Gets and Sets zoomed max X value. */
      MaxX: number;
      /** Generally read in response to a ZoomIn event when multiple y axes are implemented. Gets and Sets zoomed min axis index. */
      MinAxis: number;
      /** Generally read in response to a ZoomIn event when multiple y axes are implemented. Gets and Sets zoomed max axis index. */
      MaxAxis: number;
    };
    Boundary: {
      /** Gets and Sets visibility of upper and/or lower y axis regions. */
      Types: VBoundaryTypes;
      /** Gets and Sets upper boundary location if Grid.Boundary.Types enables upper boundary. */
      UpperValue: number;
      /** Gets and Sets lower boundary location if Grid.Boundary.Types enables lower boundary. */
      LowerValue: number;
      /** Gets and Sets upper boundary title if Grid.Boundary.Types enables upper boundary. */
      UpperText: string;
      /** Gets and Sets lower boundary title if Grid.Boundary.Types enables lower boundary. */
      LowerText: string;
    };
    Configure: {
      /** Gets and Sets the minimum X data value associated with extent of the GraphBmpFilename when GraphBmpStyle is set to BitBltZooming, enabling backgound bitmap zooming. */
      GraphBmpMinX: number;
      /** Gets and Sets the maximum X data value associated with extent of the GraphBmpFilename when GraphBmpStyle is set to BitBltZooming, enabling backgound bitmap zooming. */
      GraphBmpMaxX: number;
      /** Gets and Sets the minimum Y data value associated with extent of the GraphBmpFilename when GraphBmpStyle is set to BitBltZooming, enabling backgound bitmap zooming. */
      GraphBmpMinY: number;
      /** Gets and Sets the maximum Y data value associated with extent of the GraphBmpFilename when GraphBmpStyle is set to BitBltZooming, enabling backgound bitmap zooming. */
      GraphBmpMaxY: number;
      /** Gets and Sets meaningful frequencies to place point labels and tabled data. If the points being charted is larger than Grid.Configure.AltFreqThreshold then a frequency will be automatically selected to produce a quantity of grid lines closest to Grid.Configure.TargetPointsToTable. */
      AltFrequencies: PeArray1D<number>;
      /** Gets and Sets the quantity of point labels which will be attempted to be displayed when Data.Points and/or UI.Scrollbar.PointsToGraph is greater than or equal to Grid.Configure.AltFreqThreshold. */
      TargetPointsToTable: number;
      /** Gets and Sets the threshold quantity of points graphed which triggers Grid.Configure.AltFrequencies to be utilized in determining an alternate frequency for placing point labels and tabled data. */
      AltFreqThreshold: number;
      /** Gets and Sets the maximum value of y axis when Plot.Method is set to a stacked style plotting method. */
      ManualStackedMaxY: number;
      /** Gets and Sets the minimum value of y axis when Plot.Method is set to a stacked style plotting method. */
      ManualStackedMinY: number;
      /** Only used in a real-time implementation. Sets a point label which will be used to calculate the maximum point label length in determining the size of area for point label placement. */
      ManualMaxPointLabel: string;
      /** Only used in a real-time implementation. Sets a label which will be used to calculate the maximum table cell length in determining the table's font size. */
      ManualMaxDataString: string;
      /** By supplying this property along with .AltFrequencies, the developer has control over the placement of point labels under situations where there are too many points to label every point. See help for more info. */
      FirstPtLabelOffset: number;
      /** Set to True when you want the chart's y axis range to auto scale (ManualScaleControlY = Auto) by also considering the horizontal y axis line annotation data. */
      AutoScaleHorzLineAnnotations: boolean;
      /** Set to True when you want the chart's x axis range to auto scale (ManualScaleControlY = Auto) by also considering the vertical x axis line annotation data. */
      AutoScaleVertLineAnnotations: boolean;
      /** This property represents the percentage added above and below the automatically determined range for Y axis when Grid.Configure.ManualScaleControlY is set to None(automatic). Current state of WorkingAxis controls the respective axis modified. */
      AutoMinMaxPaddingY: number;
      /** This property represents the percentage added above and below the automatically determined range for right Y axis when Grid.Configure.ManualScaleControlRY is set to None(automatic). Current state of WorkingAxis controls the respective axis modified. */
      AutoMinMaxPaddingRY: number;
      /** Setting True allows AutoMinMaxPaddingY to set min-max values which potentially cross zero origin when padding axis with extra space. Current state of WorkingAxis controls the respective axis modified. */
      AutoPadBeyondZeroY: boolean;
      /** Setting True allows AutoMinMaxPaddingRY to set min-max values which potentially cross zero origin when padding axis with extra space. Current state of WorkingAxis controls the respective axis modified. */
      AutoPadBeyondZeroRY: boolean;
      /** Gets and Sets whether the y axis scale is log or linear/normal. */
      YAxisScaleControl: ScaleControl;
      /** Gets and Sets whether the right y axis scale is log or linear/normal. */
      RYAxisScaleControl: ScaleControl;
      /** Gets and Sets whether the right y axis's extents are automatically configured or manually configured via ManualMinRY and ManualMaxRY. */
      ManualScaleControlRY: ManualScaleControl;
      /** Gets and Sets the right y axis minimum value. Setting this value also depends on setting ManualScaleControlRY. */
      ManualMinRY: number;
      /** Gets and Sets the right y axis maximum value. Setting this value also depends on setting ManualScaleControlRY. */
      ManualMaxRY: number;
      /** Gets or Sets whether right y axis grid line frequency is automatically set or manually configured with ManualRYAxisLine and ManualRYAxisTick. */
      ManualRYAxisTicknLine: boolean;
      /** Gets and Sets the right y axis grid line frequency. Setting this value also depends on setting ManualRYAxisTicknLine and ManualRYAxisTick. */
      ManualRYAxisLine: number;
      /** Gets and Sets the right y axis tick frequency. Setting this value also depends on setting ManualRYAxisTicknLine and ManualRYAxisLine. */
      ManualRYAxisTick: number;
      /** If non-zero, this property controls the maximum number of major y axis grid lines attempted when ManualYAxisTicknLine is FALSE. Use this property when you desire fewer grid lines than the auto configuration produces. */
      YAxisLineLimit: number;
      /** If non-zero, this property controls the maximum number of major right y axis grid lines attempted when ManualRYAxisTicknLine is false. Use this property when you desire fewer grid lines than the auto configuration produces. */
      RYAxisLineLimit: number;
      /** If true, right y axis major grid lines will only be located at whole numbers. */
      RYAxisWholeNumbers: boolean;
      /** Set to True when you want the chart's x and y axis ranges to auto scale (ManualScaleControlY, ManualScaleControlX = Auto) by also considering the GraphAnnotatonX and GraphAnnotationY data. */
      AutoScaleGraphAnnotations: boolean;
      /** Gets and Sets whether the y axis's extents are automatically configured or manually configured via ManualMinY and ManualMaxY. */
      ManualScaleControlY: ManualScaleControl;
      /** Gets and Sets the y axis minimum value. Setting this value also depends on setting ManualScaleControlY. */
      ManualMinY: number;
      /** Gets and Sets the y axis maximum value. Setting this value also depends on setting ManualScaleControlY. */
      ManualMaxY: number;
      /** Gets or Sets whether y axis grid line frequency is automatically set or manually configured with ManualYAxisLine and ManualYAxisTick. */
      ManualYAxisTicknLine: boolean;
      /** Gets and Sets the y axis grid line frequency. Setting this value also depends on setting ManualYAxisTicknLine and ManualYAxisTick. */
      ManualYAxisLine: number;
      /** Gets and Sets the y axis tick frequency. Setting this value also depends on setting ManualYAxisTicknLine and ManualYAxisLine. */
      ManualYAxisTick: number;
      /** This property represents the percentage added above and below the automatically determined range for an axis when Grid.Configure.ManualScaleControlY and similar properties are set to None(automatic). This property sets all other similar AutoMinMaxPadding properties. */
      AutoMinMaxPadding: number;
      /** Setting this property to FALSE will remove the limitation that grid line numbers must be multiples of 2, 5, and 10. Normally not needed unless you are using Grid.Configure.ManualYAxisTicknLine or related property and having trouble setting certain grid line and tick frequencies. */
      GridLineMultiples: boolean;
      /** If true, y axis major grid lines will only be located at whole numbers. */
      YAxisWholeNumbers: boolean;
    };
    /** Gets or Sets whether major grid lines get drawn in-front or behind plotting method graphics. */
    InFront: boolean;
    /** Gets or Sets how x axis PointLabels are oriented. */
    ForceVerticalPoints: ForceVerticalPoints;
    /** Gets or Sets the quantity of points that get viewed simultaneously, enabling a horizontal scrollbar to access other points. */
    PointsToGraph: number;
    /** If PointsToGraph is non-zero, Gets or Sets whether the first or last points are initially shown. */
    PointsToGraphInit: PointsToGraphInit;
    /** Read only, Gets the quantity and axis indices for those axes currently visible. Zooming and adjusting PeData.RandomSubsetsToGraph can result in fewer axes being visible. This property allows the developer to learn which axes are visible. */
    WorkingAxesToGraph: PeArray1D<number>;
    /** Gets and Sets individual axis border types. Set WorkingAxis prior to setting or getting this property. */
    AxisBorderType: AxisBorderType;
    /** Gets or Sets whether Y and/or X axis major grid lines get drawn through chart. */
    LineControl: GridLineControl;
    /** Gets or Sets style of line used for Y and/or X axis major grid lines. */
    Style: GridStyle;
    /** When PeConfigure.BorderTypes is set to NoBorders, this property controls if axes still include a border line. */
    AxisBorders: boolean;
  };
  PePlot: {
    Bitmaps: {
      /** Gets and Sets the current bitmap zero based index while defining and accessing related Bitmap properties. ProEssentials supports up to 150 bitmaps to be used as Subset and GraphAnnotation symbols. */
      WorkingBitmap: number;
      /** Gets or Sets the filename used with WorkingBitmap in allocating and addressing bitmap resources used for subset or annotation symbols. /// */
      Filename: string;
      /** Gets or Sets the working bitmap style, controlling size and positioning. When set to DataSized, then BmpMinX, BmpMaxX, BmpMinY and BmpMaxY will control position and size of bitmap in data units (bitmap will zoom if chart is zoomed). */
      Style: ResourceBitmapStyle;
      /** Gets and Sets the 0 to 100 opacity or translucency of the working bitmap. 100 = full opacity. 0 = transparent. */
      Opacity: number;
      /** Gets or Sets the working bitmaps use of alpha channel. */
      IgnoreAlpha: ResourceBitmapStyle;
      /** Get and Sets if the working bitmap should be dyanimically colored based on SubsetColor and or GraphAnnotationColor. This will enable one bitmap to be used with multiple subsets and or graph annoations by dynamically changing its color. The IgnoreAlpha property will control use of alpha channel in... */
      Colorize: boolean;
      /** Gets or Sets the mode of colorizing the bitmap resource. None, Mask (simple replace color), Tint (Preserve white-gray highlights). */
      ColorizeMode: ResourceBitmapColorizeMode;
      /** When using a bitmap with Style = DataSized, this property controls the min x position. */
      BmpMinX: number;
      /** When using a bitmap with Style = DataSized, this property controls the max x position. */
      BmpMaxX: number;
      /** When using a bitmap with Style = DataSized, this property controls the min x position. */
      BmpMinY: number;
      /** When using a bitmap with Style = DataSized, this property controls the max x position. */
      BmpMaxY: number;
    };
    Option: {
      /** Set True to enable additional logic to offset bars across multiple axes within an overlapped multi axes group. Only applicable when used with PeGrid.OverlapMultiAxes to group multiple axes, and when PePlot.Methods[] is used to define plotting methods. This should normally be set True when using B... */
      OverlapMultiAxesOffsetBars: boolean;
      /** Set True to enable an optional method of drawing multiple subset bars overlapping instead of side-by-side. */
      OverlapBars: boolean;
      /** Setting this property to True and passing Data.Z for each point index will cause a stacked bar chart to float on-top of the provided Z data values. */
      FloatingStackedBars: boolean;
      /** This property controls the padding to the left and right of the first and last points when using a point or line plotting method. This property defines the padding in terms of percentage of space between data points. The valid range is from 0.0 to 2.0 for 0% to 200% of the space between data poin... */
      PointPadding: number;
      /** This property controls the padding to the left and right of the first and last points when using an area plotting method. This property defines the padding in terms of percentage of space between data points. The valid range is from 0.0 to 2.0 for 0% to 200% of the space between data points. Defa... */
      PointPaddingArea: number;
      /** This property controls the padding to the left and right of the first and last points when using a bar plotting method. This property defines the padding in terms of percentage of space between data points. The valid range is from 0.0 to 2.0 for 0% to 200% of the space between data points. Set to... */
      PointPaddingBar: number;
      /** Similar to floating bars, as in demo example 025. This feature enables the Stick Plotting method to function as a floating stick plotting method. Using ZData to control lower stick value and ZData being considered in auto scaling logic. SubsetLineTypes can be used to control thickness of sticks. */
      FloatingSticks: boolean;
      /** Gets or Sets the spline type when RenderEngine is GdiPlus or Direct2D. Default 0 is cardinal based of DrawCurve call. Set to b-spline to not use the DrawCurve api. */
      SplineType: SplineType;
      /** Gets and Sets gradient for all solid points for PePlot.Method showing points. Type ProEssentials.Enums.PlotGradientStyle. */
      PointGradientStyle: PlotGradientStyle;
      /** Gets or Sets color and control of border line around solid points when PePlot.Method is set to include points. Default is argb 0 for no borderground, set to Color.Empty for border color to match brush, else set to any color as needed. */
      PointBorderColor: number;
      /** Gets and Sets thickness for all non solid points for PePlot.Method showing points. Default 0=thinline=1pixel for display but it's actually a logical unit that scales, 1=1pixel, 3 and higer = multiples of a thin line unit. */
      LineSymbolThickness: number;
      /** Gets and Sets if a polyline is added to GdiPlus Area PePlot.Method graphics. Helps with large data sets where spikes in data results in a zero-width polygon spike that GdiPlus can hide. Value can be 1, 2, 3 pixel units for display and print, svg scales. */
      AreaBorder: number;
      /** Gets and Sets if a polyline is added on top Area plotting methods. These lines are drawn after all areas so they are always on top. Similar to HiddenLinesInArea but solid. Value can be 1, 2, 3 pixel units for display and print, svg scales. */
      SolidLineOverArea: number;
      /** Gets and Sets per subset gradient style. Type ProEssentials.Enums.PlotGradientStyle. */
      SubsetGradientStyle: PeArray1D<number>;
      /** Gets and Sets per subset gradient style boundary. Set to items included within ProEssentials.Enums.GradientBoundary. */
      SubsetGradientBoundary: PeArray1D<number>;
      /** Gets or Sets the bevel style for area plotting methods. */
      AreaBevelStyle: BevelStyle;
      /** Gets or Sets the gradient style for area plotting methods. */
      AreaGradientStyle: PlotGradientStyle;
      /** Gets or Sets the bevel style for area stacked plotting methods. */
      AreaStackedBevelStyle: BevelStyle;
      /** Gets or Sets the gradient style for area stacked plotting methods. */
      AreaStackedGradientStyle: PlotGradientStyle;
      /** Gets or Sets the bevel style for bar plotting methods. Set with Enums.BevelStyle */
      BarBevelStyle: BevelStyle;
      /** Gets or Sets the gradient style for bar plotting methods. */
      BarGradientStyle: PlotGradientStyle;
      /** Gets or Sets the bevel style for spline plotting methods. */
      SplineBevelStyle: SplineBevelStyle;
      /** Gets or Sets the gradient style for spline plotting methods. */
      SplineGradientStyle: PlotGradientStyle;
      /** Gets or Sets the gradient style for candlestick and high-low bar plotting methods. */
      SpecificPlotModeGradient: SpecificPlotModeGradient;
      /** Gets or Sets the boundary of gradient for area plotting methods. */
      AreaGradientBoundary: GradientBoundary;
      /** Gets or Sets the boundary of gradient for area stacked plotting methods. */
      AreaStackedGradientBoundary: GradientBoundary;
      /** Gets or Sets the boundary of gradient for bar plotting methods. */
      BarGradientBoundary: GradientBoundary;
      /** Gets or Sets the boundary of gradient for spline plotting methods. */
      SplineGradientBoundary: GradientBoundary;
      /** Gets or Sets the bevel lighting style for area plotting methods if AreaBevelStyle is non zero. */
      AreaBevelLighting: BevelLighting;
      /** Gets or Sets the bevel lighting style for area stacked plotting methods if AreaStackedBevelStyle is non zero. */
      AreaStackedBevelLighting: BevelLighting;
      /** Gets or Sets the bevel lighting style for bar plotting methods if BarBevelStyle is non zero. */
      BarBevelLighting: BevelLighting;
      /** Gets or Sets the bevel lighting style for spline plotting methods if SplineBevelStyle is non zero. */
      SplineBevelLighting: BevelLighting;
      /** Related to area, bar, spline bevels, defines the lower threshold where bevel is no longer drawn if attempted width is less than defined, default = 3 pixels */
      BevelLimit: number;
      /** Default 4 pixels, with no gradient, if candle/high-low-bar is greater than this width, a border is drawn on candle portion. */
      SpecificPlotModeBorder: number;
      /** Set true for bars to have an additional glass effect. */
      BarGlassEffect: boolean;
      /** Set True to force bar charts to draw bars with no gaps between bars. We mostly added this feature to draw colormap type charts. Colormaps can be built with a stacked bar chart with many subsets, points and AdjoinBars = True. */
      AdjoinBars: boolean;
      /** Read only, Gets best fit coefficients when Plot.Method is set to PointsPlusBestFitLine or PointsPlusBestFitCurve. The degree of the coefficients are also controlled by Plot.Option.BestFitDegree or Plot.Option.SubsetDegree. See help for more info. */
      BestFitCoeffs: PeArray2D<number>;
      /** Gets and Sets best fit degree per subset index. Set with ProEssentials.Enums.BestFitDegree. See also Plot.Option.BestFitDegree. */
      SubsetDegree: PeArray1D<number>;
      /** Gets and Sets whether comparison subsets are plotted via Plot.Method or Plot.ComparisonMethod. */
      TreatCompsAsNormal: boolean;
      /** Gets and Sets whether negative Bar, Area, and Stick plotting method graphics get drawn from the x axis or from bottom of chart. */
      NegativeFromXAxis: boolean;
      /** Gets or Sets the best fit degree when Plot.Method is set to PointsPlusBestFitCurve. See also SubsetDegree. */
      BestFitDegree: BestFitDegree;
      /** Gets or Sets the curve granularity for plotting methods that interpolate between data points. */
      CurveGranularity: CurveGranularity;
      /** Gets or Sets the threshold distance between any adjacent non-null data points which causes a gap in the line. */
      LineGapThreshold: number;
      /** Gets and Sets whether line graphs will have gaps (broken lines) where null data is located. Null data is defined by the Data.NullDataValue and Data.NullDataValueX properties. */
      NullDataGaps: boolean;
      /** Gets and Sets whether area graphs will have gaps (broken areas) where null data is located. Null data is defined by the Data.NullDataValue and Data.NullDataValueX properties. */
      NullDataGapsArea: boolean;
      /** Gets and Sets the bar/candlestick width when Plot.Method is set to bar or specificplotmode. See also OhlcMinWidth and FixedSpmWidth. */
      BarWidth: number;
      /** Gets and Sets whether dashed lines show hidden area plots behind other area plots. */
      HiddenLinesInArea: boolean;
      /** Gets and Sets whether the specific plot mode plotting methods will be drawn with multiple colors. The colors will be controlled via Color.SubsetColors. See Help for more info. */
      SpecificPlotModeColor: boolean;
      /** This property enables and controls the size of the minimum point size. This property is helpful when using small charts and you want to maintain a minimum point size. Sizes are 2,4,6,8,10 pixels. */
      MinimumPointSize: MinimumPointSize;
      /** This property enables and controls the size of the maximum point size. This property is helpful when using large charts and you want to maintain a maximum point size. Sizes are 2,4,6,8,10 pixels. */
      MaximumPointSize: MinimumPointSize;
      /** When using MarkDataPoints, this property controls the size of the maximum markert size. Default is enabled, So new v7 has smaller marker sizes for large charts. */
      MaxMarkerSize: MaxMarkerSize;
      /** If Plot.Methods() is being used, then by default, plotting methods will automatically be sorted. Sorting results in plotting methods being drawn in this order: Area, Bar, OHLC, Point, and Line. Set this property to FALSE to disable sorting and draw plotting methods in the order of their subset in... */
      SortPlotMethods: boolean;
      /** Gets and Sets whether width of OHLC and CandleStick graphics are independent of image size. Used when Plot.Method is set to SpecificPlotMode. */
      FixedSpmWidth: boolean;
      /** Gets and Sets the minimum width of the open and close tick widths and candle-stick widths. Units are purely logical. Default is 16, range = 1 to 30. */
      OhlcMinWidth: number;
      /** Gets and Sets whether lines can include a drop-shadow effect if Plot.DataShadows is non-zero. /// */
      LineShadows: boolean;
      /** Controls if Bars and CandleSticks include a gradient effect. Units are in pixels, the recommended setting is 8 pixels in which case bars at least 8 pixels wide will include gradient. */
      GradientBars: number;
      /** If one element is defined, controls all point sizes by this amount, else, each subset index can be assigned a point size factor. For example, setting to 2.0 will produce point sizes twice as large. */
      SubsetPointSizes: PeArray1D<number>;
      /** Gets or Sets the frequency of symbols placed into plotting methods when Color.ViewingStyle = MonoWithSymbols. */
      SymbolFrequency: SymbolFrequency;
      /** Gets and Sets whether line thickness is independent of image size. This property works with solid line types produced when Plot.Method is set to Line. */
      FixedLineThickness: boolean;
      /** Gets and Sets whether plotting method graphic commands are clipped to within the axis in which they reside. Set this property to False to disable clipping. Manually scaling and zooming can both cause plotting graphics to extend outside its axis. */
      Clipping: boolean;
      /** This property controls how triangle data symbols are located. If True, triangles are located so their vertex is at the y data value. This allows up-triangle and down-triangle symbols to work as pointers and point to the location defined by the y data value. If False, symbols are centered at the y... */
      TriangleAnnotationAdj: boolean;
      /** Gets and Sets how PeColor.PointColors colors individual line segments. Set to True to have lines colored leading up to the data point, else, the line segmnet is colored following the data point. */
      PointColorLineAdj: boolean;
    };
    Allow: {
      /** Gets and Sets whether stacked style plotting methods will be supported. If so, per point totals will be gathered. */
      StackedData: boolean;
      /** Gets and Sets whether stacked percent style plotting methods will be supported. */
      StackedPercentMenus: boolean;
      /** Gets and Sets whether user has ability of selecting the Histogram plotting method within the customization dialog and popup menu. */
      Histogram: boolean;
      /** Gets and Sets whether user has ability of selecting the Horizontal Bar plotting method within the customization dialog and popup menu. */
      HorziontalBar: boolean;
      /** Gets and Sets whether user has ability of selecting the BestFitCurveGraphed plotting methods within the customization dialog and popup menu. */
      BestFitCurveGraphed: boolean;
      /** Gets and Sets whether user has ability of selecting the BestFitLineGraphed plotting methods within the customization dialog and popup menu. */
      BestFitLineGraphed: boolean;
      /** Gets and Sets whether user has ability of selecting the horizontal stacked bar plotting method within the customization dialog and popup menu. */
      HorzBarStacked: boolean;
      /** Gets and Sets whether user has ability of selecting the BestFitCurve plotting methods within the customization dialog and popup menu. */
      BestFitCurve: boolean;
      /** Gets and Sets whether user has ability of selecting the Spline plotting method within the customization dialog and popup menu. */
      Spline: boolean;
      /** Gets and Sets whether user has ability of selecting the Spline Area plotting method within the customization dialog and popup menu. */
      SplineArea: boolean;
      /** Gets and Sets whether user has ability of selecting the Spline Ribbon plotting method within the customization dialog and popup menu. */
      SplineRibbon: boolean;
      /** Gets and Sets whether user has ability of selecting the Bar plotting method within the customization dialog and popup menu. */
      Bar: boolean;
      /** Gets and Sets whether user has ability of selecting the Area plotting method within the customization dialog and popup menu. */
      Area: boolean;
      /** Gets and Sets whether user has ability of selecting the BestFitLine plotting methods within the customization dialog and popup menu. */
      BestFitLine: boolean;
      /** Gets and Sets whether user has ability of selecting the PointsPlusSpline plotting method within the customization dialog and popup menu. */
      PointsPlusSpline: boolean;
      /** Gets and Sets whether user has ability of selecting the Step plotting method within the customization dialog and popup menu. */
      Step: boolean;
      /** Gets and Sets whether user has ability of selecting the Ribbon plotting method within the customization dialog and popup menu. */
      Ribbon: boolean;
      /** Gets and Sets whether user has ability of selecting the Line plotting method within the customization dialog and popup menu. */
      Line: boolean;
      /** Gets and Sets whether user has ability of selecting the Point/Scatter plotting method within the customization dialog and popup menu. */
      Point: boolean;
      /** Gets and Sets whether user has ability of selecting the PointsPlusLine plotting method within the customization dialog and popup menu. */
      PointsPlusLine: boolean;
    };
    /** Defines automatic statistical subsets to be appended to control. Use enumeration Enums.AutoStatSubsets. */
    AutoStatSubsets: PeArray1D<number>;
    /** Gets and Sets the hatch type associated with zero based subset indices. Hatching is possible with bars, areas, and pie chart slices. The color of hatch foreground is controlled with Color.SubsetColors. The hatch background color is controlled with Color.HatchBackColor. */
    SubsetHatch: PeArray1D<number>;
    /** Gets and Sets the hatch type associated with zero based subset and point index. Used to control hatching on a per Bar basis. The color of hatch foreground is controlled with Color.PointColors. The hatch background color is controlled with Color.HatchBackColor. */
    PointHatch: PeArray2D<number>;
    /** Gets and Sets the plotting style associated with zero based subset indices. Used to control plotting style on a per subset basis. */
    Methods: PeArray1D<number>;
    /** Gets or Sets whether shadows or 3D effects are used on bars, points, and other graphics. */
    DataShadows: DataShadows;
    /** Gets or Sets whether 3D bars have drop shadows when DataShadows = 3D. */
    Show3dShadows: boolean;
    /** Gets or Sets the multi subset style (OpenHighLowClose, CandleStick, HighLowBar) when Plot.Method is set to SpecificPlotMode. */
    SpecificPlotMode: SpecificPlotMode;
    /** Gets or Sets whether datapoints are annotated with a small dot. */
    MarkDataPoints: boolean;
    /** Gets and Sets the quantity of subsets at end of subset range to be plotted in a secondary plotting method with respect to right y axis. Used to mix plotting methods such as bars and lines. See help for more info. */
    RYAxisComparisonSubsets: number;
    /** Gets or Sets the style of chart, Line, Bar, Area, Point, etc. */
    Method: GraphPlottingMethod;
    /** Gets or Sets the comparison style of chart, Line, Point, etc. produced from the use of ComparisonSubsets or RYAxisComparisonSubsets. */
    MethodII: GraphPlottingMethodII;
    ZoomWindow: {
      /** Gets and Sets whether the ZoomWindow is visible. */
      Show: boolean;
      /** True or False if individual subsets should be visible in ZoomWindow. If this array is empty, all subsets display in Zoomwindow. */
      SubsetsToShow: PeArray1D<boolean>;
      /** Set with enum ZoomWindowPlottingMethods, controls the plotting style of individual subsets within the zoom window. If empty, subsets are plotted in their original plotting method. */
      PlottingMethods: PeArray1D<number>;
      /** Set this property to True to enable event driven zoom window x axis grid number formatting via CustomGridNumber event. Within the CustomGridNumber event, the developer can set the e.NumberString argument which controls the text for the grid line. */
      CustomGridNumbersX: boolean;
      /** Gets or Sets whether user has ability of clicking zoom window x axis label text. */
      LabelHotSpots: boolean;
      /** Gets and Sets whether the ZoomWindow x axis is visible. */
      ShowXAxis: boolean;
      /** Gets and Sets adjustment to zoom window font sizes used in image. For enlarged fonts, increase from 1.0 to any value up to 1.5. For reduced font sizes, decrease from 1.0 to 0.5. */
      FontSizeCntl: number;
      /** Gets and Sets height of zoomwindow as a proportion of total height. Default=.12 or 12%. */
      Height: number;
      /** Gets or Sets whether zoom window uses specific coloring or adobts chart GraphForeColor, GraphBackColor and/or bitmaps gradients. */
      CustomColors: boolean;
      /** Gets or sets color of border, grid lines, and text within zoom window. ZoomWindowCustomColors must be True to enable this setting. /// */
      ForeColor: number;
      /** Gets or sets color of background within zoom window. ZoomWindowCustomColors must be True to enable this setting. /// */
      BackColor: number;
      /** Gets or Sets the style of zoom window gradient. Zoom Window gradients are enabled only if ZoomWindowCustomColors is True, PeColor.BitmapGradientMode is True, and ZoomWindowBackColor = Empty. */
      GradientStyle: GradientStyle;
      /** Gets or sets start color of background gradient within zoom window. ZoomWindowCustomColors must be True and GradientStyle non zero to enable this setting. /// */
      GradientStart: number;
      /** Gets or sets end color of background gradient within zoom window. ZoomWindowCustomColors must be True and GradientStyle non zero to enable this setting. /// */
      GradientEnd: number;
      /** Gets or Sets the style of zoom window gradient. Zoom Window gradients are enabled only if ZoomWindowCustomColors is True, PeColor.BitmapGradientMode is True, and ZoomWindowBackColor = Empty. */
      BmpStyle: BitmapStyle;
      /** Gets or Sets the opacity (0 to 100) for the ZoomWindowBmpFilename bitmap. Set ZoomWindowGradientStart, ZoomWindowGradientEnd, and ZoomWindowGradientStyle to combine a background color with background bitmap. */
      BmpOpacity: number;
      /** Gets or Sets filename used for the zoom window background bitmap. Bitmaps are enabled only if PeColor.BitmapGradientMode is True, ZoomWindow.CustomColors is True, ZoomWindow.BackColor = Empty, and ZoomWindow.BmpStyle is non-zero. */
      BmpFilename: string;
      /** Gets or Sets whether the zoom window will include data hot spots. */
      DataHotSpots: boolean;
      /** Gets or Sets whether annotations show within the zoom window. */
      ShowAnnotations: boolean;
      /** Gets or Sets whether data shadows draw behind plotting methods within the zoom window. */
      DataShadows: boolean;
      /** Gets or Sets whether data points are highlighed with a dot within the zoom window. */
      MarkDataPoints: boolean;
      /** Gets or Sets whether zoom window is drawn in exported images or during printing. */
      InExports: boolean;
      /** Gets and Sets the zoom window shadow depth that denotes the active charting view. Default=100. Range 0 to 255. Larger numbers darken shadow. Only enabled when RenderEngine=GdiPlus. */
      ShadowLevel: number;
      /** Gets or Sets the style of zoom window border. Default is BorderType where if any type of border exist for chart, a thin line will surround zoom window. Border=force line border. NoBorder=never show a border. Uses Enums.ZoomWindowBorder */
      Border: ZoomWindowBorder;
      /** Read only, gets whether zoom window is currently shown. */
      Showing: boolean;
    };
    /** Gets and Sets the point styles associated with zero based subset indices. */
    SubsetPointTypes: PeArray1D<number>;
    /** Gets and Sets the line styles associated with zero based subset indices. RenderEngine = Direct3D has limits: Pesgo Direct3d only thin med thick solid lines unless ComputeShader = True which allows for dash and dot thin medium thick. Pe3do Direct3D, PolyMode = Scatter, only thin medium thick solid... */
    SubsetLineTypes: PeArray1D<number>;
    /** Gets and Sets the color associated with zero based subset indices. */
    SubsetColors: PeArray1D<number>;
    /** Gets and Sets data point types on a per-point basis. To change point types per subset use Plot.SubsetPointTypes. To control per subset use of this property, set Plot.SubsetForPointTypes. */
    PointTypes: PeArray2D<number>;
    /** Gets or Sets color on a per data point basis. To change point color per subset, use Plot.SubsetColors. If you use this property, you need to define color for all points unless Plot.SubsetForPointColors is used to designated which subsets use this property and which use the default Plot.SubsetColo... */
    PointColors: PeArray2D<number>;
    /** optionally control how PointColors property applies to Points and or Lines when PlottingMethod is PointsPlusLine 0 = Color the Points, default legacy behavior, 1 = Color the Line segments, 2 = Color the Points and the Line segments */
    PointColorPoints: number;
    /** This property will determine which subset indices will use Plot.PointTypes and which will use the default type defined with the Plot.SubsetPointTypes property. */
    SubsetForPointTypes: PeArray1D<number>;
    /** This property will determine which subset indices will use Plot.PointColors and which will use the default color defined with the Plot.SubsetColors property. */
    SubsetForPointColors: PeArray1D<number>;
    /** Gets or Sets the point size when Plot.Method is set to include point graphics. See also, Plot.Option.MinimumPointSize */
    PointSize: PointSize;
    /** Gets and Sets the quantity of subsets at end of subset range to be plotted in a secondary plotting method. Used to mix plotting methods such as bars and lines. See help for more info. */
    ComparisonSubsets: number;
  };
  PeConfigure: {
    /** When RenderingEngine = Direct3D, used to control how Direct2D and Direct3D are combined. */
    Composite2D3D: Composite2D3D;
    /** Set to override the operating system's current decimal separator symbol. Set to any decimal ascii character as needed. */
    Decimal: number;
    /** Set to override the operating system's current thousands separator symbol. Set to any decimal ascii character as needed. */
    Thousands: number;
    /** Gets or Sets whether image is prepared in memory and bit blitted to screen for smooth drawing. */
    PrepareImages: boolean;
    /** Gets or Sets whether image prepared in memory via Configure.PrepareImages is retained in memory allowing faster redraws. */
    CacheBmp: boolean;
    /** Gets or Sets whether a double buffer is implemented to support PeUserInterface.Cursor.DrawCursorToCache (CursorColor and dashed cursors styles) and ShowingQuickAnnotations feature. See Demo example 110 and 105 */
    CacheBmp2: boolean;
    /** This property adds or removes space from the left of the graph. Units are logical and are 1/100th of the size of the y axis width. For example, a value of 100 adds the distance equal to the y axis label width, 200 adds twice the distance of the y axis label width. */
    ImageAdjustLeft: number;
    /** This property adds or removes space from the right of the graph. Units are logical and are 1/100th of the size of the y axis width. For example, a value of 100 adds the distance equal to the y axis label width, 200 adds twice the distance of the y axis label width. */
    ImageAdjustRight: number;
    /** This property adds or removes space from the top of the graph. Units are logical and are 1/100th of the size of the x axis height. For example, a value of 100 adds the distance equal to the x axis label height, 200 adds twice the distance of the x axis label height. */
    ImageAdjustTop: number;
    /** This property adds or removes space from the bottom of the graph. Units are logical and are 1/100th of the size of the x axis height. For example, a value of 100 adds the distance equal to the x axis label height, 200 adds twice the distance of the x axis label height. */
    ImageAdjustBottom: number;
    /** Gets and Sets the width of a solid line border enclosing chart. 0 disables border. Set to 1-15 pixels. */
    ChartBorderWidth: number;
    /** Gets or Sets color of border if ChartBorderWidth is non-zero. */
    ChartBorderColor: number;
    /** Gets and Sets the border style for graph and table bounding boxes. */
    BorderTypes: TABorder;
    /** Gets and Sets whether text will include a drop-shadow effect. */
    TextShadows: TextShadows;
    /** If false, dark text is never shadowed. If true, dark text is drawn with a 3D inset effect rather than shadowed. */
    DarkTextInset: boolean;
    /** Gets and Sets which rendering logic is invoked. GDI is faster but does not support anti-aliasing or alphablending. Hybrid mode can AntiAlias only Text. */
    RenderEngine: RenderEngine;
    /** If true, anti-alias text. Only valid if RenderingEngine = Hybrid or GdiPlus. */
    AntiAliasText: boolean;
    /** Gets and Sets the algorithm used in anti aliasing text. */
    TextRenderingHint: TextRenderingHint;
    /** Gets and Sets if the GdiPlus anti alias mode is half or none. True = Half = HighQuality. Color edges are usually lighter. */
    PixelOffsetHalf: boolean;
    /** If true, anti-alias graphics (lines, symbols, polygons). Only valid if RenderingEngine = GdiPlus. */
    AntiAliasGraphics: boolean;
    /** Gets or Sets whether shadows are translucent when RenderEngine is set to GdiPlus. */
    DataShadowsTranslucent: boolean;
  };
  PeSpecial: {
    /** If the number of points in an image is beyond the threshold defined by this property, then 3D bar and 3D area graphs will be drawn in 2D. This prevents overly complex images from being generated. */
    ThreeDThreshold: number;
    /** For Real-Time implementation only. Normally calling PEvset with property PEP_faAPPENDYDATA will cause the image to be automatically updated. Setting this property to TRUE will disable the automatic update and the programmer will have to explicitly call PEreinitialize, PEresetimage and InvalidateR... */
    AppendWithNoUpdate: boolean;
    /** This property controls a change in the Least-Square Approximation calculation. When set to TRUE, the minimum x value is first subtracted from each x data value before processed. This improves accuracy when XData is very large. This does not effect the chart visually, but will alter the coefficien... */
    BestFitFix: boolean;
    /** When RenderEngine is set to Direct3D, Set this feature to reduce or disable the default setting of 8 multi samples. In rare cases one may want to reduce to 4 or 0 for performance or graphic system reasons. */
    DxMSAA: MultiSampleAntiAlias;
    /** Generally not needed, but just in case. You might set to false to notify that the chart is clean and avoid image reconstruction on next paint. Set this property true to notify the chart is dirty and force image reconstruction on next paint. */
    NeedToInitialize: boolean;
    /** Default is empty string, allows setting the ?XML tag string content at start of SVG xml. End string with question-mark greater sign. */
    SvgXmlParms: string;
    /** Default is empty string, allows setting the starting svg XMLNS parms. If set, start with less-than svg, include width, height, viewbox to match your export. End string with greater sign. */
    SvgXmlnsParms: string;
    /** Default 0 for default .1 to 10 aspect ratio export limit. Set to 2, 3 to relax the shape limit when exporting images. 2=20, 3=30 */
    ExportShapeLimit: number;
    /** Set to override minimum control size rendering potential. Default 0 equals 75 pixel minimum width or height. Set lower if needed. */
    MinimumSize: number;
    /** Read Only, Gets the internal type (Graph, SGraph, Polar, Pie, 3D) of the ProEssentials object. */
    ObjectType: ObjectType;
    /** Gets or Sets whether image is produced via the MM_ANISOTROPIC mapping mode. Probably never needed within .NET but who knows. We added for ActiveXs to work within Access reports. */
    AnisotropicSupport: boolean;
    /** Gets or Sets whether image is constructed with custom or original parameter set. See help for more info. */
    Custom: boolean;
    /** Gets whether control supports a custom and original parameter set. See help for more info. */
    CustomParms: boolean;
    /** Gets whether control is in the process of painting image to screen. */
    Painting: boolean;
    /** Gets whether control is in the process of scrolling image. */
    ActivelyScrolling: boolean;
    /** Gets or Sets the maximum number of hot spots possible. */
    DataHotSpotLimit: number;
    /** Default = 3300, represents likely max monitor size, triggers different logic if size of image is larger than this threshold, it assumes image is very high resolution. */
    PixelSizingThreshold: number;
    /** When exporting metafiles from an object that has been zoomed, lines may be drawn outside the metafile s region. Setting this property to zero will prevent this from happening. */
    LogicalLimit: number;
    /** Default value is '|' or char 124, the pipe symbol. You'll only need to adjust if you need to use some other character as a delimiter. ProEssentials uses this delimeter in various features. Titles use delimiter to separate into left,centered, and right justified portions. Annotation Text use delim... */
    Delimiter: number;
    /** Gets or Sets whether image construction should be halted. Generally set this property from a secondary thread so the primary thread stops the image construction process. */
    Stop: boolean;
    /** This property controls whether ProEssentials will automatically call Function.Reinitialize if properties were adjusted and then an image is requested. This property should only be disabled in certain real-time implementation. This allows new data to be set without having to reinitialize the object. */
    AutoImageReset: boolean;
    /** Gets or Sets the target Dots-Per-Inch DPI width for exported metafiles. When using the FixedFonts feature, it is recommended to set this property to 600 if export could possibly be printed, or 96 if the export will only be viewed on a monitor. */
    DpiX: number;
    /** Gets or Sets the target Dots-Per-Inch DPI height for exported metafiles. When using the FixedFonts feature, it is recommended to set this property to 600 if export could possibly be printed, or 96 if the export will only be viewed on a monitor. */
    DpiY: number;
    /** Gets or Sets whether ui souce code is being used. */
    UsingUiSourceCode: boolean;
  };
  PeData: {
    /** Gets and Sets if data can potentially be optimized. Pego data is always seqential but this setting can be used to disable filtering. */
    SequentialDataX: SequentialData;
    /** Gets or Sets strings used to identify points. */
    PointLabels: PeArray1D<string>;
    /** This property enables DateTimeMode. This mode treats X axis data as serially formatted date/time data. See help for more info. */
    DateTimeMode: boolean;
    /** Used with Data.DateTimeMode = true. This property defines the distance in time between each data point. DeltaX greater than 0 for time in minutes. DeltaX = 1 for daily data, DeltaX = 2 for weekly data, DeltaX = 3 for monthly data. */
    DeltaX: number;
    /** Used with Data.DateTimeMode = true. This property defines the number of data points per day. If Data.DeltaX is less than zero than this property is not used. See help for more info. */
    DeltasPerDay: number;
    /** This property sets the starting date and daily start time when used in conjunction with Data.DateTimeMode, Data.AutoXData, Data.DeltaX, and Data.DeltasPerDay. It is important that this value include the date value along with time. */
    StartTime: number;
    /** Used with Data.DateTimeMode = true. This property controls whether ProEssentials includes Saturdays and Sundays in automatically generated Data.X or Data.Xii. */
    AutoXData: AutoXData;
    /** If Data.DateTimeMode is True, and data is exported via export dialog, this property controls whether seconds are included in time export. */
    DateTimeShowSeconds: boolean;
    /** Gets and Sets speedboost value. Default is 3. Valid range is 1 to 100 but best results are produced with values less than 30. This feature will attempt to reduce GDI LineTo calls by eliminating calls where the line only moves slightly from its previous position. */
    SpeedBoost: number;
    /** You must set Data.Subsets and Data.Points before passing this data. Gets or Sets single precision data plotted with respect to y axis. */
    Y: PeArray2D<number>;
    /** You must set Data.Subsets and Data.Points before passing this data. Gets or Sets single precision data plotted with respect to y axis. */
    X: PeArray2D<number>;
    /** Set to true to enable JaggedData where Subsets have different amounts of data points (Points property). By default False and all Subsets have the same number of Points. If used, set this property before setting other data properties. */
    JaggedData: boolean;
    /** Set true to enable CircularBuffers. Likely not necessary when Points is less than 200,000. Related to real-time partial updating appendYData features. Slightly complicates spoon access to data, but eliminates large block copy/shifting logic when appending new data. As ProEssentials gets better at... */
    CircularBuffers: boolean;
    /** Used when RenderEngine = Direct3D and DynamicBuffers = True. Defines quantity of points to repass to video memory, starting at index RealTimeStartingIndex. */
    RealTimeQuantity: number;
    /** Used when RenderEngine = Direct3D and DynamicBuffers = True. Defines starting point index where data is repassed to video memory, amount of data is defined with RealTimeQuantity. All Subsets will be updated with same amount of data at same starting index. */
    RealTimeStartingIndex: number;
    /** Set to true when RenderEngine = Direct3D and a real-time implementation will need to incrementally change data. Used with RealTimeStartingIndex, RealTimeQuantity. */
    DynamicBuffers: boolean;
    /** Gets and Sets data reduction strength. Default=Quality. Setting to fastest setting could possibly degrade for scatter type charts. */
    Filter2D: Filter2D;
    /** You must set Data.Subsets and Data.Points before passing this data. Gets or Sets single precision data used with floating bar charts. */
    Z: PeArray2D<number>;
    /** Gets and Sets whether large/small data will be displayed with a scale factor. For example, a grid line value of 10,000,000 may be displayed as 10M if this property is true. M representing millions. */
    AutoScaleData: boolean;
    /** You must set Data.UsingYDataII=true, Data.Subsets and Data.Points before pass this data. Gets or Sets double precision data plotted with respect to y axis. */
    Yii: PeArray2D<number>;
    /** You must set Data.UsingXDataII=true, Data.Subsets and Data.Points before pass this data. Gets or Sets double precision data plotted with respect to x axis. Pego object only uses xdata for DateTime handling. */
    Xii: PeArray2D<number>;
    /** Gets or Sets subsets to be included in image. Fill this array with zero based subset indices of those subsets you wish to include in the image. */
    RandomSubsetsToGraph: PeArray1D<number>;
    /** Gets or Sets points to be included in image. Fill this array with zero based point indices of those points you wish to include in the image. */
    RandomPointsToGraph: PeArray1D<number>;
    /** Gets the current scale factor as a power of 10 for the Y Axis. Only used if Data.AutoScaleData = True. */
    ScaleForYData: number;
    /** Gets the current scale factor as a power of 10 for the Right Y Axis. Only used if Data.AutoScaleData = True. */
    ScaleForRYData: number;
    /** Gets and Sets the YData value considered null and subsequently ignored. */
    NullDataValue: number;
    /** Only used in a Real-Time implementation. Gets or Sets whether data is appended to end or beginning of memory. See also: PEP_faAPPENDYDATA. */
    AppendToEnd: boolean;
    /** If setting this property, set it before passing data. Gets and Sets whether single or double precision data is utilized. Set to true to use double precision (Data.Yii) instead of (Data.Y). */
    UsingYDataii: boolean;
    /** If setting this property, set it before passing data. Gets and Sets whether single or double precision data is utilized. Set to true to use double precision (Data.Xii) instead of (Data.X). */
    UsingXDataii: boolean;
    /** Gets or Sets whether subsets will be included in image. Similar to RandomSubsetsToGraph, this is a newer property array that makes it easier to select which subsets are visible. It also has an optional ability to control the order of how subsets are drawn. Example 033 in the demo shows the use of... */
    SubsetsToShow: PeArray1D<number>;
    /** If setting this property, set it before passing data. Gets and Sets whether single or double precision data is utilized. Set to true to use double precision (Data.Zii) instead of (Data.Z). */
    UsingZDataii: boolean;
    /** You must set Data.UsingZDataII=true, Data.Subsets and Data.Points before pass this data. Gets or Sets double precision data plotted */
    Zii: PeArray2D<number>;
    Pedo: {
      /** Read to learn the quantity of X, Y, Z, PointLabels, and PointColors data. */
      Subsets: number;
      /** Read to learn the quantity of X, Y, Z, PointLabels, and PointColors data. */
      Points: number;
      /** Rarely used property, read to learn how two dimensional properties are stored in memory, subset-by-point or point-by-subset. */
      SubsetByPoint: boolean;
      /** Usually set server-side on the Pedo WebForm control. Read to learn server setting. Mostly used internally to control if AppendFromURL appends new data to existing data. */
      DisableAppend: boolean;
      /** Single precision X-Data or optional developer defined data to read after an AppendFromURL function call. For pure data transfers without appending logic, set DisableAppend to TRUE server side within the Pedo Webform control. The quantity of data can be learned from Pedo.Subsets and Pedo.Points. */
      X: PeArray2D<number>;
      /** Double precision X-Data or optional developer defined data to read after an AppendFromURL function call. For pure data transfers without appending logic, set DisableAppend to TRUE server side within the Pedo Webform control. The quantity of data can be learned from Pedo.Subsets and Pedo.Points. */
      Xii: PeArray2D<number>;
      /** Single precision Z-Data or optional developer defined data to read after an AppendFromURL function call. For pure data transfers without appending logic, set DisableAppend to TRUE server side within the Pedo Webform control. The quantity of data can be learned from Pedo.Subsets and Pedo.Points. */
      Y: PeArray2D<number>;
      /** Double precision Y-Data or optional developer defined data to read after an AppendFromURL function call. For pure data transfers without appending logic, set DisableAppend to TRUE server side within the Pedo Webform control. The quantity of data can be learned from Pedo.Subsets and Pedo.Points. */
      Yii: PeArray2D<number>;
      /** Single precision Z-Data or optional developer defined data to read after an AppendFromURL function call. For pure data transfers without appending logic, set DisableAppend to TRUE server side within the Pedo Webform control. The quantity of data can be learned from Pedo.Subsets and Pedo.Points. */
      Z: PeArray2D<number>;
      /** Double precision Z-Data or optional developer defined data to read after an AppendFromURL function call. For pure data transfers without appending logic, set DisableAppend to TRUE server side within the Pedo Webform control. The quantity of data can be learned from Pedo.Subsets and Pedo.Points. */
      Zii: PeArray2D<number>;
      /** PointLabels or optional developer defined data to read after an AppendFromURL function call. For pure data transfers without appending logic, set DisableAppend to TRUE server side within the Pedo Webform control. */
      PointLabels: PeArray1D<string>;
      /** PointColor or optional developer defined data to read after an AppendFromURL function call. For pure data transfers without appending logic, set DisableAppend to TRUE server side within the Pedo Webform control. The quantity of PointColors can be learned from Pedo.Subsets and Pedo.Points. */
      PointColors: PeArray2D<number>;
      /** Optional developer defined data to read after an AppendFromURL function call. For pure data transfers without appending logic, set DisableAppend to TRUE server side within the Pedo Webform control. */
      ExtraStringData: PeArray1D<string>;
      /** Optional developer defined data to read after an AppendFromURL function call. For pure data transfers without appending logic, set DisableAppend to TRUE server side within the Pedo Webform control. */
      ExtraDoubleData: PeArray1D<number>;
      /** Optional developer defined data to read after an AppendFromURL function call. For pure data transfers without appending logic, set DisableAppend to TRUE server side within the Pedo Webform control. */
      ExtraIntData: PeArray1D<number>;
    };
    /** Read only. True if image is a product of data reduction. PeData.Filter2D or PeData.Filter3D and Subsets/Points and size of chart all effect if FilterOn is True. */
    FilterOn: boolean;
    /** Always set this property prior to passing Data.Y and/or Data.X. Gets or Sets quantity of data series. Example: for a line chart, this is the number of lines with different colors. */
    Subsets: number;
    /** Always set this property prior to passing Data.Y and/or Data.X. Gets or Sets quantity of data points per series. Example: for a line chart, this is the number of data points per line. */
    Points: number;
    /** Gets and Sets how two dimensional property arrays (example, Data.Y, Data.X) are stored internally. If you will be adjusting the number of Subsets and/or Points and want to preserve existing data, read the help on this property. */
    SubsetByPoint: boolean;
    /** Set or Get the precision used within table, on-screen coordinate prompting and data exporting. */
    Precision: DataPrecision;
    /** When PeData.JaggedData = True. Set the number of data points a subset will contain and allocate memory. Optional -- rows grow automatically. */
    SetJaggedPointsX(nSubset: number, nNewSize: number): number;
    /** When PeData.JaggedData = True. Get the number of data points a subset contains. */
    GetJaggedPointsX(nSubset: number): number;
    /** When PeData.JaggedData = True. Set the number of data points a subset will contain and allocate memory. Optional -- rows grow automatically. */
    SetJaggedPointsY(nSubset: number, nNewSize: number): number;
    /** When PeData.JaggedData = True. Get the number of data points a subset contains. */
    GetJaggedPointsY(nSubset: number): number;
    /** When PeData.JaggedData = True. Set the number of data points a subset will contain and allocate memory. Optional -- rows grow automatically. */
    SetJaggedPointsZ(nSubset: number, nNewSize: number): number;
    /** When PeData.JaggedData = True. Get the number of data points a subset contains. */
    GetJaggedPointsZ(nSubset: number): number;
    /** When PeData.JaggedData = True. Set the number of data points a subset will contain and allocate memory. Optional -- rows grow automatically. */
    SetJaggedPointsW(nSubset: number, nNewSize: number): number;
    /** When PeData.JaggedData = True. Get the number of data points a subset contains. */
    GetJaggedPointsW(nSubset: number): number;
    /** When PeData.JaggedData = True. Set the number of data points a subset will contain and allocate memory. Optional -- rows grow automatically. */
    SetJaggedPointsPointColors(nSubset: number, nNewSize: number): number;
    /** When PeData.JaggedData = True. Get the number of data points a subset contains. */
    GetJaggedPointsPointColors(nSubset: number): number;
  };
}

export declare function attachApi(core: unknown, h: number): PeChart;